// Populated only after every preliminary SF-P2 original has been inspected.
// Missing entries intentionally fail closed in qa-shared-frame-p2.mjs.
export const SF_P2_REVIEWED_VERDICTS = Object.freeze({})
