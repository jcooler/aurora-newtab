# Aurora 2 Decision Log

ADR-lite entries record settled implementation direction without replaying conversation history. A later decision may supersede an entry, but must name it and explain migration/release consequences.

## A2-D001 — Isolate Aurora 2 from the approved V1 checkout

- **Date:** 2026-08-13
- **Decision:** Develop on `feat/aurora-2-observatory` in `D:\DEV\Chrome plugin-aurora-2`, linked from the `eb1354b` base. Keep `D:\DEV\Chrome plugin` on clean `main` and preserve its local V1 packages.
- **Reason:** Aurora 2 is a major multi-packet release; isolation protects approved V1 source/artifacts and any future user work.
- **Rejected:** Working directly on `main`; creating an unignored project-local `.worktrees/` directory; replacing the staged V1 ZIP.
- **Consequence:** Every handoff names the literal worktree/branch. No push to `main`, Store change, or V1 artifact replacement is implied.

## A2-D002 — Use semantic profiles and zones, never canvas scaling

- **Date:** 2026-08-13
- **Decision:** Implement Compact, Standard, Display, and Ultrawide profiles over Day, Now, Work Pulse, and Signal Dock zones, selected from CSS viewport width/height/aspect ratio. Density is a bounded token/variant choice, separate from profile selection.
- **Reason:** The current percentage-position/fixed-pixel-size model spreads content without increasing hierarchy or detail.
- **Rejected:** Root transform/zoom; one canonical bitmap canvas; more exact height tiers; freeform percentages as the V2 source of truth.
- **Consequence:** Legacy x/y data is migrated and preserved, while new UI uses semantic grid placements and container queries.

## A2-D003 — Store overrides over versioned product defaults

- **Date:** 2026-08-13
- **Decision:** Keep profile defaults in source and persist only user overrides plus a legacy copy. Do not freeze every release default into every user's storage.
- **Reason:** Product defaults can improve across releases without erasing user intent.
- **Rejected:** Full per-user copies of all defaults; destructive one-time replacement of legacy layout.
- **Consequence:** Migration, backup, reset-one-profile, and copy-profile behavior must distinguish defaults, overrides, drafts, and legacy data.

## A2-D004 — Every enabled connector has one active representation

- **Date:** 2026-08-13
- **Decision:** An enabled connector is either placed on the board or represented in the Signal Dock in every active profile. Automatic items reduce variant before docking; Dock priority is explicit; Pinned items remain in their zone after deterministic collision resolution.
- **Reason:** Height-based disappearance is a correctness failure, not a density feature.
- **Rejected:** Silent whole-widget hiding; “newest connector loses”; scroll-only off-canvas placement.
- **Consequence:** Profile and QA tests count configured connectors against board-plus-Dock representations.

## A2-D005 — Utility Tray changes modality with available space

- **Date:** 2026-08-13
- **Decision:** Desktop Tray is modeless/anchored and does not trap focus; narrow Tray may become a true modal bottom sheet with backdrop, inert background, focus trap, Escape, and restoration.
- **Reason:** Desktop tools should remain glanceable beside the dashboard, while narrow screens need one safe focused surface.
- **Rejected:** Always-modal desktop drawer; always-modeless narrow overflow; multiple expanded tools.
- **Consequence:** Responsive mode is behaviorally tested, not just styled. Running timer state remains visible when details close.

## A2-D006 — Aurora Briefing is deterministic and local

- **Date:** 2026-08-13
- **Decision:** Build the Briefing from data Aurora already holds using deterministic priority, freshness, and truncation rules.
- **Reason:** A three-second glance line is the signature product element; an LLM would add privacy, latency, cost, availability, and disclosure burdens without necessity.
- **Rejected:** Remote LLM, new Aurora backend, generated prose from raw external payloads.
- **Consequence:** Briefing has pure-function tests, makes no network request, and suppresses stale/unsafe claims.

## A2-D007 — Keep local plaintext credentials with explicit risk disclosure

- **Date:** 2026-08-13
- **Decision:** Aurora 2 retains connector credentials in `chrome.storage.local` for convenience and describes them as local plaintext protected by the Chrome/OS profile—not encrypted or vault-grade. Capability URLs are secrets. Shared/untrusted profile guidance is mandatory.
- **Reason:** A shipped extension cannot safely hide an embedded encryption key; session/passphrase modes require a separate usability, recovery, and threat-model design.
- **Rejected:** Claiming encryption; cosmetic obfuscation; silently expanding 2.0 with an unplanned passphrase system.
- **Consequence:** Privacy, Settings, backup/export, logs, Store copy, and support guidance use the same honest posture. Session-only/passphrase behavior is outside 2.0 unless separately approved.

## A2-D008 — Scope snapshots with a cryptographic config/account fingerprint

- **Date:** 2026-08-13
- **Decision:** A connector snapshot stores connector ID plus a SHA-256 fingerprint of stable canonical fetch-relevant configuration. Successful token connections also stamp a new non-secret lifecycle epoch so an identical reconnect has a new scope. Raw tokens and capability URLs never appear in the stored scope string. Legacy unscoped snapshots are ignored as cache and need no schema migration.
- **Reason:** Config/account identity must change cache usability immediately and safely, including RSS/ICS URLs whose full values can be credentials.
- **Rejected:** Connector ID alone; raw config in cache keys; username-only identity; manual invalidation at a growing list of settings call sites.
- **Consequence:** The first Wave 1 packet updates the shared hook and all connector call sites, adds commit-time generation invalidation plus generation/TTL/visibility behavior, preserves the epoch through ordinary Home Assistant config edits, and tests stale completion order. A rejected hook refresh may keep only matching stale data; existing Status/Home Assistant anti-staleness sentinels remain authoritative.
- **Verification:** Implemented through `cd511f06c3758ce9b237f6a493087376e34886f8`. The bounded review found one queued-update race; W1-P1 added an updater-time generation guard and a red/green blocked-queue regression. Final evidence is 12 targeted files / 409 tests, 97 total files / 1,531 tests, clean TypeScript, and a clean production build, with no review findings left open.

## A2-D009 — Prefer a global Web Lock as the cross-context write authority

- **Date:** 2026-08-13
- **Decision:** The storage-integrity packet first verifies and uses a global Web Lock around Aurora storage mutations and restore transactions. If MV3 extension-page verification fails, stop and checkpoint before adopting a background service-worker authority.
- **Reason:** Current per-context promise chains cannot prevent lost updates. A global lock preserves the existing updater API and allows multi-key restore coordination without inventing fake compare-and-set semantics over `chrome.storage.local`.
- **Rejected:** Context-local queues; unproven revision retries without atomic CAS; granular keys alone; silently falling back to last-write-wins.
- **Consequence:** The packet includes two simulated contexts and real extension-page evidence. Platform failure materially changes architecture and therefore triggers a decision checkpoint.
- **Verification:** Implemented through `fac883ba4cccf6f567a8460188a84902d219730c`; the service-worker fallback was not taken. Timing-free simulated tests prove the unlocked lost-update control and the shared-authority success path. Two exact-path MV3 extension pages both exposed Web Locks and retained all 50 concurrent mutations. Final evidence is 7 targeted files / 312 tests, 98 total files / 1,544 tests, clean TypeScript, clean 165-module production/preview builds, no production harness bridge, and a 412 PASS / 0 FAIL / 3 SKIP real-extension run. The bounded implementation rereview found no Critical, Important, or packet-local Minor findings.

## A2-D010 — Correct behavior before final Store prose

- **Date:** 2026-08-13
- **Decision:** Wave 1 creates a code-backed data classification and fixes misleading source copy, but Wave 6 performs current official-policy verification, live dashboard reconciliation, final listing/Data Usage copy, screenshots, and submission checklist.
- **Reason:** Disclosures must describe the final network, storage, permission, and backup behavior; the repository cannot reveal the live Store version or dashboard answers.
- **Rejected:** Guessing the live version; relying on remembered policy text; changing the live listing during development.
- **Consequence:** Live Store version stays “user/dashboard verification required,” V1 stays live, and external mutations remain explicit approval gates.

## A2-D011 — Treat the repeatable permission harness failure as open baseline evidence

- **Date:** 2026-08-13
- **Decision:** Record both 408 PASS / 1 FAIL / 3 SKIP runs and route `remove revokes live` to W1-P3 instead of patching it inside Wave 0.
- **Reason:** The failure repeated identically, but Wave 0 is documentation/isolation/baseline only and the defect belongs to the accepted permission-lifecycle subsystem.
- **Rejected:** Calling the run green because the probe was historically flaky; expanding Wave 0 into permission implementation.
- **Consequence:** W1-P3 cannot be Verified until the real-browser probe passes or the user approves an evidence-backed disposition.
- **W1-P3 resolution:** The old row/dot probe now passes and is renamed to state exactly what it proves: live Status storage/UI removal. It never seeded a held native permission, so it cannot prove native revocation. A separate preview-adapter matrix drives production transaction code and proves adapter-held rollback, shared/final ownership, revoke failure/retry, and final removal. Revoking an actually held native Chrome host grant remains unautomated headlessly and is not claimed fixed. Final harness evidence is 424 PASS / 0 FAIL / 3 SKIP.

## A2-D012 - Coordinate optional-permission ownership with a startup mirror and one lifecycle lock

- **Date:** 2026-08-14
- **Decision:** Initialize a page-lifetime permission mirror before React by subscribing to `onAdded`/`onRemoved` before one `getAll` seed. A user gesture synchronously snapshots held origins, queues `aurora:origin-permission-lifecycle:v1`, and requests only snapshot-absent patterns before the first await. The same lock covers validation/persistence, every owner-changing write, fresh global owner reads, rollback, remove verification, and retry. Connector descriptors plus APOD form the extensible owner registry; configured disabled connectors remain owners while incomplete configs do not. Revoke failure persists in one Settings-level accessible Retry surface.
- **Reason:** Per-click asynchronous classification can lose the Chrome user gesture, blanket rollback can remove pre-existing grants, and connector-local release checks cannot safely handle same-host, cross-connector, APOD, or cross-context ownership.
- **Rejected:** Awaiting `getAll`/storage/the lock before request; requesting already-held patterns; fixed-delay `onAdded` classification; connector-local `releasableOrigins`; swallowed remove failures; a context-local production lock fallback; body-local retry state that unmounts with the connector.
- **Consequence:** W1-P4 restore must use the same lifecycle authority and ownership registry when it later changes owners. Preview automation may inject only the six-surface permission adapter before mirror initialization; production builds exclude its exact global name. Adapter-held evidence is never described as a native Chrome grant.
- **Verification:** Implemented through `a8d62367db97ba42b2b70047b7d0414df6019a90`. Final evidence is 18 targeted files / 631 tests, 103 total files / 1,631 tests, clean TypeScript, clean 170-module production/preview builds, no production adapter-name match, and a 424 PASS / 0 FAIL / 3 SKIP real-extension run. The bounded whole-packet rereview found no Critical, Important, or packet-local correctness issue open.

## A2-D013 - Make backup restore secret-safe, gesture-safe, and atomically recoverable

- **Date:** 2026-08-14
- **Decision:** Export strips registered token fields, every RSS feed URL, legacy and multi-calendar ICS URLs, connector snapshots, and APOD cache; it records only trusted connector IDs that need re-entry plus fixed guidance. Import fully parses, migrates, cleans, validates, reconciles metadata, and derives restored origins before Confirm. Confirm enters the existing origin lifecycle transaction synchronously, then performs one authority-held all-key storage replacement with target and rollback readback verification. Irreversible old-owner permission cleanup runs only after storage commits, while the lifecycle lock remains held. Cleanup failure commits data and becomes durable Settings Retry; it never reopens storage rollback.
- **Reason:** Capability URLs are credentials, a backup file cannot grant Chrome permissions, and storage exactness alone is insufficient if rollback or cleanup can lose a pre-existing grant. Gesture timing, storage atomicity, and permission ownership must compose under one explicit lock order.
- **Rejected:** Exporting RSS/ICS URLs; connector-ID switches in backup code; requesting permissions after an awaited preparation step; direct `setMany` restore; best-effort rollback without readback; revoking old owners inside a rollback-participating finalizer; claiming adapter-held patterns are native grants; swallowing cleanup failure.
- **Consequence:** Lock order is origin lifecycle then storage authority. Exact backup/rollback state is logical current `AuroraData`; unknown driver keys and the schema-version key remain outside the backup boundary. Legacy metadata-free token shapes may receive trusted exact reconnect labels, while ambiguous legacy Calendar shapes receive only generic guidance. Preview automation uses the existing permission adapter and real Data controls, then destroys the old document and performs a named-lock all-key final restore so late connector-cache writes cannot contaminate downstream tests. Native Chrome Allow/Block and a live Home Assistant picker remain explicit headed ceilings.
- **Verification:** Implemented through `6f30b915719586cb519aa0f8f2b4418b0724debe`. Final evidence is 15 targeted files / 600 tests, 104 total files / 1,694 tests, clean TypeScript, clean 171-module production/preview builds, no production preview-symbol match, and a controller-owned 432 PASS / 0 FAIL / 3 SKIP real-extension run. The bounded whole-packet review fix `d28d6f5` preserved old grants across rollback-triggering failure; controller verification fix `6f30b91` quiesced late connector writes during teardown. Final scoped reviews found no Critical, Important, or packet-local correctness issue open.

## A2-D014 - Minimize Home Assistant polling and make action state generation-safe

- **Date:** 2026-08-14
- **Decision:** Reserve bulk `GET /api/states` for the explicit entity picker and disclose that boundary. Regular dashboard polling dedupes selected IDs and requests authenticated `GET /api/states/{entity_id}` endpoints in selection order; an isolated 404 is omitted, while authentication, network, other HTTP, malformed, or wrong-identity results fail the whole poll to `{ entities: null }`. An action-only configuration must pass authenticated `GET /api/` health with a non-empty message. Home Assistant advances the existing opaque W1-P1 snapshot identity to `v2` so fresh legacy action-only snapshots cannot impersonate a new health result. Each action owns a synchronous local pending guard and an in-memory generation over snapshot epoch, instance URL, token, action ID, and domain; stale completions cannot update feedback or release newer work. Pending/success use associated visible status text, failure uses an associated persistent alert, and retry remains available.
- **Reason:** Bulk polling discloses unrelated household state, a local empty result is not proof that the instance is reachable, old cache meaning changed when health became mandatory, and overlapping or stale service promises can duplicate actions or misreport a reconfigured card.
- **Rejected:** Regular bulk `/api/states` plus local filtering; unauthenticated or no-network action-only visibility; partial data after poll-wide failure; raw config/cache keys; a widget-side cache bypass; global action locks; render-time generation mutation; transient color-only result tint; new action permissions, notifications, retries, WebSockets, or production harness bridges.
- **Consequence:** W1-P1 remains the sole poll cache/generation/queued-write authority and W1-P3 remains the permission authority. Tokens stay in headers and opaque SHA-256 scopes, not URLs/logs. Picker disclosure matches runtime. The real-extension harness may prove natural failure, keyboard paths, Chromium AX semantics, and teardown with a seeded unreachable instance, but live picker contents, successful service execution, and a real screen reader remain explicit user-instance/manual ceilings.
- **Verification:** Implemented through `74d4e27a6fa1262416ac8aa0149020a0ef02918e`. Final evidence is 10 targeted files / 378 tests, 104 total files / 1,709 tests, clean TypeScript, clean 171-module production/preview builds, no production preview-symbol match, and a controller-owned 437 PASS / 0 FAIL / 3 SKIP real-extension run. `screenshots/ha-action-error.png` and the Chromium accessibility tree were inspected. The bounded whole-packet review's legacy-snapshot finding was fixed in `2ab1139`; controller verification's preview-scope mismatch was fixed in `74d4e27`; final scoped rereviews found no Critical, Important, or packet-local correctness issue open.

## A2-D015 - Scope Weather cache to the provider request and make request ownership generation-safe

- **Date:** 2026-08-14
- **Decision:** A Weather snapshot is usable only when its `requestIdentity` equals a canonical public identity built from finite in-range coordinates normalized to four decimals and the complete immutable Open-Meteo forecast contract: provider/path, Celsius, km/h, ISO 8601, auto timezone, 12 forecast hours, one forecast day, and the exact current/hourly/daily field lists. Legacy or mismatched caches never render. Every forecast owns an `AbortController` and generation; only the current mounted identity may change UI or enter an authority-held `updateMany`, which revalidates the stored location before writing. Automatic refresh waits for both `location` and `weatherCache` hydration, fires at age greater than or equal to 30 minutes, converges on visibility, and dedupes same-identity work. Location and cache changes commit atomically.
- **Reason:** Labels are not location identity, partial cache keys drift from real provider inputs, independent storage hydration can manufacture a false cache miss, and abort alone cannot prevent late or cross-context completion from overwriting a newer selection.
- **Rejected:** Label-based cache matching; coordinates without units/provider fields; render-time migration of legacy cache; caller-only invalidation; abort without generations; write checks outside the storage authority; separate location/cache writes; refresh before both persisted keys hydrate; claiming headless page focus proves a native visibility transition.
- **Consequence:** Display-unit changes remain presentation-only and do not refetch the canonical Celsius cache. Provider-contract changes intentionally invalidate existing snapshots by changing identity. Backup validation accepts only bounded coordinates and string identities, while imported legacy snapshots remain safely unusable. The preview harness routes real production forecast requests and models only the unavailable visibility signal through the real listener; exact fake-time unit tests are the authoritative native-listener/fencepost evidence.
- **Verification:** Implemented through `f4ed9335dcbb74aa35b685eb0149667daaefd31e`. Final evidence is 17 targeted files / 532 tests, 106 total files / 1,751 tests, clean TypeScript, clean 172-module production/preview builds, no production preview-symbol match, and a controller-owned 443 PASS / 0 FAIL / 3 SKIP built-extension run. The first post-review full run exposed the hydration race at 441 / 2 / 3; a delayed-cache-read red/green regression fixed it in `f4ed933`. Final independent rereviews found no Critical, Important, or packet-local Minor issue open.

## A2-D016 - Construct local days in their IANA zone and own every rollover generation

- **Date:** 2026-08-14
- **Decision:** Aurora derives local-day keys and first-valid day boundaries from calendar fields in the requested IANA zone. Wall-time inversion samples a constant bounded set of offsets, validates exact round trips, chooses the earlier instant for overlaps, and chooses the least displayed wall time after a gap. One `useLocalDay` schedule owns a generation and converges at the next boundary, at a bounded probe interval, and after visibility, focus, pageshow, sleep/wake clock discontinuity, backwards clock movement, or timezone change. `useNow` shares restoration-aware resampling. APOD day ownership is revalidated inside the W1-P2 authority. ICS events carry explicit `allDay`; DATE recurrence/end arithmetic is calendar-based, timed midnight stays timed, and ICS v2 snapshot identity includes the runtime timezone.
- **Reason:** Adding 24 hours cannot represent local civil days across DST or political timezone changes, naive offset settling can return the previous civil day at midnight transitions, and render-time dates or unowned timers leave open tabs stale after midnight, sleep, or zone changes. Inferring all-day status from a midnight clock also corrupts timed events, while timezone-independent ICS caches can reuse wrong absolute instants.
- **Rejected:** Fixed 24-hour day arithmetic; unbounded or repeatedly expired timer loops; a single offset guess; treating nonexistent midnight as the previous day; component-local midnight timers; reload-only freshness; timed-midnight inference; timezone-blind ICS snapshots; allowing stale APOD completion or `finally` to overwrite or release newer ownership.
- **Consequence:** Background, Focus, Countdown, Quote, Calendar, Greeting, clocks, habits, sun/moon, and Timer consume current local-day/restoration signals without changing layout. Existing W1-P1/W1-P2 storage and generation authority remains the write boundary. Deterministic browser evidence may advance a disposable extension page's clock only if it restores an advancing shared context; genuine operating-system timezone changes and machine sleep/wake remain explicit manual environment gates backed by exact production-path tests.
- **Verification:** Implemented through `2fcb4438b0857500db1e5fb0785e1da45e9eacd8`. Final evidence is 23 targeted files / 563 tests, 110 total files / 1,831 tests, clean TypeScript, clean 173-module production/preview builds, no production preview-symbol match, and a controller-owned 447 PASS / 0 FAIL / 3 SKIP built-extension run. The bounded review found a Critical midnight-transition inverse defect affecting Havana, Santiago, and Azores plus an expired-boundary one-millisecond loop; red regressions reproduced it before the shared bounded inverse fix. The focused rereview found no remaining Critical, Important, or packet-local correctness finding.

## A2-D017 - Make Notes persistence revision-owned and every destructive exit recoverable

- **Date:** 2026-08-14
- **Decision:** Notes owns one latest editable revision, one authority-backed write in flight, a coalesced drain, and persistent recoverable error state. Saving begins with an unsaved edit; Saved appears only after the exact latest revision fulfills. Exact self-echoes and later authority-ordered external values reconcile without using timestamps as ordering authority. Pill, Escape, disable, and arrange entry call one awaited close boundary; the shared dialog stack closes newest-first and stops on false/rejection. A dirty-only beforeunload handler starts the same flush and requests the standards-based browser warning.
- **Reason:** Fire-and-forget debounce plus immediate unmount could lose the visible draft, report success before persistence, allow external subscription values to replace local edits, or inert a failed editor under arrange mode. Current-tab navigation needs an honest retention opportunity without claiming that browsers await asynchronous work after confirmed unload.
- **Rejected:** Raw production driver writes; local/session draft shadow stores; multiple concurrent Notes writes; timestamp-based ordering; clearing or closing on rejection; synchronous stack clearing; entering arrange before close settlement; promising unload-time durability; expanding W1-P8 into privacy copy or shared Wave 2 primitives.
- **Consequence:** W1-P2 remains the sole mutation authority. Error stays visible through later edits and Retry-in-flight; Retry owns a 36px target inside the focus trap. Preview automation may control only Notes driver writes behind the existing preview-only bridge, closes the disposable editor before a storage-lock teardown barrier, and never exposes note text in controller snapshots. Explicitly confirmed navigation retains the platform's unavoidable best-effort limit.
- **Verification:** Implemented through `5c570f7087d8d6e41b73db9e271339c75c93f62c`. Final evidence is 13 targeted files / 173 tests, 112 total files / 1,848 tests, clean TypeScript, clean 174-module production/preview builds, no production preview-symbol match, and a controller-owned 451 PASS / 0 FAIL / 3 SKIP built-extension run. `screenshots/w1-p8-notes-error.png` and Chromium AX output were inspected. The bounded review's two Important target-size/teardown findings were fixed in `5c570f7`; focused rereview found no remaining Critical, Important, or packet-local Minor issue.

## A2-D018 - Make privacy classification executable and enforce one Quick Link trust boundary

- **Date:** 2026-08-14
- **Decision:** A typed inventory exhaustively records every `AuroraData` key, local schema metadata, IndexedDB photos, fixed/browser-mediated flow, and connector destination, trigger, method, sent/received data, permission, cache, account, secret, backup, and backend boundary. Authentication fields remain distinct from RSS/Calendar capability URLs, while both are local plaintext protected by the Chrome/OS profile and redacted from backup. One pure Quick Link policy permits only parseable HTTP(S) without URL credentials, rejects malformed explicit schemes, and is enforced at add, import, and render. Repository manifest/privacy/listing source says “No Aurora account”; the listing remains interim source with current official policy/dashboard reconciliation deferred to W6-P3. The indirect development-only advisory is resolved only through exact `overrides.nanoid = "3.3.18"`.
- **Reason:** Prose-only inventories drift from schema/connector growth; capability URLs can grant read access without being account credentials; input-only validation leaves imported or legacy stored navigation unsafe; “no accounts” is false once optional third-party connectors exist; and a broad dependency upgrade would exceed the packet.
- **Rejected:** Treating local storage as encrypted/vault-grade; exporting RSS/Calendar capability URLs; input-only Quick Link checks; allowing URL userinfo or `javascript:`, `data:`, `file:`, Chrome, or malformed HTTP(S) forms; claiming a live Store/dashboard update; guessing final Limited Use mappings; a direct nanoid dependency or broad Vite/PostCSS upgrade.
- **Consequence:** Connector Settings warns about local plaintext and shared/untrusted profiles. Backup and render reject unsafe Quick Links. Source copy records direct provider transmission, exact Home Assistant and APOD behavior, and the W6 policy gate without changing V1 or any live Store state. Preview navigation probes must use policy-valid routed HTTPS destinations rather than test-only extension-scheme Quick Links.
- **Verification:** Implemented through `34f8960`. Final evidence is 6 targeted files / 362 tests, 114 total files / 1,890 tests, clean TypeScript, clean 176-module production/preview builds with exact manifests and no production preview bridge, zero production/full audit findings, only `nanoid@3.3.18` in the tree, and 451 PASS / 0 FAIL / 3 SKIP in the built-extension harness. Independent implementation review's three Important findings were fixed; final focused rereview returned Ready.

## A2-D019 - Treat packet boundaries as continuous-run checkpoints

- **Date:** 2026-08-14
- **Decision:** Preserve one just-in-time envelope, plan, TDD cycle, bounded independent review/fix round, verification set, implementation commit, ledger checkpoint, push, and clean-state proof per packet, but do not end the session or create a continuation prompt at every packet boundary. After the packet checkpoint is clean and equal to upstream and the protected original is reverified, continue automatically to the next Not started `ROADMAP.md` packet. One new-session prompt may hand off the whole remaining roadmap.
- **Reason:** Durable repository ledgers and context compaction provide continuity. Repeated per-packet prompts add handoff overhead without strengthening packet isolation, review, or evidence.
- **Rejected:** One combined plan or commit for all remaining work; removing packet reviews, checkpoints, or clean-state proofs; silently crossing a user, manual-evidence, architecture-authority, or external Store gate.
- **Consequence:** W2-P1 through W6-P4 may run continuously with just-in-time packet plans. Pause only for a genuine blocker, a material decision requiring new authority, required user/manual evidence (including the Wave 6 dashboard boundary), an explicit handoff request, or program completion. W6-P5 remains a hard stop before any upload, submission, publication, rollout, or live Chrome Web Store listing mutation unless contemporaneous explicit approval is received. Aurora V1 remains protected.
- **Verification:** The master specification and active status protocol were updated. Historical completed packet plans remain unchanged as evidence and do not override this decision for future work.

## A2-D020 - Keep async outcome and freshness orthogonal behind shared render-only semantics

- **Date:** 2026-08-15
- **Decision:** Model operation as exactly `idle | pending | success | error` and freshness as exactly `unknown | fresh | stale`, with data presence independent from both. Derive these values through pure clock-explicit helpers, then render caller-owned literal copy through shared visible polite atomic status, atomic alert, and resource-feedback components. Healthy/fresh state is quiet. Retained stale data remains visible while refresh is pending and becomes an honest polite offline state after failure; no caught provider message is surfaced. Notes retains its prior error independently while retry remains pending.
- **Reason:** A single loading/success/error flag cannot truthfully represent cached data during refresh or failure, while duplicated per-widget live regions drift in politeness, atomicity, association, target size, and retry behavior. Separating pure derivation from rendering provides one reusable accessibility contract without creating a new async owner.
- **Rejected:** Collapsing freshness into operation; hiding cached data during retry/failure; announcing healthy state continuously; exposing raw network errors; nested live regions; renderers that start/retry/cancel/persist work; moving connector, Weather, Notes, or Home Assistant ownership into the primitive.
- **Consequence:** `useConnectorSnapshot` remains the scope/generation/TTL/dedupe/cache authority; `useWeather` remains the coordinate identity/abort/generation/storage authority; Notes remains revision-owned and serialized; Home Assistant retains synchronous per-action exclusion and five-input generations. Feedback controls use stable descriptions, disabled/busy truth, keyboard-operable recovery, and the 36 CSS px routine target floor. W2-P2 focus/naming/boundary recovery and W2-P3 narrow reflow remain separate packets.
- **Verification:** Implemented through `f2c1b788d6f341eb0137a05f270e7a531a4db9af`. Final evidence is 10 targeted files / 187 tests, 116 total files / 1,929 tests, clean TypeScript, clean 178-module production/preview builds, no production preview bridge, zero production/development audit findings, and 452 PASS / 0 FAIL / 3 SKIP with exactly one named W2-P1 PASS. Six state captures plus two Weather recovery captures were inspected at original resolution. Chromium FullAXTree evidence supports named status/alert/button/description/disabled/busy semantics but is not a real screen-reader run. The whole-packet review's 3 Important and 1 Minor findings were fixed; focused rereview found no new Critical/Important breakage.

## A2-D021 - Keep recovery focus invoker-owned and make repeated sources explicit

- **Date:** 2026-08-15
- **Decision:** Editor and dialog callers own stable invoker references and restore them after every successful close transition, including Escape, backdrop, Cancel, and commit, even when async loading disables or replaces the trigger before the dialog trap mounts. Focus and Quick Link editors keep committed state separate from drafts, cancel without writes, and associate validation text programmatically. Repeated Calendar content includes its source in programmatic names without duplicating visible copy. Home Assistant picker domains are explicit named groups. Drawer boundaries retry only after the failed subtree is closed and reopened. Data export, validation, confirmation, pending restore, success, and failure reuse shared render-only status/alert semantics while existing backup/restore code remains the async and persistence authority.
- **Reason:** Trap-local previous-focus snapshots are unreliable when async work changes the trigger before mount; identical visible Calendar rows are ambiguous to assistive technology; retrying a still-open failed boundary can preserve corrupt subtree state; and silent backup/restore transitions hide whether an operation is pending, rejected, or complete.
- **Rejected:** Relying only on `document.activeElement` inside the dialog; making the focus trap own caller lifecycle; duplicating source names visually; closing editors after rejected persistence; boundary retry without reopen; moving export/import/restore work into feedback components; weakening the 36 CSS px target floor or existing bottom-band geometry gates.
- **Consequence:** Callers pass stable trigger ownership through dialog close transitions. Focus and Quick Link behavior stays keyboard-complete without widening into W2-P3 reflow. Calendar source identity is available through accessible names while the visual density remains unchanged. Shared feedback renders caller-owned literal state and descriptions only. The 36px Focus prompt input preserves its physical target while an exact responsive margin-box compensation retains the pre-packet centered-flow clearance at legacy bottom-band fenceposts.
- **Verification:** Implemented through `1fdaf0c9c8192b10b53d732b552aa656f1ca23a3` after `5db8dc44121fd9bece4cf670d8d04d9d337de3c3` fixed the whole-packet review. Final evidence is 16 targeted files / 661 tests, 117 total files / 1,962 tests, clean TypeScript, clean 178-module production/preview builds, no production preview bridge, zero production/development audit findings, and 453 PASS / 0 FAIL / 3 SKIP with exactly one named W2-P2 PASS. Six exact captures were inspected at original resolution; Chromium FullAXTree is supporting evidence, not a real screen-reader run. The final independent rereview was Ready with no Critical or Important issue open.

## A2-D022 - Reflow current surfaces through shared viewport-fit ownership without claiming native zoom

- **Date:** 2026-08-15
- **Decision:** Keep the current modal Settings drawer and existing tool/dialog/popover architecture while W2-P3 reflows only their present content. Settings uses a narrow contained layout and exact local target floors. Notes, Tasks, and Timer share an 8px viewport-fit owner that measures both current invoker and rendered panel boxes and fences resize/ResizeObserver/rAF work by open generation. Dialogs and popovers own one vertical scrollport, preserve real Tab or active-descendant behavior, and recompute from unshifted geometry without cumulative drift. A 320 CSS px viewport, including 320x180 short space, is authoritative automated reflow evidence but is not native Chrome 400% zoom evidence.
- **Reason:** Preferred constants, nowrap Settings rows, and post-open-only placement allowed current surfaces to clip, drift, hide controls, or leave active descendants offscreen at constrained CSS viewports. One geometry owner and explicit surface-local scroll/target contracts provide deterministic convergence without introducing Adaptive Stage, a Utility Tray replacement, or root scaling.
- **Rejected:** Root `zoom` or transforms; user-agent/browser-zoom detection; making the page/body horizontally scroll; multiple competing surface scrollports; remounting focused panels on resize; moving persistence/async ownership into layout code; weakening visibility/hit-test predicates; using a global solid-white muted token to solve one Focus prompt's photo-background contrast.
- **Consequence:** W3 inherits exact current-surface reflow, focus, lifecycle, and ordinary-placement contracts. Native Chrome zoom, Windows scaling/mixed-DPI, and real screen-reader timing remain W6-P2/manual evidence. The Focus prompt's background-independent readability is tracked for W5-P4 as a local visual/contrast treatment with representative photo-background screenshots, rather than a global muted-token override.
- **Verification:** Implemented through `64675d245df08d126ab5610b807931ac47320b8e` after whole-review fixes in `ced5e1cfc068426a8856b40bca8c22bd544b7e17`. Final gate is 21 plan-focused files / 475 tests, 119 total files / 1,998 tests, clean TypeScript, clean 179-module production/preview builds, no production preview bridge, zero production/development audit findings, and 454 PASS / 0 FAIL / 3 SKIP with exactly one named W2-P3 PASS. Fifteen exact captures were inspected at original resolution. The same whole reviewer marked all findings Addressed and returned Ready with no Critical or Important issue open.

## A2-D023 - Preserve legacy intent as versioned all-profile overrides under verified migration authority

- **Date:** 2026-08-15
- **Decision:** Aurora storage schema v10 represents layout as a version-2 envelope containing only per-profile user overrides plus optional exact legacy provenance; product defaults remain source-owned. A valid legacy percentage map is copied without mutation and deterministically mapped into Compact, Standard, Display, and Ultrawide using versioned semantic anchors, fixed zone tie order, binary stable block IDs, and dense per-zone collision normalization. Existing percentage rendering and Arrange cross narrow compatibility helpers until their scheduled W3 replacements. Startup migration validates the complete target before its first write, holds the shared storage authority through exact readback or verified rollback, and retains fixed safe public errors. Backup versions 1-9 migrate before current validation; version 10 is cleaned and restored as strict Layout V2.
- **Reason:** Mapping only one profile would silently discard customization at a later viewport transition, persisting product defaults would freeze future design changes, and an unverified startup write could strand partial schema state or release the authority before recovery. Explicit compatibility seams preserve current behavior without coupling the W3-P1 storage contract to the W3-P2 renderer or W3-P3 editor.
- **Rejected:** Persisting a complete default board; mapping legacy positions only into Standard; locale-sensitive or insertion-order collision resolution; clamping the preserved legacy copy; accepting malformed known rows; treating partial versionless data as a fresh install; migration writes without exact readback/rollback; replacing unrelated semantic overrides on one legacy move; implementing profile selection, capacity, BoardItem, Dock, or semantic editing in W3-P1.
- **Consequence:** W3-P2 may consume a stable profile/placement contract while source defaults remain evolvable. Migrated users retain exact legacy recovery data and equivalent current percentage rendering across the compatibility interval. Fresh stores contain an empty override envelope, current Arrange updates only the moved block across all profiles, reset writes the fresh envelope, and restore rollback preserves exact V2 profile/legacy state. Legacy retirement remains gated on W3-P4 parity and rollback evidence.
- **Verification:** Implemented through `925438f890de62d5540221b86b53330f2dd0a5a1`. Final evidence is 11 targeted files / 590 tests, 120 total files / 2,085 tests, clean TypeScript, clean 180-module production/preview builds, no production preview bridge, zero production/development audit findings, and 455 PASS / 0 FAIL / 3 SKIP with exactly one named W3-P1 PASS. Browser evidence proves exact all-four-profile migration, unknown-ID removal, unchanged representative centers, current-V2 prepare/commit, exact layout/version, complete restore/page-close/lock-crossing cleanup, and `errors: []`. Whole-packet and separate gate-fix reviews ended Ready with no Critical or Important issue open.

## A2-D024 - Freeze the Adaptive Stage profile, density, registry, allocation, and schema-v11 contract

- **Date:** 2026-08-16
- **Decision:** Select Compact, Standard, Display, and Ultrawide from CSS viewport pixels using the frozen precedence; resolve Auto Fit from spacious through balanced to compact only when exact viewport geometry fits and no automatic item is newly docked; persist the independent four-value density preference in schema v11; and render every active source-registry entry exactly once through deterministic Day, Now, Pulse, or Dock allocation. The registry remains source-owned, BoardItem owns finite geometry/container semantics and safe local failure isolation, Clock protection and pinned intent remain deterministic, and the root is never scaled. Keep the multi-entry percentage draft bridge only while current Arrange is active.
- **Reason:** A pure deterministic planner and one immutable registry prevent viewport, enumeration, and renderer drift while preserving stored user intent. Independent density persistence avoids coupling layout reset to display preference. Finite calibrated wrappers allow existing widgets to adapt without hiding enabled connectors, leaking failure values, or changing storage/privacy authorities.
- **Rejected:** Device-pixel or native-display profile inputs; root transforms or global typography shrinkage; storage-owned registry identity/defaults; silently dropping or duplicating active widgets; treating intentional Dock dispositions as Auto Fit failures; deleting legacy layout/provenance before editor parity; broad widget redesign inside W3-P2.
- **Consequence:** W3-P3 edits semantic per-profile placements and owns preview, save/cancel, undo, reset-one, copy-profile, lock, and keyboard placement semantics against this frozen model. W3-P4 may remove `layout.legacy`, `PositionedBlock`, and legacy CSS only after W3-P3 parity and rollback evidence. W4 retains content/condensed-Dock redesign ownership. Schema, backup, storage authority, permissions, privacy, and Store boundaries remain unchanged except for the reviewed v11 density field.
- **Verification:** Implemented through `ad79a523c81e51b593b7b57b9ba5cd45492873aa`. Final evidence is 129 files / 2,254 tests, clean 186-module production/preview builds, no production preview bridge, a successful 234-profile calibration, and exactly 456 PASS / 0 FAIL / 3 SKIP with the named W3-P2 aggregate green and clean teardown/error evidence. Six required captures passed original-resolution inspection. The bounded review/fix cycle ended Ready with no Critical or Important finding open; stale evidence naming/explanatory commentary and cosmetic edge cases remain Minor ledger items only.

## A2-D025 - Edit semantic profiles as immutable drafts and persist only on Save

- **Date:** 2026-08-16
- **Decision:** Arrange edits the active W3-P2 semantic profile through immutable draft operations over source-registry entries. Reorder, eligible-zone moves, allowed variants, priority/Dock intent, bounded spans, and lock state preview through the production planner without writing storage. Undo restores the latest draft state; Cancel restores the exact pre-session layout; reset affects only the active profile; copy uses the complete source registry so disabled-widget placements survive; and Save updates only the active profile under the existing serialized storage authority while preserving sibling profiles and exact legacy provenance. Escape cannot turn an in-flight Save into an apparent Cancel.
- **Reason:** Draft-only preview keeps experimentation reversible, while active-profile replacement avoids accidental loss of stored intent at other viewport profiles. Registry-driven copy preserves latent customizations for currently disabled widgets and keeps semantic editing aligned with the frozen allocation contract.
- **Rejected:** Percentage dragging as the W3-P3 editor; writing on each preview edit; resetting all profiles; copying only active widgets; regenerating sibling profiles from defaults; deleting legacy provenance before the W3-P4 rollback gate; adding new schema, registry, permission, or privacy contracts.
- **Consequence:** W3-P4 may retire percentage rendering and compatibility code only after explicit parity and rollback evidence. W3-P3 does not authorize schema/registry changes or Store action. Historical percentage-era harness labels are Minor evidence debt and do not reopen the verified packet.
- **Verification:** Implemented through `314bcbd`. Final evidence is 130 files / 2,232 tests, clean TypeScript, clean 182-module production/preview builds, no production preview bridge, one review/fix/rereview cycle ending Ready, and a focused real-Chromium W3-P3 PASS with three inspected captures and zero runtime errors. The single allowed legacy full-harness rerun reached 261 PASS / 0 FAIL / 3 SKIP before an unrelated Weather convergence fixture stopped execution; that causal fixture was fixed without a third full rerun.

## A2-D026 - Retire percentage runtime while retaining frozen rollback provenance

- **Date:** 2026-08-16
- **Decision:** Delete all live viewport-percentage rendering/editing, snap, clamp, pill-dodge, and legacy placement mutation paths after W3-P3 semantic parity. Remove height-tier classes capable of hiding an entire enabled BoardItem. Retain the frozen optional `LayoutV2.legacy` member, legacy validation/mapping, schema-v9 migration, and backup/restore handling solely as exact stored rollback provenance; semantic rendering and editing never read it.
- **Reason:** The Adaptive Stage and semantic profile editor now replace percentage placement completely, so keeping unreachable runtime paths would create competing layout authorities. Removing the frozen stored member would instead mutate schema, migration, backup, and user-data contracts without an authorized migration or product benefit.
- **Rejected:** Deleting `layout.legacy` from schema v11; rewriting old backups or migrated stores; retaining dormant percentage renderers for speculative rollback; treating internal content-detail tiers as whole-widget hides; redesigning W4-owned variants inside W3-P4.
- **Consequence:** W4 starts from one semantic layout authority. Migration/import can still recover and preserve exact legacy data, but no viewport or Arrange behavior depends on it. Historical percentage-era evidence naming and internal content tiers are recorded as Minor follow-up and do not reopen W3-P4. Registry, storage authority, permissions, privacy, and Store contracts remain frozen.
- **Verification:** Implemented through `e4cebe4`. Focused source/layout/App/Arrange/backup/migration/widget gates passed; final unit, TypeScript, 182-module production/preview builds, and production bridge scan passed; the single causal full-harness rerun finished at 457 PASS / 0 FAIL / 3 SKIP. The focused real-Chromium W3-P4 proof preserved exact legacy data, produced identical allocation without it, kept GitHub/GitLab/Vercel represented once without percentage positioning at Compact/Standard/Display, restored its preimage, and reported zero runtime errors. Three captures passed original-resolution inspection; review found no Critical/Important issue.

## A2-D027 - Keep Day context render-only and preserve the frozen Now hierarchy across responsive capacity

- **Date:** 2026-08-16
- **Decision:** Render a semantic compact/long local-date context only when Day has no allocated BoardItem, without adding registry identity or planner capacity. Keep Day as the shared glance surface and Now as a calm photo-forward anchor. Clock remains primary, followed by Greeting, Search, and Focus in exact semantic order. Expanded Clock variants expose a visible, assistive-technology-readable long date from the same `useNow` sample; Compact remains concise. Under the frozen Compact capacity, Clock/Greeting remain in Now and Search/Focus continue in Dock, preserving their combined order rather than mutating registry footprints or Stage capacities.
- **Reason:** Sparse Day otherwise communicates no temporal context, while a render-only label adds useful orientation without becoming a competing layout authority. The existing responsive planner deliberately preserves scarce Compact capacity through Dock overflow; forcing all four hierarchy items into Now would change the frozen W3 contract rather than satisfy W4 presentation acceptance.
- **Rejected:** Adding Day context to the registry or storage; changing schema v11, profile thresholds, capacities, footprints, placement defaults, or allocation; hiding the visible expanded date from assistive technology; forcing all four Now hierarchy items into the Compact Now cell; beginning Briefing, Work Pulse, Signal Dock, launcher, or connector-content redesign inside W4-P1.
- **Consequence:** W4-P2 may synthesize Aurora Briefing against stable Day/Now surfaces without new network or storage authority. W4-P3 through W4-P5 retain their written variant, Dock, launcher, and connector-content ownership. The first focused browser assertion that demanded all four items inside Compact Now is recorded as Minor evidence overreach and does not reopen W4-P1.
- **Verification:** Implemented through `7acb851`. Final evidence is 129 files / 2,206 tests, clean TypeScript, clean 183-module production/preview builds, no production preview bridge, focused Compact/Standard/Display Chromium evidence with exact hierarchy, single ownership, no percentage positioning, no clipping, restored teardown, and zero runtime errors, plus 457 PASS / 0 FAIL / 3 SKIP after the single permitted selector-family rerun. Three captures passed original-resolution inspection. One implementation review found one Important accessibility issue; `05669b8` fixed it and the focused rereview returned Ready with no Critical/Important issue open.

## A2-D028 - Synthesize Briefing locally inside the existing Greeting allocation

- **Date:** 2026-08-16
- **Decision:** Build Aurora Briefing at render time only from already-held, current inputs in fixed Calendar, Tasks, then Rain priority. Calendar must be enabled, structurally valid, fresh, and scoped to the exact current calendar configuration and timezone; Weather must match the exact current location request identity and existing 30-minute freshness boundary. Only event summary/time/all-day fields, incomplete-task count, and likely-rain time enter synthesis. Compact, Standard, and Display/Ultrawide show deterministic one-, two-, and three-signal sentences inside the existing Greeting BoardItem.
- **Reason:** A useful glance sentence does not require an LLM, cloud request, refresh owner, or persisted derived state. Exact input identity/freshness prevents stale or cross-configuration summaries, while the existing Greeting allocation preserves the frozen Stage model and semantic Now hierarchy.
- **Rejected:** Adding a Briefing registry identity, planner capacity, schema/storage field, migration, analytics/log output, permission, provider URL, credential/capability value, LLM/cloud dependency, or connector refresh; changing profile thresholds or footprints; moving W4-P3 Work Pulse, W4-P4 Dock, or W4-P5 launcher/content work into this packet.
- **Consequence:** W4-P3 may design attention-first Work Pulse connector variants against a stable Now hierarchy. Briefing never owns asynchronous work and cannot transmit, persist, or expose secret/capability-bearing values. Compact typography remains allocation-contained after the Greeting wrapper change without altering Stage capacity.
- **Verification:** Implemented through `e656263`. Final evidence is 131 files / 2,218 tests, clean TypeScript, clean 185-module production/preview builds, no production preview bridge, and a focused built-extension PASS with exact responsive sentences, restored storage, no external Briefing requests, and no runtime errors. Required Compact/Standard/Display captures and the corrected 600x800/320x800 Compact family were inspected. The full harness progressed from 455 PASS / 2 FAIL / 3 SKIP to 456 PASS / 1 FAIL / 3 SKIP on its single permitted rerun; the sole residual was the same Compact Greeting paint family and passed the bounded final focused replay. No third full run was performed, and no Critical/Important issue remains open.

## A2-D029 - Derive Work Pulse attention locally and reveal detail through existing variants

- **Date:** 2026-08-16
- **Decision:** Give GitHub, GitLab, Jira, Vercel, and Status one shared render-only attention summary derived solely from data each widget already holds. Compact preserves connector identity and one primary value, Standard adds prioritized rows, and Expanded retains configured detail. Healthy Status stays quiet; confirmed trouble, unknown service state, failed deployments, and pending work gain explicit text and tone without color-only meaning.
- **Reason:** A consistent scan hierarchy makes the existing Pulse region useful without creating another request, refresh owner, persisted aggregate, layout authority, or secret-bearing data flow. Existing Adaptive Stage variants already provide the correct responsive authority.
- **Rejected:** New Work Pulse storage/schema fields, migrations, registry identities, capacities, footprints, percentage geometry, Dock behavior, connector requests, provider changes, analytics/logging, token or capability text, and W4-P4/W4-P5 functionality.
- **Consequence:** W4-P4 may now make constrained-profile Dock entries meaningful against stable connector summaries. Two historical Status pixel-sampling probes remain Minor cosmetic ledger items; accessible text and non-color-only meaning are authoritative and green. Chrome Web Store state remains untouched pending explicit W6-P5 approval.
- **Verification:** Implemented through `1ab0fa6`. Focused component/source and built-extension gates pass across Compact/Standard/Display, including healthy/attention Status and final Vercel Compact containment, with safe links, restored storage, zero presentation requests, and zero runtime errors. Production/preview builds transformed 186 modules and the bridge scan was clean. The one full unit run passed 2,225 / 2,226 tests; its unrelated Settings timing failure passed the exact rerun. The full browser harness and its one permitted rerun were exhausted; the remaining genuine Vercel family passed a bounded focused fix and the two cosmetic Status pixel probes were ledgered. No Critical/Important issue remains open.

## A2-D030 - Preserve one connector renderer while Signal Dock owns condensed operation

- **Date:** 2026-08-16
- **Decision:** When the frozen planner allocates an enabled connector to Signal Dock, wrap its existing renderer exactly once in a shared Dock-only disclosure. App owns one open connector ID; allocation changes clear stale open state. Closed entries retain registry identity, truthful `Enabled` fallback when no held value exists, one already-rendered current primary value when available, an inert detail subtree, and a 36 CSS px disclosure. Opening reveals that same renderer in one bounded opaque active-work surface; Escape closes and restores the invoking disclosure. Connector Dock sizing uses one finite 14rem floor and no legacy full-card block calibration.
- **Reason:** Constrained and dense profiles must not make enabled connectors disappear or present unreadable preserved desktop cards. Reusing the mounted renderer preserves its request, cache, state, action, link, secret, and permission authorities while a shared condensed shell supplies consistent identity and keyboard operation.
- **Rejected:** Cloning renderers for preview/detail; creating synthetic summaries, provider links, fetches, storage, schema, registry IDs, capacities, or migrations; changing connector snapshot/permission ownership; root scaling; hiding whole connector allocations; or widening W4-P4 into the W4-P5 launcher/content variants.
- **Consequence:** W4-P5 can consolidate bookmarks/quick links and expand RSS, Home Assistant, Calendar, and Weather content against a stable operable Dock. Historical Status pixel samples and a stale W3-P2 aggregate-report predicate remain Minor evidence debt only; semantic ownership, paint containment, collision safety, runtime errors, cleanup, and W4-P4 acceptance are green. Chrome Web Store state remains untouched pending explicit W6-P5 approval.
- **Verification:** Implemented through `049be58` after `4481358`, `c90fd94`, and `f441149`. Final evidence is 135 files / 2,235 tests, clean TypeScript, clean 187-module production/preview builds, no production preview bridge, and a focused built-extension PASS proving all nine connectors exactly once across Compact/Standard/Display with meaningful closed values, 36px operation, safe existing links, open detail, Escape/focus restoration, keyboard reveal, zero presentation requests/runtime errors, and exact cleanup. Three required captures were inspected. The full browser run plus its one permitted rerun cleared the genuine Vercel containment family; one review/fix/rereview ended Ready with no Critical/Important issue open.

## A2-D031 - Compose launchers render-only and spend existing variant capacity on held content

- **Date:** 2026-08-16
- **Decision:** When the frozen planner places the single Bookmarks and Quick Links allocations in edge-adjacent finite rectangles within the same non-Dock zone, render their existing BoardItems once inside one accessible, content-sized launcher shelf and rebase only their local grid coordinates. Pass each allocation's existing variant through the renderer boundary. Calendar exposes the next event then bounded agenda rows, RSS exposes 2/6/up-to-8 held headlines, Home Assistant exposes bounded state/action sets with one action-only Compact fallback, and Expanded Weather opens its existing held forecast by default while variant transitions reset to the new tier's default.
- **Reason:** The launcher identities already describe one navigation purpose, and the planner already owns enough responsive information to select useful content depth. Render-only composition and bounded slicing improve information hierarchy without creating another registry, layout, fetch, persistence, permission, or privacy authority.
- **Rejected:** Merging or renaming registry IDs; changing profile thresholds, capacities, footprints, schema v11, migrations, backups, connector snapshots, refresh owners, provider requests, permission flows, secret/capability handling, or Store state; grouping separated, overlapping, or Dock launchers; mounting hidden extra rows/actions; or widening W4-P5 into Utility Tray behavior.
- **Consequence:** W5-P1 starts with stable Day/Now/Pulse/Dock content and can build only the responsive Utility Tray shell. Historical full-harness assumptions that every BoardItem is a direct zone child or that detail is invariant across Compact/Standard/Expanded are Minor evidence debt and do not reopen W4-P5. Chrome Web Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Implemented through `6656182` after `e1167ca`, `58d3d1a`, `a1ee6d7`, `4b8c714`, and `250c1c3`. Final evidence is 136 files / 2,244 tests, clean TypeScript, clean 188-module production/preview builds, no production preview bridge, and a focused Compact/Standard/Display built-extension PASS with exact IDs/budgets, safe links, keyboard restoration, exact cleanup, zero external requests/runtime errors, and three inspected captures. One review/fix/rereview ended Ready with no Critical/Important issue open. The full harness and its single permitted rerun exposed only stale ancestry/detail/pixel assertions; their source was corrected without a third full run and is ledgered as Minor evidence debt.

## A2-D032 - Derive one Utility Tray shell from the existing responsive profile

- **Date:** 2026-08-16
- **Decision:** Keep the Utility Tray render-only and outside the frozen widget registry/planner. App owns one persistent invoker and one open flag. Standard, Display, and Ultrawide render an anchored modeless dialog that moves initial focus but never traps it or inerts the dashboard; genuine outside click or Escape closes and restores the invoker. Compact renders the same shell as a contained modal bottom sheet with backdrop, dashboard inertness, focus trap, Escape/backdrop dismissal, and exact restoration. W5-P2 retains all tool-content integration.
- **Reason:** The existing Adaptive Stage profile is the single responsive authority and already distinguishes constrained geometry. Sharing one shell avoids user-agent branches and competing tool surfaces while preserving desktop operability and narrow-screen accessibility.
- **Rejected:** User-agent sniffing; a modal desktop drawer; a modeless narrow overlay over reachable background controls; adding a registry identity, planner allocation, stored open state, schema/migration, permission, provider request, analytics, or Store action; moving Tasks, Notes, Timer, Home Assistant actions, or refresh behavior into W5-P1.
- **Consequence:** W5-P2 can compose the accepted working tools inside one stable responsive shell without changing its modality contract. The stale W2-P2 Compact Calendar harness assertion corrected after the permitted full rerun is Minor evidence debt and does not reopen W5-P1. Chrome Web Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Implemented through `e7b3c5f` after `e762699` and `8b36bac`. Focused component/App tests passed 30 assertions; final unit evidence is 137 files / 2,250 tests, clean TypeScript, clean 189-module production/preview builds, and no production preview bridge. The focused built-extension replay passed Standard modeless and Compact modal semantics, focus behavior, dismissal/restoration, containment, cleanup, and zero runtime errors; two captures were inspected. One implementation review returned Ready with no Critical/Important issue open.

## A2-D033 - Render working-tool details through their existing mounted owners

- **Date:** 2026-08-16
- **Decision:** App controls one selected Utility Tray tool and one dynamic invoking control. Tasks and Notes embed their existing panels without nested dialog/focus ownership; Timer portals presentation from its still-mounted reducer; Home Assistant portals configured actions from its current snapshot/config owner; Background portals the existing `nextPhoto` operation. Notes registers one awaited guard that gates tool switches, close, Settings, Arrange, and external disable before its surface may disappear.
- **Reason:** Moving presentation instead of duplicating owners preserves timer continuity, revision-owned Notes writes, task storage authority, connector generation/scope, and photo rotation while satisfying the one-expanded-tool contract.
- **Rejected:** Duplicating Timer or Home Assistant hooks in the Tray; new persisted open/tool state; new refresh requests; bypassing Notes flush errors; keeping independent floating details beside the Tray; schema, migration, registry, planner, permission, privacy, or Store changes.
- **Consequence:** W5-P3 can replace the Settings information architecture against a stable working-tool surface. Historical standalone Notes/Tasks/Timer harness selector and evidence names are Minor debt only; Chrome Web Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Implemented through `44e05c0`. Focused component/browser evidence passed, including one selected detail, Tasks and Notes writes, timer survival, refresh rotation, exact restoration, responsive modality, and no runtime errors; two captures were inspected. Final unit evidence is 137 files / 2,254 tests with clean TypeScript, 189-module production/preview builds, and a clean production bridge scan. One review/fix/rereview ended Ready after closing the Important Notes guard edge. The full harness and one rerun were exhausted by the retired standalone-tool selector family; the final implicit-region selector was source-corrected without a third run and remains Minor evidence debt, not a product defect.

## A2-D034 - Derive Settings layout and connector state without new authorities

- **Date:** 2026-08-16
- **Decision:** Keep the existing Drawer as the sole modal/scroll owner, but render it full-screen below 900 CSS px and as a bounded wide workspace above that threshold. Tabs owns the matching horizontal/vertical ARIA orientation and keyboard axes while SettingsPanel continues mounting only the active section. Connector cards derive `Off`, `Setup needed`, `Reconnect needed`, `Connected`, or `Ready` solely from the frozen descriptor/config contract. TokenConnectForm reveals blank fields only after explicit setup/edit or immediately for stripped-secret reconnect, and returns keyboard focus to the invoking disclosure after Cancel.
- **Reason:** The written W5-P3 acceptance needs a clearer responsive information architecture and state-first connection management without adding another persistence, health-fetch, snapshot, secret, permission, or layout authority.
- **Rejected:** New schema/migrations; persisted tab/disclosure state; snapshot-derived connection claims; prefilling stored tokens, emails, sites, or instance URLs; duplicate connector fetches; registry/profile/planner changes; extra browser matrices; and any Chrome Web Store action.
- **Consequence:** W5-P4 can converge surfaces, typography, targets, and restrained motion against one stable Settings workspace. Historical harness assumptions that an inert Drawer is absent, first-time Home Assistant fields are already open, or Timer has a literal `role="region"` attribute were corrected in the one allowed failing-family rerun and are not product defects. Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Implemented in `49d97f4`. Focused evidence passed 4 files / 278 tests, TypeScript, and one built-extension replay across 1600x900 and 800x600 with state/disclosure/privacy/focus/overflow checks, two inspected captures, and zero runtime errors. Final evidence passed 137 files / 2,261 tests, clean 189-module production/preview builds, clean production bridge scan, and the corrected full browser rerun. One review/fix/rereview ended Ready with no Critical/Important issue open.

## A2-D035 - Localize photo contrast and derive shared visual roles without new state

- **Date:** 2026-08-16
- **Decision:** Name the existing Day, Now, Pulse, Dock, Settings, and Utility Tray surfaces through semantic visual tokens while preserving their geometry and ownership. Give only the empty Focus prompt a fixed opaque near-black label surface with fixed light ink. Size all persistent Stage actions from the resolved 36/44px control token, normalize shared Settings controls to the 36px routine floor, and disable the three observed residual transitions under reduced motion.
- **Reason:** Photo-independent Focus readability and consistent visual hierarchy require local surface authority, not a globally flattened muted-text token or new persisted theme state. The existing Adaptive Stage already owns density-dependent persistent-control sizing.
- **Rejected:** Global `--canvas-fg-muted` replacement; photo sampling; dynamic contrast storage; schema, migration, registry, planner, connector, permission, privacy, backup, or Store changes; new geometry tiers; exhaustive photo/browser cross-products.
- **Consequence:** W6 starts from one converged visual system with unchanged stored-data and privacy contracts. Bright/dark/detailed Focus evidence, routine targets, type floors, and reduced motion are automated; real screen-reader behavior, native zoom/mixed-DPI, and Store actions retain their explicit Wave 6/manual boundaries. Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Implemented through `a83f6ad` after `6aa0637` and `2c6b35f`. Focused evidence passed 6 files / 280 tests and one packaged Chromium replay with three inspected captures, exact 18.04:1 Focus contrast, 36/44px targets, 14px/12px type floors, reduced motion, and zero runtime errors. Final evidence passed 137 files / 2,262 tests, clean 189-module production/preview builds, clean production bridge scan, and 457 PASS / 0 FAIL / 3 SKIP after the single permitted actual-family correction/rerun. The implementation review returned Ready with no Critical/Important issue.

## A2-D036 - Reuse the unchanged product full gate for focused recovery QA

- **Date:** 2026-08-16
- **Decision:** W6-P1 executes four focused automated recovery families over migration/stored data, permissions/secrets, offline/stale/error persistence, and civil-time/open-tab behavior. Because no product or test code changed after the immediately preceding W5-P4 full gate, that exact full unit/build/bridge/browser result remains the W6-P1 regression baseline and is not repeated for report-only changes.
- **Reason:** The written packet requires a complete automated/recovery matrix and tracked findings, not redundant execution of an unchanged full gate. Focused causal suites give denser recovery evidence while protecting delivery time.
- **Rejected:** New recovery infrastructure; schema/registry changes; expanded browser/zoom matrices retained by W6-P2; policy/package/Store work; rerunning full gates because ledgers or reports changed; reopening verified packets for Minor evidence debt.
- **Consequence:** W6-P2 begins with every automated recovery family green and every external/manual ceiling explicit. No product code changed and no new finding is untracked. Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** The four first-run families passed 317, 219, 190, and 94 tests respectively. The bounded review returned Ready with 0 Critical, 0 Important, and 0 Minor new product findings. `W6-P1-QA-MATRIX.md` records commands, acceptance mapping, full-gate reuse, findings, and later-packet exclusions.

## A2-D037 - Use bounded viewport witnesses and stop after one failing-family rerun

- **Date:** 2026-08-16
- **Decision:** Cover each named W6-P2 viewport, state, and CSS-space zoom equivalent once, inspect captures in three bounded batches, and distinguish real touch input from Chromium capability-media emulation. Fix only demonstrated Critical/Important product failures. After the one final full-harness rerun, source-correct remaining Minor legacy evidence debt without a third run.
- **Reason:** The written acceptance requires complete named coverage and honest hardware ceilings, not a Cartesian browser matrix or repeated gates. The 200/400% compact Weather overlap was a real core reflow failure; the legacy W2-P3 backdrop point-discovery failure was not.
- **Rejected:** Native-zoom or mixed-DPI claims from CSS viewport emulation; repeated full harnesses for report/source corrections; new layout tiers, schema, registry, storage, permission, privacy, connector, or Store authority; reopening the packet for cosmetic partial Dock items or stale evidence naming.
- **Consequence:** Compact Weather preserves its full accessible name while using a short visible label and finite safe allocation. Native Chrome zoom, Windows mixed-DPI, real screen-reader, physical hardware, native permissions, and live-service behavior remain explicit manual ceilings. The direct Compact backdrop source correction will be exercised by the next scheduled full harness, not an immediate third run. Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Focused product tests passed 78 assertions and the final source contract passed 21/21. The packaged replay passed named viewport/zoom, overflow, targets, keyboard/restoration, reduced motion, AX naming, touch taps, cleanup, and runtime-error checks. Final unit evidence passed 137 files / 2,264 tests; production/preview builds transformed 189 modules and the production bridge scan was clean. `W6-P2-QA-MATRIX.md` records the full matrix, one Important fix, the 455 PASS / 2 FAIL / 3 SKIP legacy rerun evidence, and all manual ceilings.

## A2-D038 - Compose the Adaptive Stage as a content-sized Calm Canvas

- **Date:** 2026-08-16
- **Decision:** Keep the frozen planner as the sole identity, order, zone, span, variant, priority, lock, and Dock authority, while allowing non-Dock `BoardItem` presentation to flow through content-sized Day, Now, and Work Pulse grids. Center Now in the viewport, shrink-wrap the edge clusters and Signal Dock, and keep Compact overflow in the Stage's one semantic vertical scrollport. Portal Bookmark menus to the viewport while the Dock retains its own finite horizontal scrollport.
- **Reason:** The previous planner-coordinate presentation stretched sparse zones into page-height slabs and scattered related content across the photo. A render-only flow seam preserves every data and planning contract while restoring the approved V1-first calm hierarchy.
- **Rejected:** Registry/schema/migration changes; new profile or density tiers; planner rewrites; absolute-positioned widget collages; viewport-wide Dock or Day/Pulse panels; hiding dense identities; permission, privacy, connector, backup, Store, or protected-original changes; repeated browser cross-products during incremental fixes.
- **Consequence:** The owner can judge one coherent visual direction from 17 bounded composition witnesses and 24 interaction captures. The final full suite/build/browser gate remains intentionally deferred until that manual visual decision. Minor emergency-height initial-viewport and partial-Dock continuation observations remain ledgered without reopening the remediation. Store actions remain blocked until explicit W6-P5 approval.
- **Verification:** Focused implementation passed 4 files / 83 tests; the single review's active-bookmark-toggle finding was reproduced RED and the fix passed 31/31 bookmark tests plus a packaged-Chromium rereview. Preview TypeScript/build transformed 189 modules. All 26 registry identities, 17 layouts, and 24 interaction/state captures are indexed in the Calm Canvas reports with no Critical or Important issue open.

## A2-D039 - Restore the V1 canvas with adaptive safety rails

- **Date:** 2026-08-16
- **Decision:** Supersede the Calm Canvas and user-facing semantic-zone layout with a V1-first canvas. Restore the top Bookmarks bar, centered Clock and Focus, meaningful edge widgets, movable Timer/Tasks/Notes launchers, and direct free placement. Keep responsive profiles, safe margins, snap guides, keyboard movement, collision feedback, and migration recovery as safety rails. Preserve Aurora 2's security, privacy, storage, connector, accessibility, and recovery foundation.
- **Reason:** Owner visual and interaction review found that the semantic Day/Now/Work Pulse/Signal Dock model, abstract order controls, Dock-only tools, ineffective variants, and content suppression contradicted Aurora's established vision even when packet-local geometry checks passed. Product acceptance must evaluate the whole experience, not only the written mechanics of the rejected candidate.
- **Rejected:** Continuing to polish the current semantic zones; treating `3c5015a` as visually accepted; a full rollback that discards Aurora 2 correctness work; opaque passive-card proliferation; non-movable tools; four-day Month fragments; fixed-only ICS colors; profile or size controls without visible effect; destructive layout migration; any Store action.
- **Consequence:** The current release path pauses for owner review of `2026-08-16-aurora-v1-canvas-adaptive-safety-rails-design.md`. After approval, a new session must create a bounded implementation plan before touching production code. Calm Canvas remains historical evidence only. Store actions remain blocked through W6-P5.
- **Verification:** This is a design decision. No production code or stored data changed. The active and protected repositories were clean and equal to their upstreams before the design checkpoint.

## A2-D040 - Approve the eight-packet V1 Canvas implementation path

- **Date:** 2026-08-16
- **Decision:** Treat the owner's implementation-session prompt as approval of the complete Option 1 specification. Execute the written eight-packet Canvas plan continuously, with one bounded review/fix cycle and one checkpoint/push proof per packet. Stop after Canvas-P4 for the four-capture Canvas/Arrange owner gate and after Canvas-P7 for the three-capture connector/calendar owner gate. Canvas-P8 alone owns the post-approval full unit/build/browser gate.
- **Reason:** The approved visual direction and storage architecture are now complete enough to implement without reopening product discovery. Bounded packets protect frozen correctness work while the two original-resolution owner gates prevent another mechanically green but visually rejected release path.
- **Rejected:** Resuming Calm Canvas; silently continuing into deep widget work before the first owner gate; repeating full gates during incremental packets; weakening V1/V2 recovery; combining Store work with Canvas recovery; or treating test counts/contact sheets as visual approval.
- **Consequence:** Canvas-P1 is active. W6-P3 waits behind Canvas-P8, and W6-P5 remains the hard external-action approval gate. The protected original stays read-only.
- **Verification:** At planning start, the active branch and upstream were equal and clean at `03bca3b920eba6eaf54f8266795a0e7124cee284`; the protected original and upstream were equal and clean at `eb1354b6a5b041fb6d494655c3dae1862572bc51`. The executable plan is `docs/superpowers/plans/2026-08-16-aurora-v1-canvas-adaptive-safety-rails.md`.

## A2-D041 - Adopt Canvas V3 only on explicit Save with exact recovery

- **Date:** 2026-08-16
- **Decision:** Advance storage metadata to schema v12 without rewriting the stored layout at boot. Accept V1, V2, or V3 through one in-memory adapter. Create V3 only through an explicit valid Canvas Save, preserve the exact prior valid V1 or V2 object as recovery, and restore that value exactly. Drop an invalid active placement per block at runtime while keeping strict backup validation. Legacy semantic Arrange fails closed on V3, and the legacy global Settings reset is unavailable for V3.
- **Reason:** Canvas needs independent profiles, direct coordinates, layers, and recovery without converting user data before the owner chooses Save. Corruption in one active placement must not disable valid siblings or exact recovery, while backups and new drafts still require complete validation before persistence.
- **Rejected:** Eager V1/V2 conversion; schema-version writes that also normalize layout; destructive V3-to-V2 Arrange saves; a Settings action that clears every V3 profile and recovery; partial invalid draft writes; accepting invalid backup geometry; or exposing raw persistence errors.
- **Consequence:** Packet 2 can render one normalized V3 view over every stored generation. Packet 3 owns the first real V3 Save surface and profile-scoped reset. Until then, legacy V1/V2 arrangement remains available, while V3 cannot be downgraded by the compatibility editor. The protected original and Store boundaries are unchanged.
- **Verification:** Implemented through `f71148d` after `412a420` and `b074187`. The final focused gate passed 9 files / 609 tests, clean TypeScript, and `git diff --check`. One implementation review and one fix/rereview cycle closed destructive downgrade, global V3 reset, and exact recovery defects. The rereview found one additional Important corrupt-placement case; it was fixed with a focused red/green test under the exhausted cycle and no second rereview was requested. Repeated Settings test `act(...)` warnings are ledgered as Minor test-harness noise.

## A2-D042 - Render every stored layout generation through one safe Canvas

- **Date:** 2026-08-16
- **Decision:** Adapt V1, V2, and V3 layouts in memory into one Canvas surface. Keep source-owned V1 anchors and independent Small, Desktop, Large, and Wide profiles, fit display geometry to 8px safe margins without mutating stored coordinates, and paint a Bottom bar only for explicit placements. Reuse the preserved compact presentation contract until Packet 5 owns distinct content contracts. In a custom profile, reserve all saved blocks exactly and place only a newly enabled identity at the nearest safe snapped position. `Use Desktop layout everywhere` remains a preview-only Desktop source until explicit Save.
- **Reason:** The approved recovery needs V1 composition and direct coordinates without losing additive storage safety, duplicating identities, or letting narrow profile content escape its owned geometry. Saved custom work has priority over source defaults, and responsive preview must remain observable before persistence.
- **Rejected:** Semantic Day/Now/Work Pulse/Signal Dock presentation regions; eager storage conversion; moving existing custom blocks to accommodate a new identity; always-painted Bottom bars; root transforms; hidden overflow as a geometry fix; or writing while previewing Desktop across profiles.
- **Consequence:** Canvas-P3 can replace the remaining semantic Arrange compatibility editor with one direct-manipulation V3 draft over stable measured Canvas items. Packet 5 still owns truthful distinct connector content, and Packet 6 owns the complete Month sizes. The protected original and Store boundaries remain unchanged.
- **Verification:** Implemented in `dffa11d` with the review fix in `68c8cd6`. The final focused gate passed 8 files / 71 tests, clean TypeScript, and `git diff --check`. One implementation review and one fix/rereview closed compact presentation, Desktop-everywhere preview, and custom new-identity placement findings. The rereview returned Ready with no Critical, Important, or Minor finding open.

## A2-D043 - Keep Arrange edits local until one explicit commit

- **Date:** 2026-08-16
- **Decision:** Replace the semantic compatibility editor with one direct Canvas draft. Measure the live Canvas as the coordinate authority for pointer, neighbor, guide, keyboard, resize, overlap, and layer operations. Keep visibility and all profile drafts in memory until one explicit Save, preserve the exact invoking control and stored snapshot for terminal focus and Cancel, and keep a durable live region outside the removable overlay.
- **Reason:** Direct manipulation is truthful only when every input method edits the same local geometry, respects the same 8px safety rail, and cannot partially persist layout or visibility. Long-press and Settings entry need distinct focus owners, while Save and Cancel announcements must survive overlay teardown.
- **Rejected:** Viewport-origin drag math; render-only clamping that persists unsafe coordinates; immediate writes; semantic zone/order controls; inert visibility furniture; losing Desktop-everywhere state across tabs; returning every session to Settings; or mounting the only live region inside the editor being removed.
- **Consequence:** Canvas-P4 can restore direct V1 launchers and centered ritual content over one stable editor. Packet 5 still owns materially distinct widget content, Packet 6 owns calendar completion, and Store actions remain blocked through W6-P5.
- **Verification:** Implemented in `5aa01e5`, with review fixes in `96482b8` and `8996b15`. The final focused gate passed 6 files / 79 tests, clean TypeScript, and `git diff --check`. The single review/fix/rereview cycle closed Canvas-origin drag, bounded keyboard/size, cross-tab preview state, invoker focus, and terminal announcement findings. The rereview's residual Restore default size path was reproduced and fixed under the exhausted cycle; no second rereview was requested and no Minor issue was reported.

## A2-D044 - Restore direct V1 rituals without duplicating their command surface

- **Date:** 2026-08-16
- **Decision:** Keep Bookmarks at the top, Clock and one stable Focus footprint centered, and Timer/Tasks/Notes as movable Canvas launchers that open their established direct panels. Treat Briefing as explicitly enabled and signal-dependent. Retain the optional Utility Tray only for working Home Assistant and Refresh commands rather than duplicating the direct everyday tools.
- **Reason:** The first owner gate must show the approved photo-first V1 hierarchy and direct interactions without empty prompt or panel surfaces. A visible command surface may remain, but every advertised command must paint working content and cannot replace the movable launchers.
- **Rejected:** Opaque empty Focus prompts; empty `Nothing urgent.` briefing; Dock-only everyday tools; duplicate Timer/Tasks/Notes tray tabs with no host content; new persistence owners; eager settings writes; or connector, permission, privacy, and Store changes.
- **Consequence:** Canvas-P4 is verified and Canvas-P5 remains blocked on explicit owner approval of the four early-gate captures. The later connector/calendar and final gates remain unchanged, and Store actions remain blocked through W6-P5.
- **Verification:** Implemented in `6403b26`, with the review fix in `045ed09`. The exact focused gate passed 7 files / 332 tests, clean TypeScript, and `git diff --check`. The focused Chromium probe passed direct panels, Timer continuation, transparent Focus, pointer and keyboard Arrange interactions, guides, inspector, exact Cancel, Small sheet containment, overflow, and error checks. All four required captures were regenerated and inspected at original resolution. The rereview returned Ready with no Critical or Important finding open. The exercised Clock overlap warning and existing Settings test `act(...)` hydration warnings remain Minor.

## A2-D045 - Make Canvas size a truthful presentation contract

- **Date:** 2026-08-17
- **Decision:** Give each registry identity an ordered set of materially distinct Canvas sizes and an explicit content promise. Derive inspector conflicts from the connector's existing selected sections, pass Canvas size into each connector renderer, and keep connector configuration as the sole authority for what the user selected. Compact preserves identity and a truthful primary value, Standard adds selected rows or a visual, and Full exposes the complete selected composition that fits. Include the owner-approved compact Weather hierarchy, named-folder monograms, and Small Search clearance in the same presentation packet.
- **Reason:** A size control is meaningful only when it changes useful content without contradicting held data or silently discarding selected sections. Large and Wide layouts must reveal detail, while compact summaries must never claim healthy or empty state merely because rows are not painted.
- **Rejected:** CSS-only hiding that violates a size promise; generic conflict counts detached from actual connector views; size changes that write connector settings; false empty/healthy summaries derived from presentation slices; identical size choices; calendar work before Packet 6; or any frozen authority change.
- **Consequence:** Packet 6 can narrow Month to its two truthful sizes and add identity-owned ICS colors on top of one general size-contract authority. Packet 7 owns the real-browser Small clearance witness, dense connector compositions, and the deferred cosmetic Canvas/viewport folder-mark duplication. Store actions remain blocked through W6-P5.
- **Verification:** Implemented in `10ff533`, `484cd63`, and `121da8d`. The final focused gate passed 15 files / 317 tests, clean TypeScript, and `git diff --check`. One implementation review plus one fix/rereview found five Important issues; three were closed in the reviewed fix and the two residual Jira/RSS guidance and branch-test gaps were closed under the exhausted cycle with focused evidence and no second rereview. No frozen schema, storage, connector, credential, permission, snapshot, backup, Notes, Tasks, privacy, or Store authority changed.

## A2-D046 - Keep calendar display choices outside fetched-event identity

- **Date:** 2026-08-17
- **Decision:** Give Month two truthful Canvas sizes: a complete seven-day Compact week and a complete natural Standard month. Give each ICS feed an optional closed color choice, with absent color reading as the existing position-based Auto sequence. Treat ICS color as presentation metadata outside fetched-event identity, and use the same canonical event identity for hook lifecycle, queued authorization, and persisted snapshot scope. Preserve omitted optional ICS fields on color-only saves.
- **Reason:** Calendar color must follow feed identity without triggering network work, invalidating fresh events, or stranding an in-flight refresh. Backward-compatible configs must not change shape merely because a user chooses a color. Compact Month must never return to the rejected four-day CSS fragment, while its named navigation remains functional and coherent.
- **Rejected:** A redundant Full Month; CSS-hidden day fragments; open-ended or position-only explicit colors; eager Auto writes; color-only permission or snapshot clearing; hashing color into event identity; using different lifecycle and persisted identities; materializing view defaults during a color edit; or changing feed, capability URL, backup, permission, selected-view, schema, migration, privacy, or Store authority.
- **Consequence:** Packet 7 can exercise complete Month variants, Auto and explicit calendar colors, rich connectors, and responsive profile compositions in real Chromium. Existing calendars remain readable without writes, explicit colors follow their feed object, and color-only edits reuse fresh or in-flight event data. Store actions remain blocked through W6-P5.
- **Verification:** Implemented in `45bcf45`, `8ce4fb8`, and `f6c35af`. The final packet gate passed 7 files / 445 tests, the focused snapshot gate passed 3 files / 76 tests, TypeScript, and `git diff --check`. One review/fix/rereview cycle accepted functional complete-week navigation and exposed a legacy omitted-default branch; exact RED/GREEN residual closure proved that only color and no cached snapshot or optional field changes. Existing Settings test `act(...)` warnings remain Minor.

## A2-D047 - Freeze the integrated Canvas direction at the second owner gate

- **Date:** 2026-08-17
- **Decision:** Treat the three regenerated Packet 7 captures and their direct Chromium evidence as the stabilized connector/calendar candidate. Keep Desktop focused on the V1 hierarchy with top Bookmarks and rich GitHub/Jira content, while independent Large and Wide profiles expose every shipped connector family, complete Month content, and visible multi-calendar source names. Stop before Packet 8 until the owner explicitly approves this direction.
- **Reason:** The second visual gate now proves the approved photo-first hierarchy and the restored connector/calendar depth together, rather than inferring presentation quality from component tests. Direct evidence also proves that the Canvas remains operable through long press, pointer capture, keyboard movement, every visible identity, local scrolling, responsive containment, Focus lifecycle, and cache-neutral calendar color edits.
- **Rejected:** Treating colored dots as the only visible calendar-source identifier; accepting a report without direct interaction evidence; substituting a contact sheet or test counts for original-resolution inspection; continuing into final gates before owner approval; broadening Packet 7 into exhaustive browser coverage; or touching Store state.
- **Consequence:** Canvas-P7 is verified and awaits owner approval. Canvas-P8 remains blocked and will own the one full unit/build/bridge/browser gate after approval. The compact folder glyph/monogram duplication remains Minor. Native zoom/DPI, physical touch, real screen-reader speech, live connectors/permissions, sleep/wake, and unload timing remain manual ceilings. W6-P5 remains the hard Store approval boundary.
- **Verification:** Implemented in `cb58bd2` with review fix `73b2794`. Calendar focused tests passed 44/44; the focused real-Chromium gate, TypeScript, and diff hygiene passed. The 1600x900, 2560x1440, and 3440x1440 captures were each inspected at original resolution. Evidence records all nine connector families, complete Month, named ICS sources, all visible Arrange identities, real long press and pointer capture, Focus restoration, exact color-cache preservation, and zero intersections, clipped blocks, missing images, runtime errors, or failed requests. One implementation review found two Important gaps; the one fix/rereview cycle returned Ready with neither open.
