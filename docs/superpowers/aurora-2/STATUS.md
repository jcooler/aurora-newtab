# Aurora 2 Status

**Updated:** 2026-09-02<br>
**Branch:** `feat/aurora-2-observatory`<br>
**Worktree:** `D:\DEV\Chrome plugin-aurora-2`<br>
**Current wave:** Tab Two V2 product and premium architecture<br>
**Last verified packet:** `PM-P3 hosted sandbox billing return and Checkout recovery` - Stripe test catalog/webhook, Supabase billing functions and migration, static Cloudflare return surface, production-only permission-free extension handoff, same-plan Checkout recovery, and paid-status convergence<br>
**Current packet:** PM-P3's approved hosted sandbox gates are active. The owner completed the signed-in sandbox Checkout lifecycle, and read-only hosted evidence confirmed one active Annual subscription. A complimentary-owner presentation collision found during that witness is repaired in `account-snapshot` version 9 and the extension client; the corrected installed-build display remains the sole manual check. Merge, release, live billing, Supabase Pro, PM-P4, and Store gates remain closed.

- **PM-P2 hosted lease hydration repair (2026-09-01, VERIFIED TO MANUAL ENTITLEMENT DISPLAY):** The owner's real sign-in advanced hosted `auth.users.last_sign_in_at`, and both `account-snapshot` and `entitlement-lease` recorded successful invocations with zero 4xx/5xx responses, proving the Google callback, PKCE exchange, session creation, and hosted account/lease responses. The client still returned Local because `hydrate()` captured time before the network calls and passed that stale instant into future-issued lease validation; the freshly signed server lease was therefore a few milliseconds newer and failed closed. An observed RED reproduces a server clock advancing during the lease request. The one-line fix verifies against a fresh post-response clock. The focused gate passed 43 tests and TypeScript; the stabilized gate at `a6be8691717a657ac6486711282940671673e0af` passed 233 files / 3,649 tests, 21/21 account contracts, TypeScript, diff hygiene, and exact 326-module production, 279-module preview, and 326-module account-local builds. The owner then reloaded the corrected build and confirmed `Complimentary subscription` in Account & Sync. This witnesses account and entitlement hydration, not encrypted sync transport; sign-out remains a manual ceiling. No hosted configuration, secret, database, permission, storage schema, sync, billing, merge, release, or Store state changed.

- **PM-P2 hosted production activation (2026-09-01, ACTIVATED WITH MANUAL CLIENT CEILING):** Activated the approved account-only production boundary on Supabase Free project `ovlobmvxtryitupxwylg` in US East with no payment method, paid add-on, or Pro spend. Deployed the two reviewed migrations and the JWT-protected `account-snapshot` and `entitlement-lease` functions; configured Google-only OAuth with minimal `openid`, email, and profile scopes; pinned production to extension id `akjalbmacojpmebkgohhcaaiacicpgkh`, one exact Supabase origin, and signing key id `production-2026-09-01`; and kept all secret values outside git and the extension bundle. The owner completed Google authentication through the exact unpacked production build in normal stable Chrome. An independent hosted query found one provider-neutral Google account mapping, and the action-time-approved mutation created exactly one active `complimentary_owner` grant with the six approved capabilities and one audit event; replay produced no duplicate audit event. Anonymous method/resource boundary probes returned the expected 401/404/405 responses. Current stable Chrome no longer accepts command-line unpacked-extension sideloading, while Google rejected the automation-only Chromium sign-in, so the OAuth interaction is a manual verification ceiling rather than an automated screenshot/request/storage ledger. The hosted authority, hydration repair, and database evidence are retained in `docs/superpowers/reports/TAB-TWO-PRODUCTION-ACCOUNT-ACTIVATION-QA.md`; final in-panel owner-entitlement display and sign-out cleanup remain manual client checks unless separately witnessed. Supabase Pro, Stripe, sync, merge, release, and every Chrome Web Store action remain closed.

- **PM-P2 Supabase auth and entitlements local foundation (2026-09-01, VERIFIED LOCAL-ONLY):** Added pinned local Supabase tooling; provider-neutral account and Google identity tables; default-deny RLS and service-only RPC boundaries; audited complimentary-owner grant mutation by exact account UUID; Google-only PKCE with exact callback correlation and replay rejection; a typed session key isolated from AuroraData, backup, diagnostics, logs, screenshots, and UI; Ed25519-signed, canonical, account-bound entitlement leases; fail-closed refresh and offline behavior; and an `account-local` client/build boundary. `identity` and `http://127.0.0.1/*` exist only in `account-local`; production and preview permissions remain at PM-P1 authority. One bounded review found an Important Edge Function extension-CORS boundary and closed it in the sole focused fix/rereview cycle. The stabilized gate passed 232 files / 3,638 tests, TypeScript, 10/10 Node contracts, a fresh database reset, 50 pgTAP tests, zero-error database lint, 24 Edge Function tests, and diff hygiene. Exact commit `b3555b0f47245a31ea8878c562b708ed8935430c` produced 326-module account-local, 279-module preview, and restored 278-module production builds; the production account/auth/secret scan had no matches. Installed Chromium at 1600x900 and touch-enabled 768x812 passed explicit sign-in, account-bound owner-fixture lease, callback replay rejection, sign-out cleanup, and zero AuroraData writes with exactly six approved request intents and zero console/page/failed-request errors. Both retained screenshots passed original-resolution inspection. Evidence: `artifacts/qa-account-auth-local/b3555b0f47245a31ea8878c562b708ed8935430c/`; report: `docs/superpowers/reports/TAB-TWO-SUPABASE-AUTH-ENTITLEMENTS-QA.md`. At that local-only checkpoint, no hosted project, production OAuth application, production secret/key, real owner grant, deployment, merge, release, or Store action existed.

- **PM-P1 Account & Sync client shell (2026-09-01, VERIFIED):** Added pure account, entitlement-lease, capability, sync-summary, and device contracts; a deep-frozen zero-network Local production client; a separately imported deterministic preview client; one AccountProvider at the new-tab root; a permanent sixth Account & Sync Settings tab; truthful Local, signed-in, subscription, sync, device-limit, and destructive-confirmation states; and an inline PremiumPrompt primitive that is not consulted by current free paths. Owner approval covered five original-resolution Account & Sync PNG mockups before production React or CSS changed. The bounded review closed account-identity and future-issued lease validation, fresh Google verification and explicit device targeting for revocation, and nested-dialog QA measurement. The stabilized product gate passed 227 files / 3,544 tests plus TypeScript; the final QA contract passed 6/6. Exact production and preview builds transformed 278 and 279 modules, and production contained no preview fixture marker. Installed extension Chromium at 1600x900 and touch-enabled 768x812 passed all required interactions across 11 retained states with zero storage writes, external requests, console errors, page errors, failed requests, horizontal overflow, viewport escape, or control overlap, plus one contained Settings scroll owner. All 11 final screenshots were inspected at original resolution. Runtime evidence source: `448cbd5ffb2647a8bac8d204a97195406a380464`; evidence: `artifacts/qa-account-sync-shell/448cbd5ffb2647a8bac8d204a97195406a380464/`. README and PRIVACY remain accurate without edits because production remains Local, adds no backend or data flow, and requests no permission. No authentication, entitlement service, owner grant, billing, sync transport, storage schema, analytics, provider integration, paid infrastructure, production secret, merge, release, or Store action was added.

- **Free-baseline hardening (2026-09-01, VERIFIED):** Removed the obsolete global `isPremium()` dependency and dead module from the five-tab Settings shell, Layout recovery/management, connector-widget Settings routing, keyboard editing, long-press editing, and the layout badge. No replacement entitlement abstraction was added. Behavior-first RED/GREEN coverage proved the free paths, and no `isPremium` or `lib/premium` reference remains under `src`. Original-resolution review found one Important touch-release defect: a toolbar rendered beneath a held pointer could activate its Layout selector on release. The focused fix keeps the toolbar inert through the release task; regression coverage and the exact touch witness prove no unintended activation. The final stabilized gate passed 222 files / 3,520 tests, TypeScript, the 6-test QA contract, and diff hygiene with only existing line-ending notices. The exact production build transformed 274 modules. Installed Chromium at 1600x900 desktop and 768x812 supported touch-device geometry passed all nine interactions, three intended `layouts` writes, exact protected-authority checks, zero requests or runtime failures, and five original-resolution captures with no overflow, escape, or object overlap. README and PRIVACY remain accurate without edits because permissions, collection, storage authorities, and network behavior did not change. Evidence: `artifacts/qa-free-baseline/<exact-commit>/`.

- **Competitive inspiration (2026-08-31, OWNER-APPROVED DESIGN INPUT):** Later feature and UI packets will study Momentum's calm daily-focus hierarchy, Tabliss's modular customization/privacy posture, and Vivid Tab's approachable drag and drop, bookmarks, bang search, and local-first presentation. Tab Two remains differentiated by connector depth, professional second-screen composition, named layouts, stacks, docks, multi-account premium connectors, aggregate metrics, and encrypted settings/text sync. This direction adds no feature or visual implementation to the free-baseline packet. Research: `02-research/2026-08-31-tab-two-competitive-inspiration.md`.

- **PM-P3 hosted sandbox return, Checkout recovery, and paid-status convergence (2026-09-02, VERIFIED TO CORRECTED INSTALLED-BUILD DISPLAY):** The approved Stripe sandbox catalog, Portal, webhook destination, Supabase billing schema/functions/secrets, and test-only authority are active. A static Cloudflare Pages surface provides polished neutral, success, cancel, and billing return states with no analytics, cookies, storage, remote assets, Function, Worker, paid service, or billing authority. Production alone accepts exact messages from that one origin and focuses Tab Two through `runtime.getContexts`; preview/account-local omit the bridge and no Chrome permission was added. Hosted migration 00400 and Checkout function version 8 safely resume only an exact same-plan open Session. The owner completed the signed-in sandbox lifecycle, and hosted rows confirm one active Annual subscription. The follow-up generic unavailable message was a presentation collision: both the Edge snapshot and client reduced active Stripe billing to `complimentary` whenever the independent owner grant was also present, leaving purchase actions visible until the server's duplicate-subscription guard correctly returned 409. Two observed-RED regressions now preserve every non-`none` paid state while keeping verified complimentary access independent; 3 files / 65 focused tests and 237 files / 3,779 repository tests pass with TypeScript, and only JWT-protected `account-snapshot` version 9 was deployed. The corrected exact account-enabled build still needs one installed-extension reload/refresh witness. Reports: `docs/superpowers/reports/TAB-TWO-BRANDED-BILLING-RETURN-QA.md` and `docs/superpowers/reports/TAB-TWO-RESUMABLE-CHECKOUT-QA.md`.

- **PM-P3 Stripe sandbox implementation (2026-09-01, HOSTED TEST AUTHORITY ACTIVE):** Implemented the approved client/UI, server-owned two-price plus one-use-coupon catalog, short Checkout reservations, default-deny billing schema, atomic introductory redemption and Stripe-grant mutation, exact subscription binding, signature-first idempotent webhooks, deterministic same-second ordering, current-object normalization, lifecycle/courtesy/revocation rules, privacy disclosure, and strict independence of `complimentary_owner`. An owner-approved presentation follow-up gives eligible accounts exactly two choices: quiet Monthly and one highlighted USD 9.99 first-year Annual offer with a `50% off first year` badge and muted USD 19.99/year renewal disclosure; ineligible accounts see the standard annual choice instead. The local implementation gate passed 235 files / 3,724 tests before the owner authorized the listed hosted sandbox gates. No live Stripe mode, live payment, merge, release, or Store state is active. Local report: `docs/superpowers/reports/TAB-TWO-STRIPE-BILLING-LOCAL-QA.md`.

- **Paid MVP program and owner access (2026-09-01, OWNER-APPROVED PLAN):** The consolidated architecture and threat model include the permanent sixth Account & Sync tab, optional explicit Google sign-in, separate sync opt-in, one eligible-data sync switch, five-installation replacement behavior, Stripe-hosted billing, immediate self-service account deletion, server-managed encrypted sync, and server-sequenced conflict handling with pre-overwrite local backups. Preview/test premium must be deterministic and absent from production bundles. The owner's real premium access is now an audited signed server-side complimentary grant bound to the provider-neutral account UUID and independent of Stripe, email comparisons, or mutable extension storage. The paid program remains decomposed into PM-P1 through PM-P9, with each later packet retaining its own approval gates.

- **Tab Two freemium product and architecture (2026-08-31, OWNER-APPROVED DESIGN):** Approved a connector-led premium model that preserves every current local capability and all 15 existing connectors as free. Premium is specified at $1.99 monthly, $19.99 annually, or a one-time $9.99 introductory first year, with Google-only MVP sign-in, Stripe Managed Payments as the preferred payment authority, Supabase Pro as the proposed production baseline, five-device encrypted sync, a 2 MB vault quota, 13 months of aggregate-only metrics, a multi-account framework, and read-only Google Calendar, Microsoft Calendar, and approval-contingent Strava connectors. Spotify is approval-gated and not a launch dependency. Support is self-service first with no SLA. Before launch, one exact-provenance full-product QA gate must exercise every existing and premium widget and state, drag and drop, keyboard and touch movement, named layouts, stacks, docks, persistence, Settings, account, billing, sync, metrics, desktop, short-height, ultrawide, high-density, and touch-narrow geometry, original-resolution screenshots, storage and request allowlists, accessibility, and zero unexpected runtime failures. This approval changes no shipped behavior and authorizes no code, provisioning, backend cost, analytics, permissions, deployment, merge, release, or Chrome Web Store action. Implementation requires a separately approved plan and threat model. Design: `docs/superpowers/specs/2026-08-31-tab-two-freemium-product-architecture-design.md`.

- **Tab Two V2 product identity (2026-08-30, VERIFIED):** Replaced customer-facing Aurora branding with Tab Two across the manifest, new-tab title, Settings, connector explanations, backup and diagnostic copy, README, and privacy policy. Added one deterministic layered-tab `T` vector mark and generated 16px, 32px, 48px, and 128px extension icons from that single source. Settings now presents the approved slogan, “The best tab for your second screen.” Existing connectors are labelled Included today, while the About footer explains that optional premium services are planned but not activated or sold here. Persisted `aurora:*` keys, the backup envelope id, internal TypeScript names, repository paths, and the scientific Aurora & Kp connector remain unchanged compatibility or domain authorities. No onboarding state or UI was added because the owner deferred that work. No account, payment, sync, backend, analytics, permission, entitlement, layout, connector, credential, backup, release, merge, or Store authority changed. The bounded review found one Important documentation gap; its single correction added the approved README slogan and current privacy effective date, and rereview found no Critical or Important issue open. Focused coverage and the stabilized full gate passed 222 files / 3,523 tests, TypeScript, diff hygiene, and an exact 275-module production build. Exact desktop 1600x900 and touch 375x812 extension Chromium verified the manifest identity, all four icon declarations, Settings marks, Included today connector education, slogan and premium-later disclosure, zero unintended storage writes, zero requests, zero runtime failures, and no viewport overflow. All six screenshots were inspected at original resolution; evidence is under `artifacts/qa-tab-two-v2-identity/826e8ac2b7f8197090e498013448babb9aeca63a/`.

- **Tab Two V2 Progress, refresh, and Calendar polish (2026-08-30, VERIFIED):** Consolidated Habit creation, rename, two-step deletion, and completion editing under Progress while retaining `habits` as the only Habit authority. Added per-source safe refresh presets, Manual mode, Refresh now, hidden-tab catch-up, and cross-tab Web Lock ownership for configurable connectors and Weather; On This Day and Public Holidays remain fixed daily sources, and NWS alert checks retain their separate five-minute safety cadence. Calendar now uses short month labels, natural 4/5/6-week grids, gray adjacent-month dates, calendar-colored dots in a measured marker lane, hover/focus/click context, silent empty holiday months, contained holiday footers, top-aligned controls, and outside-first context placement. Canvas text selection is suppressed while editable input selection remains available. The only Important review finding was duplicate Weather environment fetching after a waiting tab observed a current unavailable result; one RED/GREEN correction made that shared result satisfy the waiting owner, and rereview found no remaining Critical or Important issue. Focused and stabilized full tests, TypeScript, diff hygiene, exact production provenance, and targeted real-extension Chromium cover Progress/Habit editing, refresh presets and Manual mode, Weather policy behavior, Calendar row geometry and context, desktop/touch containment, storage-write isolation, and request/runtime ledgers. All final screenshots were inspected at original resolution; Calendar markers remain separated from date numerals. Credentials, storage authorities, calendar feed colors, named layouts, stacks, docks, backups, permissions, privacy boundaries, analytics, backend, entitlement, merge, release, and Chrome Web Store state remain unchanged.

- **Tab Two V2 Progress (2026-08-29, VERIFIED):** Added ordinary local `progressGoals` user content, schema v20 migration and backup validation, a dedicated Settings tab, the existing-Habits bridge, and an off-by-default intrinsic Compact canvas rail that appears only through explicit widget/layout placement. `habits` remains the sole Habit authority; Progress adds no account, sync, provider, permission, Attention, Briefing, notification, analytics, payment, entitlement, or automatic layout authority. The bounded review and rereview ended with no Critical or Important issue open. The stabilized gate is split by exact provenance: full Vitest passed 219 files / 3,501 tests and TypeScript passed at reviewed code `c0974e3`; the expansion contract passed 36 Node tests plus 5 files / 109 Vitest tests at `52912b6`; and the remaining information-first 9/9, widget catalog 29/29, Progress QA contract 10/10, 270-module build, and exact browser gates passed at `e2065b7`. Closeout repaired only stale guard/evidence surfaces: both catalog contract helpers are recognized without weakening literal promises, Calendar Full catalog evidence is restored, the manifest contract expects all 37 identities including Progress, and the information-first contract preserves intrinsic Status while the other eight connectors remain framed. Exact Progress Chromium passed all 21 interactions at 1600x900, 1408x600, 3440x1440, and touch-enabled 375x812 with zero requests, console/page errors, collisions, viewport escapes, or horizontal overflow; evidence is under `artifacts/qa-tab-two-v2-progress/e2065b7fa2850e8924abe3f3b5b0c7a145e53580/`. The widget-production suite used the established exact preview-evidence boundary so Bookmarks was install-time only for automation while deterministic adapters remained evidence-only; it recorded 31 captures, 34 approved targets, 15 layouts-only facing-write entries, two approved Weather-alert requests, and zero runtime, failed, or unexpected requests in `docs/superpowers/catalog/widget-redesign/production/`. All 32 widget-production screenshots and four Progress screenshots were inspected at original detail. Native permission prompts, real assistive technology, manual-device behavior, release packaging, merge, and Chrome Web Store actions remain explicit later boundaries.

- **Tab Two V2 connector experience (2026-08-29, VERIFIED):** Implemented the first bounded packet from the approved Tab Two V2 visual direction. Settings now uses a 60rem roomy workspace and 11rem rail, while Connectors receives a full-width discovery gallery with outcome-led cards, category filters, full-catalog search, connected count, and shared setup or edit dialogs. All existing connectors remain included and their storage, credentials, permissions, backup redaction, provider, and Canvas ownership remain unchanged. The full gate passed 214 files / 3,424 tests, TypeScript, diff hygiene, five Node QA contracts, and a 265-module exact production build. Exact Chromium at `2d74afd` proved desktop, short-height, and 375px reflow, one Settings scroll owner, no horizontal overflow, a fully transparent sticky header with a 40px glass blur, an offscreen prepainted Settings surface with motion beginning in 14.6ms, focus restoration, RSS preference and visibility writes, secret non-rendering, a real 2560x1600 photo, and zero console or page errors. Four final captures were inspected at original resolution. Report: `docs/superpowers/reports/TAB-TWO-V2-CONNECTOR-EXPERIENCE-QA.md`. No account, payment, entitlement, merge, or Store action was performed.

- **Background library and lock (2026-08-27, VERIFIED):** Removed owner-rejected catalog photos 19 and 23, added six approved untouched source-resolution landscapes, and expanded the bundled catalog to 27 photos with crisp local Settings previews. Daily Photo now supports keeping the current image and selecting any bundled image; selection locks immediately, unlocking preserves the image until the next local day, and serialized fresh-value updaters prevent delayed rotation from overwriting a new choice. The full gate passed 212 files / 3,409 tests, TypeScript, diff hygiene, and a 263-module production build. Two consecutive exact-commit Chromium runs exercised all six originals and the 27-photo picker at 1600x900 and touch-emulated 375x812 with exact source widths, no overflow, and zero console, page, or request failures. The protected checkout and Chrome Web Store remain untouched.

- **Canvas polish and stack interactions (2026-08-26, VERIFIED):** Free Search and Quick Links are restored as intrinsic photo-first controls; Weather preserves `Dallas, GA` style region labels and separates Clear; Focus keeps its prompt, saved item, check control, and short reduced-motion-safe celebration; compact Status is names and dots with service-specific hover and focus context. Stack grip movement reorders in place, arrows and Remove remain interactive, controls overlay the fixed stack footprint, stacks reach the same 8px edge as ordinary widgets, and dock depth is reduced to `clamp(60px, 10vh, 78px)`. The bounded review found and closed one pointer-control regression and added bottom-edge-safe Status tooltip placement. Final evidence includes 212 files / 3,396 tests, a 263-module build, 31 exact production catalog captures, exact attention, dock, UI recovery, and canvas polish Chromium witnesses, zero accepted runtime errors, and the report at `docs/superpowers/reports/AURORA-CANVAS-POLISH-AND-STACK-INTERACTIONS-QA.md`. The feature branch is approved for GitHub push and MacBook testing; merge and Store state remain untouched.

- **Complete widget redesign production Task 8 (2026-08-23, VERIFIED):** The exact production catalog pins all 34 approved presentation targets and renders Weather plus On This Day, GitHub plus Unified Calendar, Tasks plus Notes, Clock plus Quote, and Jira plus Sentry in dark, light, and saturated panels with both faces visible. The 30/30 accepted Chromium replay measured exact Compact, Standard, and Full frames, an 11px visible text floor, no clipping or internal scroll owner, 15 layouts-only facing writes, one approved Weather alert cache write, a byte-stable legacy `layout`, zero runtime errors, and zero failed or unexpected requests. Visual review caught a synthetic legacy-Calendar fixture and then a real 10px Full weekday floor; the fixture was corrected to model explicit consolidation and the product floor was fixed RED-first. The stabilized suite produced 3,277 green tests plus the corrected 5/5 authority family, TypeScript, 15/15 Node catalog contracts, and a clean 253-module production build. Report: `docs/superpowers/reports/WIDGET-REDESIGN-PRODUCTION-QA.md`. The protected checkout and Chrome Web Store remain untouched.

- **Complete widget redesign production Tasks 6 and 7 (2026-08-23, VERIFIED):** The work/service and browser-native families already matched the approved composition catalog at packet entry, so the takeover preserved their production code instead of restyling owner-approved cards. GitHub and GitLab retain graphs at every tier; Jira, Linear, Sentry, Todoist, Vercel, Status, Home Assistant, RSS, and Crypto retain their provider-specific hierarchy, actions, and state truth. Reading List, Recently Closed, Downloads, and Tab Groups retain 25-record input fixtures, bounded visible rows, unique action names, permission-specific states, and dense dock summaries. Evidence passed 12 connector component files / 280 tests, five browser widget files / 57 tests, 24 connector/security/backup files / 695 tests, and seven browser boundary/permission files / 46 tests. Exact-build SF-P2 Chromium covered 91 work/service scenarios and the deterministic exact-commit preview build covered 39 browser-native scenarios; all 130 captures had exact frames, no internal scroll owner, zero runtime, failed, or unexpected requests, and the browser-native run recorded zero storage writes. Human inspection covered representative Full, Standard, permission, and action-heavy faces. The required GitHub plus Calendar and Jira plus Sentry pairs remain pinned to the centralized Task 8 production catalog. The protected checkout and Chrome Web Store remain untouched.

- **Complete widget redesign production Task 5 (2026-08-23, VERIFIED):** Weather, Sun, Moon, On This Day, and Aurora & Kp retain their existing cache, retry, civil-time, permission, and provider owners while gaining their approved information signatures. Full Weather now exposes hourly conditions plus Air, UV, Pollen, Rain, Sun, and Daily context without requiring disclosure; Standard retains current conditions plus High/Low context, and Compact and Docked remain concise. Sun uses distinct rise and set glyphs. Aurora & Kp uses proportional forecast bars while preserving explicit stale copy. On This Day keeps one heading and date, selects distinct year markers within its row budget, and labels the trusted destination Read more on Wikipedia. Observed RED covered each missing signature. The focused production gate passed 19 files / 266 tests plus TypeScript and diff hygiene. Exact build `7136edc0c2b33abf1c5a578639f3c81f2b77f1c5` transformed 253 modules. The real Chromium matrix found and closed the 10px metadata floor, stale and narrow Full containment, a flex-shrunk Daily row, and a location-selection harness allowlist gap. The final 36/36 pass covered exact tiers, states, four panel colors, proportional narrow frames, Weather plus On This Day stacks, Weather disclosure, and location selection with 26 audited requests, 7 expected storage-write batches, and zero runtime, failed, or unexpected requests. The protected checkout and Chrome Web Store remain untouched.

- **Complete widget redesign production Task 4 (2026-08-23, VERIFIED):** Calendar, Month, and Public Holidays now consolidate explicitly into canonical Calendar per named layout. Legacy placements remain unchanged until the user chooses a surviving placement and Save; Later and stale ownership write nothing, while Save atomically updates `layouts` and its companion preference. Docked and Compact are agenda-led, Standard owns the labelled Agenda/Month switch and complete 42-cell month, and Full renders roomy Month plus Agenda regions. The Standard month header and view control share the reclaimed top row, with the nearest holiday named in a separated footer below the grid. Layout-local Default view and Include public holidays live in the inspector; global week start lives under Calendar settings. The focused gate passed 27 files / 757 tests plus TypeScript and diff hygiene. Exact build `8c8db65c9ee2359e87bf7a9c73c9540ea9c73a34` transformed 253 modules. The bounded real-browser pass caught and fixed Full's flex-over-grid containment defect, then measured exact 216x132, 320x200, and 460x284 faces, 42 Standard and Full cells, zero overflow, presentation-only preference writes, and zero runtime or request failures. The production manifest, protected checkout, and Chrome Web Store state remain unchanged.

- **Complete widget redesign production Task 3 (2026-08-23, VERIFIED):** Tasks, Notes, Timer, Habits, and Bookmarks now consume the explicit presentation context while retaining their existing reducers, persistence hooks, detail panels, and browser authorities. Compact Tasks exposes two direct completion controls plus total open progress without opening the panel. Docked Notes carries one readable note cue; Compact Notes preserves its bounded recent-note treatment. Timer keeps remaining time dominant, Habits adds a completion ring while preserving direct daily toggles, and Bookmarks keeps its free linear bar while gaining purpose-built Compact, Standard, Docked, empty, permission, and error faces. Observed RED covered the missing information budgets and renderer propagation. The ownership regression passed 15 files / 223 tests plus TypeScript and diff hygiene. The bounded exact-build browser review caught one real Compact Tasks overlap; `1cdd2e9` removed the redundant footer and its focused 8-test correction passed. The final 250-module production build at `1cdd2e99d37dc61bd28df02ac2346fc28d47019e` was inspected through a disposable permission-enabled copy because headless Chromium cannot approve the native optional Bookmarks prompt. Two 1408x800 captures covered Tasks and Notes facing the same Compact stack, Standard Bookmarks, Compact Habits, and Docked Timer. Exact 216x132 and 320x200 faces had zero measured clipping; the task action wrote only `todoLists`, stack paging wrote only `layouts`, and runtime errors and failed requests were zero. The production manifest remains unchanged, the protected checkout remains at `eb1354b6a5b041fb6d494655c3dae1862572bc51`, and Chrome Web Store state remains untouched.

- **Complete widget redesign production Task 2 (2026-08-23, VERIFIED):** Clock, Greeting, Search, Focus, Quick Links, Quote, World Clocks, and Countdown now consume the explicit presentation context. Their existing free faces remain photo-first, docked faces remain dense lines where supported, and stack faces use exact authored frames. Greeting uses a transparent free surface and a painted stack surface. Standard Quick Links uses a fixed two-column by three-row mark, name, and destination composition; links beyond six remain reachable through bounded local paging instead of growing or scrolling the frame. Existing search submission, focus editing/completion, link add/remove/drag/keyboard reorder, daily quote, clock timers, local-day rollover, and storage owners remain intact. The bounded real-browser review found three presentation defects: legacy Greeting width overrode the exact frame, default Greeting copy truncated inside that corrected footprint, and negative Quick Link remove controls expanded the grid while long hosts crowded destination copy. Observed-RED corrections closed all three; the final Chromium rerun was clean. Final evidence is 18 files / 193 tests, an App and stack path slice of 4 files / 120 tests, TypeScript, diff hygiene, exact 250-module build `125d483685d8cac272a286c78343559b37b6b03b`, and six individually inspected 1408x800 captures covering frameless Greeting, dark/light/bright-pink Standard Quick Links, and Clock plus Quote mixed-stack faces. Every face measured exactly 320x200; all names and destinations fit; ordinary clicks selected nothing; storage writes, runtime errors, and failed requests were zero. The protected checkout and Chrome Web Store remain untouched.

- **Complete widget redesign production Task 1 (2026-08-23, FOCUSED GATE GREEN):** Production renderers now receive one explicit `free`, `stack`, or `docked` presentation context. `CanvasSurface` sends `stack` to every mounted stack owner, `docked` only to dock members, and `free` to anchored widgets; `App` derives the legacy Boolean dock signal during the bounded migration so existing widget behavior remains unchanged. `TierFrame` can remove only card paint through `surface="none"` while retaining the exact tier geometry, accessibility region, focus contract, and one mounted data owner. Observed RED preceded implementation. Focused evidence is 6 files / 92 tests plus TypeScript and diff hygiene. The production family packets are next. The protected checkout and Chrome Web Store remain untouched.

- **SF-P2 framed catalog migration (2026-08-23, OWNER VISUAL GATE):** All 25 remaining Framed identities now use the exact shared Compact 216x132, Standard 320x200, and Full 460x284 frame system only at authored tiers; Weather and On This Day remain the accepted reference pair. Every migrated family has bounded tier-authored content, retained signature context and actions, no framed internal scrollbar, and one mounted data owner. The bounded review found three Important evidence/ownership gaps; focused RED/GREEN fixes added truthful setup and permission states, preserved the actual hidden renderer behind legacy compatibility faces, and made swipe/plain-click probes exercise real interactions. The one rereview returned Ready with no Critical or Important issue open. Evidence at exact reviewed build `c4f98d0`: 106 files / 1,402 focused pre-review tests, one 200-file / 3,217-test full gate, 4 files / 110 fix tests, 40/40 SF Node contracts plus 9/9 information-first contracts, TypeScript clean, and 189/189 Chromium captures with 49 Compact, 107 Standard, and 33 Full frames. All 189 originals are byte-identical to the inspected reviewed catalog; 36 request records, 20 layouts-only interaction writes, and zero capture, runtime, failed-request, unexpected-request, clipping, internal-scroll, owner-count, or legacy-layout failures are recorded. Report: `docs/superpowers/reports/SHARED-FRAMES-SF-P2-QA.md`. Accepted Minor debt is limited to SVG-only clipping relying on native-resolution inspection. SF-P3 remains blocked on owner acceptance. The protected checkout and Store remain untouched.

- **SF-P1 frame foundation and reference pair (2026-08-23, VERIFIED - OWNER ACCEPTED):** The owner accepted the exact shared frame foundation and authored Weather plus On This Day reference pair after reviewing the final owner-facing Full captures. The bounded review returned With fixes; one focused TDD fix/rereview cycle returned Ready with no Critical or Important issue open. Evidence includes 23 files / 459 focused pre-review tests, one stabilized 198-file / 3,093-test full gate, a 10-file / 212-test independent rereview slice, App 53/53, Node 24/24, TypeScript clean, 36/36 exact-build catalog captures, 20 audited requests, 4 expected stack-facing write batches, exact 216x132, 320x200, and 460x284 desktop frames, proportional 388x239.546875 Full frames at 412px with stored tier unchanged, four panel themes with minimum 6.3002:1 accent/focus contrast, and zero runtime, failed-request, unexpected-request, clipping, or internal-scroll failures. A headed non-emulated `viewport:null` witness from exact build `62dacdd65e41f52a71cc273dc28bb6ca44cddbc2` measured Full Weather at 460x284 in a real 1889x1988 CSS viewport with zero writes or failures. Report: `docs/superpowers/reports/SHARED-FRAMES-SF-P1-QA.md`. Accepted Minor evidence debt is recorded there. SF-P2 just-in-time planning may begin. Store state and the protected checkout remain untouched.

- **DY-P1 two-axis dock placement (2026-08-22, VERIFIED):** Every top and bottom dock member can now own independent safe X/Y coordinates inside the transparent responsive band. Existing absent-Y documents preserve exact rectangles, screenshot pixels, storage bytes, and reading order; direct drag crosses canvas and either dock without a Compact prerequisite; undocking restores the recorded free tier. Live peer/center guides are transient, Alt bypasses magnetism, overlap truth updates during movement, explicit edge coordinates receive only a storage-neutral runtime clamp, and one gesture owns one Undo entry. The bounded review returned With fixes; one fix/rereview cycle returned Ready with no Critical or Important issue open. Final evidence includes 28 files / 558 focused tests, 8/8 harness contracts, 197 files / 3,075 stabilized tests, TypeScript clean, 12/12 exact absent-Y rectangles, 3/3 byte-identical legacy screenshots, exact 599/600 boundary behavior, and a headed non-emulated 1408x445 DPR 1 witness from exact build `dc828c34abfece5f50fbb9e541c16c82f382ee0f` with one layouts-only Save and zero runtime, request, or harness failures. Report: `docs/superpowers/reports/TWO-AXIS-DOCKS-QA.md`. SF-P1 is next but remains unstarted until its just-in-time implementation plan is written. Store state and the protected checkout remain untouched.

- **DY-P1 5px dock inset refinement (2026-08-22, OWNER TRIAL VERIFIED):** The owner identified that the original 16px edge and 72px side perimeter left corner regions classified as canvas, then approved a bounded 5px top, bottom, left, and right trial. The rendered bands, drag fallback, transient boundaries, and toolbar clearance now share that exact geometry. The original absolute absent-Y baseline remains immutable historical evidence; this approved parent-band move intentionally changes its viewport rectangles while preserving stored absence, layouts bytes, reading order, member containment, and internal 16px/2px legacy margins. Observed RED preceded production changes. Focused evidence is 5 files / 118 tests plus 10/10 harness contracts and TypeScript clean; the stabilized gate is 197 files / 3,076 tests. Exact-build Chromium at `1e9a1afe3275b6688cf24676d4694dbc5d237e96` measured both 600x800 and 1408x445, proved seeded bytes survive the measured boot, proved 5,5 remains docked while 4,4 exits to canvas, captured both edited compositions, and recorded zero writes, runtime errors, failed requests, or harness failures. Report: `docs/superpowers/reports/DOCK-INSET-REFINEMENT-QA.md`. SF-P1 remains next and unstarted. Store state and the protected checkout remain untouched.

- **Shared frames and two-axis docks (2026-08-22, DESIGN APPROVED):** The owner approved exact shared painted frames for every framed or stacked widget at a selected tier: Compact 216x132, Standard 320x200, and Full 460x284 CSS px. Desktop frames never auto-grow; narrow safety may only scale them down without changing the stored tier. Every widget keeps an authored information hierarchy, signature data is resized before it is removed, framed cards use no internal scrollbar, and excess content opens a truthful details surface or trusted destination. Intrinsic free forms remain intrinsic but receive purpose-built framed stack faces. Stack tier choices become the intersection of member stack contracts, replacing intrinsic tallest-member sizing and silent nearest-tier fallback. The owner also approved independent X/Y placement inside a transparent `clamp(96px, 16vh, 128px)` top or bottom dock band, with transient two-axis peer/center guides, Alt bypass, live overlap truth, exact legacy absent-Y rendering, and restoration of the pre-dock free tier. The two independent authorities are `2026-08-22-aurora-shared-widget-frames-and-stack-composition-design.md` and `2026-08-22-aurora-two-axis-dock-placement-design.md`. DY-P1 is Verified; shared-frame production remains deferred until SF-P1's just-in-time plan is written. Store state and the protected checkout remain untouched.

- **Widget-stack control refinement (2026-08-22, VERIFIED):** The owner approved arrows that are completely invisible until stack hover, while keyboard focus must reveal the same controls and touch retains swipe plus direct dots. Resting arrows now have zero opacity, hidden visibility, and no pointer events; hover or focus-within restores them. The stack gesture surface suppresses native text selection so horizontal paging cannot drag a highlight across the page. Review found one Important React-portal edge: editable controls painted outside the card still bubble through StackCard, so input, textarea, select, content-editable, and textbox targets now refuse to arm paging while their local editing remains intact. Observed RED covered both missing CSS contracts and all four editable-control paths before production changes. The one fix and rereview cycle returned Ready with no finding open. Final evidence: 195 files / 3,035 tests, 82 focused interaction tests, 8/8 information-first contracts, TypeScript and diff hygiene clean, and a real Chromium stack run with 24 captures, exact Previous to Next to first-dot Tab order, no selected text after a 50px swipe, and zero assertion, runtime, or failed-request failures. Normal captures now fail if any stack remains hovered or focused, preventing a focused state from masquerading as rest. No storage, schema, renderer, permission, dependency, Store, or protected-checkout boundary changed.

- **At-a-glance additions (2026-08-22, VERIFIED):** On This Day, Public Holidays, and Aurora & Kp now ship as keyless public-data widgets with Compact, Standard, Full, and Docked presentations. Weather alone owns official NWS active-alert enrichment, preserving its Open-Meteo forecast and environmental authorities. Public Holidays uses an explicit country picker and rejects a stale editor instead of overwriting a newer configuration. Full-store replacement rotates derived-cache ownership even when connector config or Weather coordinates are byte-identical, and Weather failure retries remain visible-only at a bounded 30-second cadence. The initial review found five Important issues; one rereview closed three and retained two original gaps. Focused observed-RED corrections then added the firing-time visibility guard and completed the honest browser matrix without another review cycle. Final evidence at exact `75500790b1f5fca5a3fa7120184d7bb27f8705e7`: 195 files / 3,032 tests, 35 Node plus 103 Vitest expansion contracts, 8/8 information-first contracts, TypeScript clean, a 247-module exact preview build, and 38 individually inspected Chromium captures with 30 exact provider requests, 38 storage records, and zero assertion, runtime, failed-request, or storage-boundary failures. The matrix includes all four tiers, exact 1408x445, setup, empty, stale, hard error, unsupported NWS coverage, maximum data, year boundary, dock detail, and a real emulated local-midnight crossing through Aurora's focus reschedule. Report: `docs/superpowers/reports/AT-A-GLANCE-ADDITIONS-QA.md`. The protected checkout and Chrome Web Store remained untouched.

- **Work connectors (2026-08-22, VERIFIED):** Linear, Sentry, and Todoist now ship as direct, account-scoped integrations with Compact, Standard, Full, and Docked tiers plus structured Settings pickers. Linear shows assigned and due-soon work with the nearest due issue; Sentry shows unresolved and critical counts with the top trending issue; Todoist shows due, overdue, and today counts and supports only an explicit named confirmation for completion. Reconnect clears account-bound choices, picker caches reject another account's scope, and connector ownership is revalidated before both request start and cache commit so stale credentials cannot make one final request after disconnect or replacement. Provider origins, request routes, methods, authorization headers, filters, storage redaction, and backup boundaries remain exact. The initial review and one rereview exposed four Important evidence and account-isolation gaps; focused RED/GREEN corrections closed all four plus the stale-owner request race. Final evidence at `9fc1eec723dc21db5c2d6b5dd49300bbc4c0ff0b`: 186 files / 2,968 tests, 35 Node plus 103 Vitest expansion contracts, 8/8 information-first contracts, TypeScript clean, a 237-module exact production build, and two consecutive 58-capture Chromium passes with zero assertion, runtime, failed-request, or unexpected-request failures. Report: `docs/superpowers/reports/WORK-CONNECTORS-QA.md`. At-a-glance additions are next. The protected checkout and Chrome Web Store remained untouched.

- **Browser-native widgets (2026-08-22, VERIFIED):** Reading List, Recently Closed, Downloads, and Tab Groups now ship as four independent browser-owned widgets with Compact, Standard, Full, and Docked tiers. Feature-specific permissions remain optional in production; no broad `tabs`, `history`, host, or `downloads.open` permission was added, and browser results are never persisted. Full tiers normalize and locally scroll a maximum of 25 results; Docked permission, empty, and error states stay one dense line and open truthful details. Recently Closed deliberately uses only Sessions API facts: session kind, age, and window tab count. The initial review found six Important issues; one observed-RED fix cycle closed all six. Rereview confirmed them and found one new duplicate-accessible-name issue introduced by the privacy correction; observed RED preceded `a22dfe9`, which added distinct type, age, count, and row-position context without broadening permissions. Final evidence at exact `a22dfe90399452a27446fa0c43b08f1fb687e2c6`: 177 files / 2,841 tests, 15 focused files / 544 tests, TypeScript clean, 34 Node plus 103 Vitest expansion contracts, a 229-module rebuilt preview, and 56 individually inspected captures with zero assertion, runtime, request, external-request, forbidden-API, or storage-write failures. Report: `docs/superpowers/reports/BROWSER-NATIVE-WIDGETS-QA.md`. Work connectors are next. The protected checkout and Chrome Web Store remained untouched.

- **Expansion platform (2026-08-22, VERIFIED):** Aurora now has a developer-facing expansion safety system rather than another runtime authority: a validated 40-candidate research catalog, deterministic Markdown generator, guarded widget/connector/browser-native scaffold, executable widget identity/tier/migration and connector identity/privacy/origin contracts, a single `test:expansion-contract` command, and scratch-only Chromium catalog output. The initial bounded review found one Critical and six Important issues. Observed RED tests preceded `b895207`, which fixed caller-controlled protected-root bypasses, independently pinned runtime origins, candidate-specific tier/cache/state research, browser permission wording and Uptime network truth, frozen connector-origin behavior, exact migration-step checks, per-widget contract drift checks, check-mode wording validation, and painted-content usefulness checks. The same reviewer returned Ready with no Critical or Important issue open. Stabilized evidence: 166 files / 2,759 tests, TypeScript clean, 8/8 information-first contracts, 34 Node plus 97 Vitest expansion contracts, 18 combined scratch/output-safety tests, a 218-module preview build, and 76 useful captures across both existing widget catalog batches with zero assertion, runtime, request, or external-request failures. The reviewed implementation range is `03eb9c6f0978ac3a638db78090fd071b91255258..b8952070c1b44803eda2b98b170bc90f82eab864`; report: `docs/superpowers/reports/EXPANSION-PLATFORM-QA.md`. Browser-native additions are next. The protected checkout and Chrome Web Store remained untouched.

- **Weather enrichment (2026-08-22, VERIFIED):** Expanded Weather now uses its accepted whitespace for current US AQI, UV, and provider-available pollen while Compact and Docked remain unchanged. The original forecast URL and `open-meteo:v1:` identity are byte-compatible; the optional `open-meteo-air:v1:` leg is separately identified, validated, backed up, privacy-disclosed, race-safe, and bounded so a failed or hung environmental request cannot suppress forecast. Old/mismatched cache self-heal remains under the one Weather request owner, explicit Refresh recovers without hiding cached forecast, and successful no-readings, pollen unavailable, and endpoint failure each have distinct truthful copy. The independent review found three Important issues; observed RED tests preceded `32fed81`, and the same reviewer returned Ready with none open. Final evidence: 162 files / 2,737 tests, TypeScript, 8/8 information-first contracts, 7/7 scratch-output contracts, 3/3 Weather harness contracts, a 218-module build, and 9 individually useful original-resolution captures over exact 1408x445, four corners, pollen unavailable, Docked, and reload. Six exact provider requests completed with zero failures, unexpected runtime errors, or failed requests; permissions, origin owners, and all non-Weather storage remained unchanged. Report: `docs/superpowers/reports/WEATHER-ENRICHMENT-QA.md`. No Store action occurred.

- **Widget stacks (2026-08-22, VERIFIED):** Named layouts can now own manual cards with ordered existing widget identities and one stable face. All members mount once in one grid, only the facing member is visible and interactive, and the stored shared tier resolves independently per member so a compact-only face cannot collapse a Full stack. A continuous 500ms editing hold is the only creation path; ordinary overlap stays movement. Users can page with arrows, dots, swipe, or keyboard, drag the whole stack, append a widget, reorder, remove, detach, or dissolve members, and Undo/Cancel remains exact. Normal paging is the one immediate serialized `layouts` write; edit changes stay draft-only. Native `pointercancel` restores the exact pre-preview session and never invokes drop semantics. The bounded review found two Important issues (face-dependent tier collapse and cancel-as-drop); seven observed RED regressions preceded `619da14`, and the one rereview returned Ready with none open. Evidence: 172-cell full scratch matrix, 24-cell post-fix stack rerun, exact 1408x445 DPR 1 nine-stage real-window witness, 161 files / 2,679 tests, TypeScript, 8/8 information-first contracts, 7/7 output-safety contracts, and a 217-module build. Report: `docs/superpowers/reports/WIDGET-STACKS-QA.md`. Manual ceilings are real touch hardware, real screen-reader speech, mixed-DPI pointer cancellation, live connector combinations, and subjective long-session judgment. No Store, permission, manifest, dependency, credential, request-contract, or protected-checkout boundary changed.

- **NL-P5 appearance ink system (2026-08-19, owner-approved all three layers, "soft at rest but white on hover", and the standing directive "let's not develop specifically for black widgets, people may want bright pink" — saved to assistant memory):** The owner-observed mismatch was one base ink at two strengths applied ad hoc (bookmarks/settings gear muted, Tasks/Notes/Timer/weather digits full). Layer 1, the chip law: every one-line chip and dock line rests at the SOFT tier and brightens to full on hover — the three launcher pills flipped to `text-fg-muted hover:text-fg`, `.dock-line` owns its color (muted rest, full hover incl. the wrapper-hover path), and DockLine/weather/clock secondary facts moved from fixed muted classes to RELATIVE 0.68 opacity so they brighten with the line while attention/critical tones keep their tints. Layer 2: `settings.widgetTextColor` — applyInkColors (composed after applyPanelColor; clearing re-derives) overrides `--fg`/`--fg-muted`, the muted tier DERIVING from any pick at the standing 0.68 alpha (`mutedInk`), with an advisory (never blocking) WCAG 4.5 contrast warning against the actual panel color (`contrastRatio`). Layer 3: `settings.photoTextColor` re-inks photograph text via the new `--photo-ink` chain (themes.css routes `--canvas-fg` through it), with per-element `photoClockColor`/`photoGreetingColor`/`photoQuoteColor` overrides scoped per canvas-item var chains, and a dark-pick advisory (luminance floor 0.25). Storage per the STANDING RULE: CURRENT_VERSION 13→14, migrations[13] backfills the five nested nulls, METADATA_ONLY_FLOOR moved to 14 (its rule) with the v11/v12 metadata-only tests rewritten to the full-path reality and the floor guard probe hardened to OMIT an ink field (a defaults-shaped probe would let a non-identity step masquerade), backup isSettings covers all five. Settings UI: the picker machinery extracted to a shared ColorPickerRow (labels/behavior byte-compatible with the widget-color tests); Appearance gained Widget text, Photo text, and a per-element disclosure. The witness proves the system on BRIGHT PINK panels (per the directive) with measured computed colors — chips at the derived muted ink brightening to the full pick on hover, clock override beating the global photo ink, greeting AND quote (measured `rgb(255,215,0)`, correcting my own misreading of the compressed capture) taking it — and CAUGHT one real regression fixed under it: the full-width lane swallowed pointer events across the whole band (Tasks launcher/settings gear unreachable); the strips are now pointer-transparent with only members as hit targets, CSS-pinned. Full `npm test` 154 files / 2545 green, TypeScript clean, `dist` rebuilt, checkpoint `3b6b2cd`. Single-coin crypto (owner: "sometimes people just want one") shipped separately at `e95ba80`.

- **NL-P5 chrome fixes (2026-08-19, owner-reported):** The corner "New background photo" button was genuinely unclickable — root-caused, not guessed: it renders BEFORE the canvas surface, and the surface (`position: relative; isolation: isolate`, full-viewport `minHeight`) hit-tested above it as a positioned later sibling, swallowing every click (the settings gear only survives by rendering after the surface). `z-10` restores it, and the witness now performs a REAL click and asserts `photoPrefs` advanced. The Utility Tray question (owner asked twice) resolved by earning its place: the tray's Refresh tool is a duplicate of that photo button, so the briefcase trigger renders ONLY when Home Assistant actions exist (the one thing the tray offers that nothing else does), pinned by an App test for the no-HA absence and the witness; the two tray behavior tests seed an HA action and keep their modeless/modal coverage. Full `npm test` 154 files / 2546 green, witness 19 stages green, `dist` rebuilt, checkpoint `c32d30a`.

- **NL-P5 badge position (2026-08-19, owner-reported):** The layout badge (the "profile" pill) kept a fixed 108px right offset that existed solely to clear the utility-tray trigger — which now hides itself — leaving a dead gap beside the settings gear. The badge hugs the gear at 60px and slides to 108px only while the trigger actually renders (`clearsTray` prop → `data-clears-tray` host attribute, App-test-pinned both ways). The owner also noted the badge disappearing during an edit session: confirmed INTENDED (spec 2.5 — the edit toolbar's own Layout switcher replaces it while a session is live) and answered in chat, no change. Full `npm test` 154 files / 2547 green, `dist` rebuilt, checkpoint `aab24ca`.

## Packet envelope

- **Flow (2026-08-21, VERIFIED):** Schema v15 adds one nullable `timerSession` authority with an absolute running deadline, exact backup validation, and privacy inventory coverage. The dashboard Timer and immersive Flow surface share one App-level provider; actions and deadline rollover reread both session and duration config inside the same storage lock, idle or paused sessions own no clock, and the stable Flow context prevents countdown ticks from invalidating the Canvas. Flow leaves the photograph but suppresses Canvas, docks, layout/editor chrome, Settings, Utility Tray, palette, and photo controls; it renders today's focus, the live work/break timer, and the first unfinished task, with Escape or End flow restoring the exact named dashboard and writing no layout state. The initial review found three Important issues (stale cross-tab config, idle App invalidation, and a self-fulfilling vertical QA bound); focused RED/GREEN fixes landed in `5802e59`, and the single rereview returned Ready with none open. Post-fix evidence: 148 scratch captures with zero failures/runtime errors/failed requests/writes, exact 1408x445 DPR 1 real-window proof with synchronized second tab and two timerSession-only interaction writes, TypeScript, 8/8 information-first and 6/6 output-safety contracts, and a 214-module build. The stabilized full test run reached 158 files / 2,615 tests with 2,614 green and one test-only launcher Escape timing drift; `373784f` flushed the passive dialog registration and the required causal rerun passed, with no second full-suite churn per plan. Manual ceilings remain real screen reader, sleep/wake, timezone change, audible host behavior, and long-session judgment across arbitrary photographs and colors. No Store, permission, manifest, dependency, connector, or accepted NL-P6 evidence changed.

- **Program A baseline stabilization (2026-08-21, VERIFIED):** The owner authorized continuous back-to-back delivery without routine continuation prompts. Program A reconciled the named-layout source of truth, removed the two remaining React `act(...)` warning families, made the information-first contract a first-class npm command, and made accepted NL-P6 evidence unwriteable by ordinary QA reruns. The bounded review found one Important redirected-output hole; the single fix/rereview cycle closed it by rejecting symlinks/junctions before any scratch write and returned Ready. Stabilized gate: 155 Vitest files / 2,570 tests with no `act(...)` warning, TypeScript clean, information-first contract 7/7, output-safety contract 6/6, and a 211-module production build. The first scratch sweep exposed one tooling-only stale Weather request identity after `wind_direction_10m` joined production; an observed-RED contract and `ecace7e` aligned both fixture builders. The single causal rerun passed 124 cells with zero failures, runtime errors, failed requests, unexpected external requests, or storage writes. The real operating-system witness measured exactly 1408x445 at DPR 1 and passed settle, live edit, Save/reload, and zero-write observation. Original-resolution inspection covered fresh, named-saved, connectors-default, edit, dock-hover, narrow-floor, and real-window captures. The accepted F9 all-widgets density ruling remains visible and unchanged by owner decision; no automatic re-flow was introduced. `docs/superpowers/qa/nl-p6` stayed byte-identical. Store mutations remain blocked by W6-P5.

- **NL-P6 evidence (2026-08-19, OWNER ACCEPTED):** The product QA gate ran under the corrected A2-D060 standard: **124 captures, every one individually judged, zero left unjudged** (the A2-D059 failure mode, closed by construction — the report generator seeds every row `_pending_` and the gate is not done until none remain). Matrix: FIVE storage shapes (fresh install; a legacy V1-shaped `layout` with user positions; a saved named-layouts document exercising free/docked-x/docked-tier/legacy-align/hidden placements plus custom appearance inks; the nine-connector fixture over that document; and — added mid-pass because nothing else covered it — **every connector enabled with PURELY DEFAULT placements**, the real new-user shape and the only way to separate a product defect from a user's own authored overlap) x TWELVE viewports (the short-height desktop family including the owner's exact 1408x445, the common core to 3440x1440, the 720px bookmarks boundary, and both sides of the 600px narrow floor) x normal/edit, plus hover-dock probes. Programmatic invariants hold at every cell: no horizontal page overflow, no degenerate box, nothing fully offscreen, the settings gear always hit-testable, and the write log layouts-only (the frozen legacy `layout` key is never written by the running product; a cancelled session writes nothing at all). A REAL OS window — not viewport emulation — measured at exactly 1408x445 CSS px passed free drag, dock-out-and-back-in, Save + reload round-trip, and exact-cancel. **Nine defects found; eight fixed under this packet, each TDD with observed RED:** F1/F2 clock-greeting-focus collision on a fresh install at short heights; F3 the Tasks launcher permanently unclickable under the badge/gear cluster at <=1024 widths; F4 sun/moon over the Month card; F5 nine standard connector cards that cannot compose in one column (connectors now default to their compact glance); F6 edge-placed widgets pushed off-screen in a narrower window (a live edge-safety clamp that touches no storage and moves no neighbour); F7 an enabled-but-unconfigured widget rendering an invisible GHOST — selectable, draggable, chrome-bearing, counted in overlap warnings — now wholly inert, detected with a MutationObserver because widgets fill in asynchronously and a render-time check latched "empty" forever and stripped a real widget's chrome (caught by App.test before it shipped, not after); F8 the designed default table colliding with ITSELF at common heights (Calendar struck the Month card 25-40px at every size including 1920x1080), literals re-derived against measured card heights so 1920x1080 is now completely clean. **F9 was put to the owner and RESOLVED 2026-08-19 as accept-as-is** ("let the user decide if they want a whole screen filled with plugins" — the no-re-flow law stands, unamended): with 22+ widgets enabled at once on a 1408x445 window the two side columns crowd (~15 small overlaps), because percent positions against pixel-height cards shrink as the window shortens and the spec's own law forbids auto re-flow; everything stays legible, movable, and reachable, and the owner ruled that a user who fills their screen has made a choice the product honors rather than overrides. Residual minors are recorded rather than tuned (a 2-15px Headlines/Tasks meeting at 1366x768) because chasing them risked re-breaking F3. Full packet gate: 155 files / 2562 green, TypeScript clean, `dist` rebuilt. Report: `docs/superpowers/qa/nl-p6/QA-REPORT.md`. Acceptance was checkpointed in `3e9a003`; Program A stabilization is now active.

- **Release state:** The owner rejected the installed `864ffce` presentation as production-ready. The current local ZIP, Store images, hash, and dossier remain historical evidence only and must not be uploaded.
- **Current acceptance:** The named-layout and live-canvas product is owner accepted through NL-P6, including the F9 ruling that intentional user-authored density is not auto-reflowed. Program A, Flow, widget stacks, Weather enrichment, the Expansion platform, browser-native additions, Work connectors, and At-a-glance additions are verified.
- **Approved written design:** `docs/superpowers/specs/2026-08-17-aurora-named-layouts-live-canvas-design.md` remains the layout authority. `docs/superpowers/specs/2026-08-21-aurora-continuous-delivery-roadmap-design.md` governs the continuing sequence without reopening frozen boundaries.
- **Last completed executable plan:** `docs/superpowers/plans/2026-08-22-aurora-at-a-glance-additions-implementation.md` implements On This Day, Public Holidays, Aurora & Kp, and NWS Weather alerts from the approved At-a-glance design.
- **Historical rejection state:** The owner rejected the earlier PR-P6 result against the installed extension at 1408x445. A2-D060 preserves that evidence and its root-cause fixes as history. The replacement named-layout NL-P6 matrix added short-height desktop and existing-layout-shaped scenarios, was individually judged, and was owner accepted in `3e9a003`; the old reopened state is no longer current.
- **NL-P5 batch-1 evidence:** The Docked tier exists as a real render path: `WIDGET_SIZE_CONTRACTS` declares one-line Docked content contracts for exactly clock, focus, weather, timer, tasks, notes, and bookmarks (with `supportsDocked` derived, never hand-maintained), and the `docked` presentation flag threads from the strip through `renderWidget(entry, size, docked)` into the SAME widget renderers, so each widget keeps sole ownership of its data and panels (docked XOR free). Weather and Clock gained designed dense lines - `76°F · Dallas · Partly cloudy` (icon + temperature + location + condition, middle dots) and `6:52 · Tue, Aug 18` - with the Weather line opening the identical details panel via the shared trigger (spec 2.4 click parity, tested). Timer/Tasks/Notes chips, the Bookmarks full bar (spec exemption), and the Focus line are declared as their own Docked forms rather than rebuilt; greeting, search, and quote declare no Docked tier in batch 1. The tracked owner-review catalog (`docs/superpowers/catalog/batch-1/`: 26 captures + CATALOG.md with per-tier contracts and verdict lines) was generated by `scripts/catalog-nl-p5.mjs` from the production preview build at 1600x900 with seeded content-rich data and always-on ritual widgets parked out of frame; the run reported zero failures, runtime errors, or failed requests, and key captures were inspected individually (the full Weather card is dense and complete; docked lines read as clean status-band facts). The focused gate passed 21 files / 310 tests with TypeScript, diff hygiene, and a 208-module production build clean. The bounded review found no Critical and two Importants, both about the honesty of the owner-gate deliverable itself: the catalog script claimed a contract cross-check that was dead code, and five tier pairs rendered byte-identical captures without disclosure. The single fix cycle (`247b73b`) replaced the dead check with a real drift guard (every catalog contract string must appear verbatim in `widgetSizeContracts.ts` or the run fails) and added automatic hash-detected identical-capture disclosures to CATALOG.md (the Bookmarks pair cites its spec exemption; the others state that tier differentiation is pending owner direction), plus minor cleanups and a stated owner call on the docked Weather line omitting staleness feedback. Noted plan-estimate correction: the honest capture matrix per the declared contracts is 26, not the plan's ~31. The rereview returned Ready with all checks reproduced; one residual nit is ledgered (the drift guard is a file-wide substring check that could false-pass if a stale string still appears under a different widget or tier). The owner reviewed batch 1 on 2026-08-18 and APPROVED it with two refinements, both applied the same day with focused RED/GREEN coverage and regenerated catalog captures: the compact Weather chip's F/C scale letter dropped from its inherited 14px to the documented 12px metadata floor, and compact-TIER Bookmarks became the explicit opt-in for single-letter folder marks (N for News, D for Docs, M for Music — `folderMonogram` now always returns one initial, and tier-gated CSS activates the mark form at any viewport width while the dock strip keeps the full readable bar per the exemption). Owner verdicts are recorded as data in the catalog generator so regeneration preserves them. Batch 1 is accepted; batch 2 (connector widgets) may begin.
- **NL-P4 evidence:** Docks are user-created and only user-created: dragging a widget within 56 CSS px of the canvas top or bottom edge in an edit session highlights the drop zone, and dropping there is the single production path into the pure `dockSelected` operation (insertion index from the pointer against member centers; orders renumbered compactly in both docks). Order is draggable - dragging a docked item undocks it on the first real move (one mechanism: drop back into a zone reorders, drop on the canvas undocks to a Standard-tier anchor at the point), and an emptied dock disappears. The strips are the spec's clean status band: the nav owns position, the inner scroller owns a structurally scrollbar-less horizontal scroll (`scrollbar-width: none` plus the webkit rule), fade masks key exclusively on the MEASURED `data-dock-overflow` state (a ResizeObserver on the members keeps it truthful as async widget content grows), wheel scroll is a native non-passive listener that prevents default so dock scrolling never moves the page, arrow keys scroll when overflowing, and subtle hover nubs appear at the faded edge only. Docked interiors stay live in normal mode (click parity witnessed) and inert only during a session. The focused gate passed 23 files / 501 tests with TypeScript, diff hygiene, and a 208-module production build clean. The six-stage real-Chromium witness passed (create/persist, reorder, undock with empty-dock disappearance, true-overflow contract at 900x600 including the page-unmoved wheel proof and the no-affordance-when-content-fits check, docked-click parity, and the dock-flow write log) after exposing three real defects fixed under it: docked items received no editing chrome so strip drags never started, the scroller's flex `min-width: auto` grew past the nav instead of clipping, and React's passive `onWheel` rejected the local-scroll preventDefault. The bounded review found no Critical and two Importants (the dock insertion index counted against a rect map that is stale after the undock re-center or a strip scroll, and a zone-drag gesture cost two undo entries); the single fix/rereview cycle closed both in `7e1eeb8` - the index is measured fresh at the drop and the drop completes through `dockSelectedLive`, reusing the gesture's one undo entry, with a pure regression proving a single Undo reverts the whole gesture - plus the witness's dead helper removal and a stage-5 harvest asserting the docked panel click never touches the frozen legacy `layout` key. Ledgered for NL-P5: the undock tier defaults to Standard (docked placements store no tier until Docked tiers are designed), and docked widgets render their compact compositions in the strip until the tier catalog delivers true Docked tiers. Minor debt: stacked items now receive editing chrome too (deliberate - interiors should be inert during a session - but untested at the narrow floor), `useDockOverflow` allocates state per scroll tick and its ResizeObserver path lacks unit coverage, and the strip's keydown guard excludes only form controls rather than every arrow-driven widget role.
- **NL-P3 evidence:** Editing now happens on the real page. Normal use: hovering any anchored widget fades in a grip and a gear; the gear opens Settings focused on that widget's own row or connector card (deep-link with a retry that lands after the Drawer's own focus management); plain clicks still paint nothing. The pure `editSession` draft model (baseline = the initial materialized draft, so what the user sees, edits, resets to, and saves are the same stored values; bounded undo; mutation-verified suite) drives a session entered by grip, whole-widget grab, badge "Edit layout", touch long-press (retained `useLongPress`), or Ctrl/Cmd+Shift+E: the page dims behind a scrim, interiors go inert with no DOM-shape change, the wrapper becomes the selection target, drags reuse the retained `canvasSnap` (8px grid, 6px magnetic guides rendered as surface hairlines, safe-margin clamps) and stream through one undo entry, arrows nudge 8px and Shift+Arrow 1px, Escape exact-cancels through the shared dialog stack with zero writes, and Save is the single committing write - also the moment an unsaved My layout first materializes. The floating inspector offers declared-size tier radios, layer forward/backward, per-layout Hide (`{ kind: 'hidden' }`, a new additive placement kind that backups round-trip; never re-added by the default slot; the global toggle untouched), restore-to-designed-slot, and the passive overlap note (warned, never corrected). The toolbar carries the switcher (disabled while dirty), the bulk tier control (AC9 re-baselining proven), Undo, Reset, Cancel, Save. The layout badge (spec 2.1) shows the active name and switches, edits, or creates layouts; Settings Layout gained the full management list (switch, rename, duplicate, reorder, two-step delete with last-layout protection) - every action one pure operation plus one validated `layouts` write. Dock drop zones are deliberately absent (NL-P4). The focused gate passed 24 files / 641 tests with TypeScript, diff hygiene, and a 207-module production build clean. The twelve-stage real-Chromium witness passed with the write log harvested across reloads: exactly four storage writes in the whole run, every one touching only `layouts`; it exposed and fixed two real defects (the gear-focus race and the missing in-session whole-widget drag handle) and its captures were inspected individually - the edit session renders the spec's picture (dim, slim toolbar, content-tight selection outline, dashed Weather footprint parked in corners). The bounded review found one Critical (the entry chord during a live session silently discarded the draft) and four Importants (unguarded keydown target casts polluting the gate with unhandled errors, Hide as a dead end after Save, long-press not touch-only, and a stale-prop double-apply window in Settings management); the single fix/rereview cycle closed all of them in `24e095d` - begin is identity while a session lives, both handlers guard `instanceof Element`, the toolbar gained the "Hidden in this layout" Show controls backed by the new `restoreHiddenWidget` operation, long-press filters to touch and suppresses below the narrow floor, management latches inert until the written document round-trips, Save carries an in-flight guard, and the witness gained the plan-specified 1408x445 stage (13 stages green, four writes, all layouts-only). Deferred as Minor ledger debt: `documentsEqual` can report a conservative false-dirty on foreign placement key order; keyboard-nudge and drag conversions use the window viewport rather than the surface rect (equivalent until NL-P4 docks change page height - revisit there); the premium gate for hover-chrome visibility awaits a real free tier (isPremium is hardcoded true); generated layout names may collide (ids stay unique); and the badge menu lacks arrow-key navigation. The rereview returned Ready with independent verification (644 tests, zero unhandled errors) and added three residual minors to this debt list: a 500ms MOUSE hold still swallows its release click through the retained long-press suppressor even though it no longer enters edit (filter pointerType at the hook's arming step when the hook is next touched); the Settings management inert latch can stick if a write fails or produces an unchanged document (clear it in a finally or skip no-op commits); and many hidden widgets could overflow the non-wrapping toolbar pill.
- **NL-P5 batch 2 evidence:** Fourteen more widgets declare Docked contracts (the nine connectors plus habits, worldClocks, countdown, sun, moon; monthCal and links deliberately none). The shared `DockLine` renders the middle-dot dense row with the Work Pulse tone palette and returns nothing when no fact survives, enforcing the no-whitespace law structurally. Ten widgets gained docked branches fed from the SAME values their cards already compute (GitHub/GitLab/Jira in the spec's own counts shape; Vercel/Status as summary+metadata; Headlines/Crypto/Home Assistant/Calendar/Habits as first-item facts), each RED-first after the existing no-data guards, with no new fetches, hooks, or writes (docked XOR free keeps one data owner). worldClocks/countdown/sun/moon are declare-only: their compact compositions already ARE dense lines, proven in the strip captures with a one-line geometry assertion. The catalog script gained `--batch=2` with the authoritative nine-connector fixture seed (scope-valid snapshots from the shared information-first module) and per-capture connector isolation; batch-1 regeneration stayed byte-stable with its owner verdicts intact. 51 batch-2 captures passed with zero failures/runtime errors/failed requests; key captures inspected individually (GitHub docked reads `2 PRs · 1 issue · 4 unread` with attention tint, Calendar `Release planning · in 4 h`, Home Assistant `Studio temperature 22.4°C`, worldClocks `London 1:03 PM · Tokyo 9:03 PM`). The focused gate passed 79 files / 967 tests with TypeScript clean and a 209-module production build. The bounded review returned Ready with zero Critical or Important findings and every gate independently reproduced (including a hook-safety audit of all ten docked early returns); its three cleanup minors are closed or ledgered: the script's dead isolation constant was deleted, the drift guard remains substring-based defense-in-depth behind the vitest set-pin (tighten to per-widget lines if drift ever bites), and the shared seed's task copy change means a batch-1 regeneration would re-capture the tasks PNG with new text (verdict data unaffected). Plan deviations accepted as improvements: the connector seed reuses the shared information-first fixtures module rather than duplicating harness code, and GitHub/GitLab/Jira dock facts follow the spec's counts exemplar rather than the plan's summary-string prescription. The owner reviewed batch 2 on 2026-08-18 and APPROVED it with five refinements, all applied the same day with focused RED/GREEN coverage: (1) the Full GitHub/GitLab cards now actually use their room — a wider 25rem card with a larger contribution graph (`ContributionGraph` gained `cell`/`gap` props; full renders 18px cells vs the 13px default; hash-verified visibly distinct from standard); (2) compact GitHub matches compact GitLab's shape (graph plus contributions and streak, gate parity); (3) Status dots gained visible service names beside each dot at standard (compact keeps dots-only with hover titles naming each service); (4) sun and moon docked forms dropped their padded compact cards for bare `DockLine`s at the shared 13px strip density (previously the padded card out-sized compact in the strip); (5) the compact Month tier is REMOVED ("takes up way too much space, just remove it") — `WidgetSizeContract.compact` became optional, Month declares `['standard']` only, and every compact-Month CSS rule was deleted with a retirement pin replacing the old contract tests. The owner's "Where are the bookmarks?" is answered in the catalog notes: Bookmarks are batch-1 (reviewed and approved there). All batch-2 verdicts are recorded as data in the generator, the catalog was regenerated clean (50 captures, zero failures/runtime errors/failed requests), and the focused gate passed 105 files / 1539 tests with TypeScript clean. At that checkpoint NL-P6 remained gated on owner acceptance of the refinements; that gate was later completed and accepted.
- **NL-P5 live-defect hotfix evidence (2026-08-18):** The owner exercised the installed extension and reported four real defects; all were root-caused before any fix (systematic-debugging discipline). (1) *Month could be docked and rendered garbage in the strip*: no code path consumed `supportsDocked` — the drop zone offered for every widget and the pure dock ops accept any id. Fixed at three layers: `useCanvasDrag` gained a `canDock` predicate (App feeds registry `supportsDocked`; a rejected widget is never offered a zone and its edge drop is an ordinary free placement), and the new pure `enforceDockEligibility(document, dockableIds)` in renderLayout.ts corrects any STORED invalid docked placement to the identity's free default slot at document-resolve time in App — so rendering, edit sessions, and the inspector all see one truthful placement (pure, identity-stable when clean, persisted only via the user's own Save); `planLayoutRender` carries the same optional gate as render-path defense. (2) *No visible way out of a dock* and (3) *grip/gear vanish on docked widgets*: CanvasSurface routed docked items to chrome `none` in normal mode on an over-narrow reading of spec 2.5 whose text ("hovering a widget fades in two small controls") never restricts to anchored items — docked members now get the same hover grip + gear (grip enters the session and drags straight out; gear opens the widget's Settings), with dock-local CSS keeping the buttons inside the slim row; only the mechanical narrow stack stays chrome-free. (4) *Compact Month persisted after the owner's extension refresh*: the batch-2 refinement gate verified a separate `.preview-nl-p5-dist` build while the extension's `dist` sat at a pre-refinement 8:04 AM build — a rebuild-before-owner-check violation, acknowledged; additionally MonthCalWidget still carried the week-view branch (reachable through the docked size fallback) and the inspector honestly exposed whatever sizes the running build declared. The week branch, compact-only short-label span, and its last CSS rule are deleted — Month renders the complete month at EVERY size prop. Six RED-first tests cover the planner gate, the document normalization (including document identity when clean), the drag-hook refusal + dockable control, docked hover chrome + narrow-stack absence, strip-free rendering of stored bad state, and the size-invariant month. The focused gate passed 105 files / 1546 tests with TypeScript clean (one unidentified single-test failure in the first full run did not reproduce across two subsequent full runs — recorded as a flake observation, identity lost to output truncation). The new six-stage real-Chromium witness (`scripts/preview-nl-p5-hotfix.mjs`) passed: docked Weather/Focus hover chrome inside the strip geometry, grip-drag undock with empty-strip disappearance and exact-Cancel restore, Month edge-drag refusing a zone with the dockable Clock control case, stored docked Month rendering free with legitimate members retained, Month inspector offering exactly [Standard], and a whole-run write log touching only seeded keys (never the frozen legacy `layout`). All five witness captures inspected individually. The NL-P4 witness's overflow seeds were updated to dock-eligible widgets so it stays truthful under the gate. `dist` was rebuilt from the fixed tree and the bundle grep-verified (standard-only Month contract present, week renderer absent, dock gate present). Standing correction adopted: every owner-facing check now ends with a `dist` rebuild from the exact reviewed tree.
- **NL-P5 live-defect hotfix 2 evidence (2026-08-18, same day):** The owner's second wave of live reports was root-caused to six defects, several sharing one cause. (1) *Stale rect map* — `CanvasItem` published geometry only from a ResizeObserver, which never fires for position-only moves: the next grab of a moved widget computed a garbage pointer offset (the reported "bouncing all over the place"), the inspector's overlap note compared positions widgets had long left (the reported "actively lying... isn't updating until you save and reopen"), and the inspector anchored to stale rects. Fixed with an item-keyed re-publish in CanvasItem (every placement change refreshes the rect; the RO effect keeps observation and null-cleanup ownership). (2) *Undock-on-first-move* — dragging a docked member popped it out of the strip immediately, making in-strip reordering ("you are just forcing them center") and re-docking ("I CANNOT GET IT BACK IN") feel broken; replaced with LIVE dock mechanics: `useCanvasDrag` passes the current band with every move, App docks/reorders live inside a band (`dockSelected`/`dockSelectedLive` with fresh insertion indices), undocks on leaving it (new `undockSelectedLive` keeps the whole gesture ONE undo entry, pure-tested), and the probe-measured band now includes the RENDERED fixed strip rects, not just the 56px surface threshold (the strip's upper half previously read as "outside the dock"). (3) *Unlabeled bulk tier control* — the toolbar's Compact/Standard/Full re-tier EVERYTHING; clicking them with Month selected visibly changed the Clock and read as targeting the wrong widget. The group now carries a visible "All widgets" label and the inspector rows gained "Size"/"Layer" labels. (4) *Toolbar covered the top dock* (the reported "can't edit the bookmarks... blocked by the layout editor"): with a top dock rendered, the session toolbar now offsets below the strip, witness-asserted non-overlapping. (5) *Grip clicks fell through to widget interiors* — witness stage 9 caught the session beginning on pointer-down, unmounting the hover chrome, and the released click landing on the bookmark link underneath (the page NAVIGATED); the grip press now swallows the one click it produces, scoped to clicks landing inside the grabbed widget, and `.canvas-item-chrome` moved from z-index 1 to 60 because Bookmarks' own z-20/z-50 bar beat the grip in hit-testing (the reported "no icon to edit the bookmarks" — the icons rendered but lost the pointer). (6) *Clock tiers* — standard and full rendered identically; the tier scales now derive from the shared `--clock-font` curve (compact 0.62x/14vh cap, full 1.3x/30vh cap), witness-measured strictly increasing (60/160/208px at 1600x900), with the two CSS pins updated. Docked Clock keeps its batch-1-approved `Time · date` line. The eleven-stage witness now also covers: in-strip live reorder without the item ever leaving the strip, weather out of the dock and back IN at standard tier in one continuous gesture with a pointer-proximity stability assertion on the second grab, an overlap note asserted against LIVE measured rects (two earlier witness iterations proved the note was truthfully reporting REAL clips of Month and Greeting), real chrome.bookmarks seeds for the top-dock chrome and toolbar clearance, clock tier font ordering, and the whole-run seeded-keys-only write log. Full gate 105 files / 1550 tests, TypeScript clean, `dist` rebuilt 2:59 PM from the fixed tree. Hotfix 3 (same day, owner-reported and witness-reproduced before any fix): the docked Bookmarks bar could not be grabbed at all — the legacy `.canvas-item[data-block-id="bookmarks"] { pointer-events: none }` wrapper rule (kept so the closed bar's allocation cannot intercept other controls) combined with inert session interiors left no hittable pixel; an `--editing` override restores the wrapper as the drag target, pinned in CSS-contract tests, and witness stage 9c now drags the real-bookmarks bar out of the top dock, asserts it follows the pointer, and re-docks it live (grab-target diagnostics recorded in evidence). Full gate 1551 tests green, `dist` rebuilt 3:45 PM, checkpoint `cbfbdc0`. Owner design direction (same day): the current edit INTERACTION stands; the proposed side-panel redesign is parked pending user feedback; the floating inspector was restyled in place (`3269135`) — named header with placement context, labelled Size/Layer rows, a joined segmented size control, quiet icon footer actions — same controls, roles, and accessible names (WidgetInspector/App tests unchanged and green). Alignment-guide leak fixed: guides now render only while a session lives (Escape mid-drag previously left accent hairlines painted on the normal page — the owner's "blue lines where borders are"; the in-drag hairlines at neighbor edges are the designed snap aid). `npm test` restored to green for the owner's own runs: `scripts/preview-information-first.test.mjs` is a `node:test` suite (6 contract checks pass under `node --test`) that vitest picked up by filename and failed with "No test suite found" — now excluded from vitest with the node-runner invocation documented; `scripts/adaptive-stage-probe.test.mjs` is a real vitest suite and stays included. Full `npm test`: 156 files / 2534 tests green; `dist` rebuilt 3:56 PM. Scripts-folder coverage question answered in-session: harness scripts are verified by execution against the real build (each run is the test), with pure contract surfaces covered by the two script test suites; unit coverage of the Playwright drivers themselves is deliberately not pursued. (The owner subsequently widened the vitest exclusion to all of `scripts/**` in their own edit.)
- **NL-P5 dock positioning (2026-08-18, owner-ordered spec refinement of 2.4):** "ALL OF THE THINGS ON THE DOCK CANNOT BE MOVED THEY ARE FORCED TO THE MIDDLE" — root cause was the DESIGN: the strip was a centered, content-hugging row where members could only be ordered, and the bar itself sat at screen center. Docked placements now carry `align: start | center | end` (additive schema — absent means center, so every existing document renders byte-identically; validation, backups, and the planner pass it through), the strips span the full width (72px side insets) with three sections laid out on a `1fr auto 1fr` grid, the drop pointer's third of the viewport picks the section, the insertion index orders within it, and `dockSelectedInternal` renumbers section-major so orders stay unique per dock and the narrow stack reads left-to-right. Dock lines were also restyled to the Tasks/Notes chip metrics (14px, matching padding, panel pill with border and shadow) per "MAKE IT THE SAME SIZE IN THE BOTTOM DOCK AS TASKS NOTES ETC" — note this changes batch-2 catalog dock-line captures on next regeneration (verdict data unaffected). New unit coverage: align validation passthrough, section-major reorder and re-sectioning, three-section strip rendering with stored aligns. Witness stage 7b drags Focus to the far left of the bar in a real gesture and asserts the start-section placement, the far-left rendered position, weather remaining centered, and probe-measured dock-line chip metrics; all 13 stages green. Full `npm test` 155 files / 2532 green (count reflects the owner's scripts exclusion), TypeScript clean, `dist` rebuilt, checkpoint `85b781c`. DEFERRED with owner visibility: "the bookmarks bar is not able to be made smaller" needs a docked-size control (docked placements store no tier by design); the honest fix is a small owner-gated design decision — which smaller form the docked bar takes (single-letter marks?) and how it is chosen from the inspector.
- **NL-P5 docked sizes (2026-08-18, owner-confirmed):** The owner confirmed the compact docked Bookmarks form is the one-letter mark bar, and re-reported the docked weather line as smaller than its chip neighbours with the unit letter dominant. Root causes: (1) the dock line's facts were sized by the FLUID canvas type roles (body ~14-16vw/vh-clamped, metadata 12px) rather than the chip metrics, and the weather unit letter stacked a second `text-[0.75em]` shrink on top of its metadata role — so the digits, not the letter, ended up the smallest glyphs; `.dock-line [data-canvas-type-role]` is now pinned to the chip scale (14px/20px, metadata 11px), the temperature digits carry `font-medium`, and the em-shrink is deleted. (2) There was no docked size at all: `DockedWidgetPlacement` now carries an optional `tier` (additive schema; validation, planner `dockTier` passthrough, and `dockSelectedInternal` renumbering all preserve it), `setSelectedTier` sizes docked members, the inspector shows the Size control for docked members with more than one declared size, and the shared `dockedRenderSize` helper resolves the render size — Bookmarks defaults to 'standard' (the spec 2.3 full-readable-bar exemption now RIDES ON the default size instead of fencing the mark rules out of the dock), everything else defaults to its compact composition. The compact-size mark rules (monogram shown, folder glyph hidden, sr-only labels, square chips) now apply in the dock too; the three-chip board limiter stays board-only. Witness stage 9d seeds a real bookmarks FOLDER plus links, sizes the docked bar compact, and proves the letter-mark bar in Chromium (monogram visible, labels clipped, bar 99px vs the 235px full bar); all 14 stages green. Full `npm test` 155 files / 2537 green, TypeScript clean, `dist` rebuilt, checkpoint `8df4439`. Next per the owner: they set up connector API keys to exercise the connector widgets live.
- **NL-P5 hotfix 4 (2026-08-18, third weather-line report):** The owner correctly rejected the fix again. TWO stacked root causes survived: (a) the dock-line typography pin lost the CSS cascade — `.aurora-canvas [data-canvas-type-role="body"]` has equal specificity and sits later in the file — and (b) the deeper one: every compact-weather CHIP design rule keys on `data-canvas-size="compact"` WITHOUT a dock guard, and the docked wrapper carries that attribute, so the chip interior rules restyled the dock line the whole time (the 10px collapsed-callout rule hit the temperature digits — measured, not guessed). Verification failure acknowledged: the previous witness probe measured a SYNTHETIC span without the type-role attributes and passed while the real line was wrong. Fixes: all 19 compact-weather chip selectors are dock-guarded (`:not([data-canvas-mode="docked"])`, 8 test pins updated in lockstep), the typography pin carries the `.aurora-canvas` ancestor with a test asserting the winning specificity (not just presence), and the dead docked Size radios are gated to widgets whose strip form actually varies (`dockSizeVaries`: Bookmarks only) so no docked widget offers do-nothing controls. Witness stage 8b now seeds the catalog's exact weather cache recipe and measures the REAL rendered line: `89°F · Dallas · Clear` with digits 14px/weight-500, unit letter 11px, and line height EQUAL to the measured Tasks chip (38px vs 38px); 15 stages green. Owner's revert demand verified against git: the free compact/standard/full weather forms were never modified by any of these commits (the diffs touch only the docked branch), so there is nothing to revert — recorded here as the factual answer. Full `npm test` 155 files / 2538 green, TypeScript clean, `dist` rebuilt, checkpoint `b14d3f1`.
- **NL-P5 hotfix 5 (2026-08-18, widget color tinting the photo):** The owner picked red widgets and the whole background image went red — the full-canvas legibility wash (`.canvas-legibility-layer`) built its four gradients from `var(--panel-solid)`, the exact token `applyPanelColor` re-tints; with the default near-black panel the coupling was invisible. New fixed `--canvas-wash` token (themes.css, same neutral value) owns the wash and the engine deliberately never touches it. Verified honestly: CSS pins in CanvasSurface.test (wash uses --canvas-wash, NOT --panel-solid) and CanvasLegibilityLayer.test updated in lockstep, and witness stage 8c sets `panelColor: '#dc2626'` in real Chromium, asserts the computed wash gradients contain NO red channel while a dock pill DOES render red, and captures the proof (red Month/search/chips over the unstained aurora); 16 stages green. Also answered the owner's coupling worry in-session: panel text derives contrast from the pick's luminance (light panels get dark ink automatically) and photo text (--canvas-fg) is fixed and untouched by the engine, so white panels cannot produce white-on-white. Process slip recorded: the fix commit `8e44e1d` was pushed with the stale CanvasLegibilityLayer pin still red; corrected in the immediate follow-up `28be725` (full `npm test` 155 files / 2539 green). `dist` rebuilt.
- **NL-P5 hotfix 6 (2026-08-18, hover chrome over content):** The owner's screenshot showed the docked grip + gear COVERING the Tasks pill's own label — the docked chrome sat inside the pill (the earlier clip workaround) and short chips have no room. The chrome now floats above the pill's top-right corner like anchored items, with the scroller reserving 16px of INVISIBLE headroom (the strip has no background) so the overhang isn't clipped; the overflow nubs re-centered on the pills. The companion report — quote hover icons appearing "on the top left side" — traces to wide transparent boxes (the quote figure carries 64px side padding, and neighbouring boxes overlap): the icons belonged to a box the owner could not see. Normal-mode hover now paints a whisper 1px hairline around the OWNING widget box whenever the grip + gear fade in (`:has` keyed on the chrome, never in a session, never a selection ring), so the controls are always attributable. Canvas/legibility suites and the full 16-stage witness green (chrome position visually confirmed above the docked weather chip); full `npm test` 155 files / 2539, TypeScript clean, `dist` rebuilt, checkpoint `452fc46`.
- **NL-P5 free-x docks + weather rain fill (2026-08-18, owner-refined spec 2.4 a second time):** "I want complete control over placing items in those docks... far left, or 10 pixels to the right of that... just like the regular screen" — the section model was still rationing placement. `DockedWidgetPlacement` now stores an exact `x` (percent of the strip width, member center; additive — legacy `align` reads through `DOCK_ALIGN_X`, no-x documents render centered, `align` is never written again); the drop pointer IS the position, clamped in App only so the member's own measured box stays inside the bar (witness-proved: a 290px focus line dropped at pixel 140 hugs the left edge, the canvas safe-margin law); orders derive from position (`dockSelectedInternal` renumbers both edges left-to-right by x; the narrow stack reads left-to-right). The strips render as ONE free grid-stack lane (all members share the cell, offset by their own percent margins, tallest member sizes the lane); the ordered-row scroller, wheel/keyboard scroll, overflow fades, and hover nubs are RETIRED with `useDockOverflow` + its suite deleted and retirement pins replacing the scroller CSS contracts — this also closes the owner-reported top-dock defect (the section tracks squeezed the weather pill into truncation and surfaced a stray scroll nub). The NL-P4 witness's overflow stage was rewritten to the dense free-lane contract (ten members at their own positions, no scroll machinery, no page overflow, left-to-right DOM order) and its seeds gained explicit x values; both witnesses green (17 + 6 stages). The weather details panel gained a Rain outlook cell (peak hourly precipitation probability with its hour, "None expected" under 10%) from ALREADY-fetched data — no request-identity change — closing the owner's whitespace note. Full `npm test` 154 files / 2536 green (file count reflects the deleted overflow suite), TypeScript clean, `dist` rebuilt, checkpoint `724a014`. Two honest notes: the commit was pushed while one full-run test failure was unverified — `RssWidget > drops feed A immediately while a preserved mount waits for feed B` — which then passed in isolation AND a full rerun: a now-IDENTIFIED flaky test (almost certainly the earlier unidentified one-off), spawned as its own follow-up task rather than papered over. Owner asks deferred to a scoped packet: the crypto widget re-design (inline text row like the quote, PICKLIST symbol selection replacing comma-typing) plus a NEW stocks section — the stocks half needs an owner decision on the market-data provider and its key model before any build.
- **NL-P5 crypto cleanup (2026-08-18, owner: "keep the crypto for now... clean up the widget and widget settings"; stocks explicitly parked):** The settings body is a PICKLIST — a new static curated catalog (`cryptoCatalog.ts`, 24 well-known CoinGecko ids, no request/no key) renders as a checkbox grid where SELECTION order is display order, the 5-coin cap disables further picks (checked boxes stay enabled so the cap is always reversible), any saved id outside the catalog renders as its own removable "(custom)" row (comma-era configs and hand-edited backups never lose coins), and a single-id "Other CoinGecko id" add field replaces comma typing entirely; the same origin-transaction save/clear/rollback discipline is unchanged and its seven flow tests were rewritten to checkbox interactions plus two new ones (cap-disable, custom-row round-trip). The widget: the owner's "stacked on top of each other" was CSS archaeology — the legacy narrow-track era stacked the board strip into a COLUMN (`flex-direction: column`) with per-cell symbol/price/change grids and cell-hiding condense rules (compact showed 1, standard only 2); all of it is retired with regression pins, the strip is content-tight (`w-max`, the old fixed 22rem box would have overflowed five cells), and every selected coin renders on ONE baseline row at every free size (compact keeps its 1-coin contract via the existing JS slice, the single owner now). Batch-2 catalog regenerated (crypto captures now show the one-line strip; verdict data intact). Also answered in-chat: the briefcase button is the Canvas-P4 Utility Tray trigger (Home Assistant actions + background Refresh; with no HA configured it holds only Refresh) — offered the owner removal or hide-when-trivial as a follow-up decision. Full `npm test` 154 files / 2534 green, TypeScript clean, `dist` rebuilt, checkpoint `fa5ae79`. Honest notes: the geometry-freshness unit test and the two clock CSS pins were written/updated alongside their changes rather than observed RED first (the witness iterations provided the observed-failing evidence instead); and the docked location-less Weather renders its tall setup chip in the strip (~97px, probe-measured), which violates the slim-band intent for that one no-data state — ledgered for the NL-P6 sweep rather than patched blind here.
- **NL-P2 evidence:** The Canvas now renders the resolved named layout with anchor+offset percent geometry and content-tight boxes: `planLayoutRender` (pure) produces anchored, stacked, or docked items; `CanvasItem` imposes no width or height and emits `canvas-item`/`data-canvas-size`/`data-canvas-mode` only; below 600 CSS px one mechanical narrow-floor stack renders docks first then free widgets in layer order. The width rule survives solely as `migrationSourceProfile`, the frozen interpreter of pre-named-layouts storage inside the My-layout derivation. A STATIC per-identity default table (`DEFAULT_WIDGET_POINTS`, the V1 Desktop composition as literals) replaces the deleted derived defaults, with identity-stable layers so toggles never renumber neighbours. Deleted after reference proof with live-source and CSS retirement regressions: automatic profile selection, `canvasDefaults` slot catalogs and spread/edge guessing, the `SMALL_CANVAS_COORDINATE_HEIGHT` plane, `ITEM_BOXES`/`BASE_BOXES`, the Arrange artboard/controller/draft/profileEditor, `SignalDockEntry`, the `board-item`/`data-stage-variant` emission, the `:focus-within` container ring, and ~28KB of stage-zone/adaptive-stage/signal-dock/launcher-shelf CSS (brace-aware rebuild from HEAD after a regex sweep proved unsafe). `canvasSnap` and `useLongPress` survive for NL-P3. Dead stage-var control minimums were materialized as the documented literal 36px floors (previously inert; a deliberate defect fix), and dead stage-profile-gated tool-sheet/Briefing-reveal/day-context rules were excised to match live behavior with intent recorded for NL-P3/NL-P5. Settings Layout keeps only the legacy V1/V2 Reset and Restore recovery actions. The focused packet gate passed 27 files / 798 tests with TypeScript, diff hygiene, and a 197-module production build clean. The real-Chromium witness (production preview build, four storage shapes x five displays including exact 1408x445 and the 599/600 floor boundary) passed 20 captures with zero failures, runtime errors, or failed requests, and caught one real defect fixed under it: `container-type: inline-size` on the content-tight wrapper zeroed intrinsic widths and collapsed widgets to slivers. All key captures were inspected individually: the short-window and roomy desktop renders are information-first and useful; recorded tensions for NL-P3/NL-P5/NL-P6 are static-default overlaps in the left personal column at extreme short heights, user-placed-clock vs default-slotted-greeting collisions when mixing stored and default placements, and the Briefing rendering only its compact sentence. A PowerShell ANSI round-trip mojibake incident in two test files was repaired byte-exactly and the repair verified by the reviewer's own scan. The bounded review returned Ready-with-fixes (0 Critical, 2 Important: orphaned emergency-fit container queries and a circular container-relative clock glyph cap surviving their deleted size container, plus witness scratch dirs missing from .gitignore); the single fix/rereview cycle closed both Importants and three minors in `b72d0bc` (container queries and container units deleted with retirement pins, compact clock keeps its real 17vh vertical cap, board-item scan escape hatch closed, relative-vs-absolute layer semantics documented for NL-P3's save path), the witness re-ran green once post-fix, and the rereview returned Ready with independent re-verification. Remaining Minor debt: stale pre-existing machinery prose around index.css:920-995.
- **NL-P1 evidence:** Storage schema advanced additively to v13 with the new top-level `layouts` document key defaulting to `null`; `migrations[12]` is the identity and live initialization stamps only `aurora:version` for stored v11/v12 (generalized `METADATA_ONLY_FLOOR` path with verified rollback), so no data key is rewritten at boot, ever. The validated layouts document model (nine-anchor free placements with exact point round-trip, docked placements, per-layout bulk tier, strict reject/drop cleaning that always drops unknown widget ids) lives in `src/lib/layout/namedLayouts.ts`; `myLayoutAdapter.ts` derives the in-memory "My layout" from any stored V1/V2/V3 state plus enabled widgets without mutating its input, and `resolveLayoutsDocument` prefers a valid stored document. Pure switcher operations (switch, create, duplicate, rename, delete with last-layout protection and active-pointer survival, reorder) validate every result, share one trim-and-reject name policy, and feed `saveLayoutsDocument` — the single write path, proven by write-spy to touch exactly the `layouts` key and never the legacy `layout` recovery input. Backups round-trip the document with full validation, import pre-v13 backups with `layouts: null`, and reject malformed documents with the established reason format. The exact focused gate passed 18 files / 513 tests with TypeScript and diff hygiene clean and zero `App.test.tsx` expectation changes (the no-presentation-change witness). The bounded review returned Ready with no Critical finding; its one Important finding (a guard binding the metadata-only floor to identity migrations) plus two minors were fixed in `1441704` under the single fix/rereview cycle. Noted Minor debt: extra properties on an otherwise valid placement survive cleaning (matches the established canvasTypes convention) and `resolveLayoutsDocument` validates twice (trivial).
- **PR-P1 evidence:** Stable identity-owned source slots now preserve exact x, y, size, and layer through widget and connector toggles without layout writes. Runtime viewport ownership publishes only width, height, and Canvas profile. The closed Settings drawer is inert, invisible, non-hit-testable, and fully beyond its inset. Weather details are body-portaled, 8px-clamped, internally scrollable, resize-aware, directionally anchored at every corner, and geometry-neutral to the Canvas. The exact final gate passed 7 files / 103 tests, TypeScript, and diff hygiene. The focused built-extension probe passed four representative displays and eight corner activations with no horizontal overflow, runtime error, or failed request. One Important missing resize witness was closed by mutation-proven focused coverage; no Critical or Important finding remains. Minor stale Weather elevation commentary is assigned to PR-P5 cleanup.
- **PR-P2 evidence:** Canvas presentation now projects legacy-compatible Standard/Large text without writing storage or changing geometry, exposes shared role typography through explicit Canvas attributes, and uses one pointer-transparent token-based four-wash legibility layer without passive cards. Weather Compact, Standard, and Full now add literal useful rows without blank padding; Bookmark identity marks are mutually exclusive; Search clears fixed controls and its focus outline; `Timer completion sound`, `Daily summary`, and `Text size` replace the unclear prior labels. The exact focused gate passed 12 files / 408 tests plus the 1 file / 3 retired-cascade regression, TypeScript, and diff hygiene. Twelve original Chromium captures across four displays and three background families were inspected individually with recorded type floors, contrast, Search clearance, Weather occupancy, no horizontal overflow, and zero runtime errors or failed requests. The bounded review returned Ready with no Critical or Important defect open. Existing Settings hydration warnings and stale retired Weather commentary remain Minor.
- **PR-P3 evidence:** Settings now uses one full-viewport narrow workspace or an inset 54rem roomy document with a 9rem navigation rail and 38rem content measure. Widgets are grouped as Core, Personal, and Time & sky, while Weather location, World clocks, Countdowns, and Habits editors remain storage-owner compatible inside session-only labelled disclosures. Layout guidance explains the four independent profiles without density language. The exact focused gate passed 6 files / 311 tests, TypeScript, and diff hygiene. Seven original Chromium captures across 320x568 through 3840x2160 were inspected individually; the probe covered every tab, real keyboard disclosure activation, local scrolling, Escape, focus restoration, exact geometry, closed containment, and overflow with zero runtime errors or failed requests. The bounded review's two evidence gaps were closed in its one fix/rereview cycle. Existing Settings hydration warnings and stale retired Weather commentary remain Minor.
- **PR-P4 evidence:** Connector Settings now shares the bounded Drawer surface without the double-composited black header slab. All nine registry identities derive truthful Not set up, Hidden, On canvas, or Reconnect required summaries; only configured cards expose Show on Canvas, hiding preserves the complete config, reconnect fields remain immediate, and one parent-owned Setup/Edit/Reconnect body is active at a time with exact focus restoration. The concise visible privacy statement retains the complete local-plaintext, shared-profile, capability URL, backup, disconnect, and data-clear guidance in a keyboard-operable disclosure. The exact present-file gate passed 7 files / 353 tests, TypeScript, and diff hygiene; a permission/reconnect/disconnect review slice added 3 files / 57 tests. Five original Chromium captures from 375x812 through 3840x2160 were inspected individually after all nine identities, setup/cancel, edit/close, reconnect, exact visibility round-trip, privacy, sticky scrolling, and containment passed at each viewport with no live external request, runtime error, failed request, nested scroll owner, or horizontal overflow. The bounded review returned Ready with no Critical or Important defect. Existing Settings hydration warnings and legacy connector presentation helpers/comments remain Minor and are assigned to PR-P5's reference-proof cleanup.
- **PR-P5 evidence:** Arrange modality now follows physical width at the exact 1099/1100 boundary while Small, Desktop, Large, and Wide render inside exact 390x844, 1440x900, 2560x1440, and 3440x1440 logical artboards. Uniform Arrange-only scaling maps pointer coordinates back to logical space without transforming the production Canvas root; the 328px side inspector or dismissible 50dvh sheet leaves the selected Canvas visible. The listed unconsumed BoardItem, LauncherShelf, Dock calibration, and superseded viewport-hook files were deleted after reference proof, while pure V2/recovery compatibility code remains. New Small source defaults use an additive validated coordinate plane that is adopted only by new defaults or explicit Save, so untouched custom layouts retain their previous percentages and exact recovery. The expanded focused gate passed 11 files / 188 tests, TypeScript, and diff hygiene. All four profiles and the full Arrange interaction set passed in real Chromium at six displays from 375x812 through 3840x2160 with zero runtime errors, failed requests, external requests, or horizontal overflow; six originals were inspected individually. One Important backward-compatibility review finding was fixed and rereviewed Ready. Existing React `act()` warnings remain Minor test noise.
- **PR-P6 evidence:** The exact 23 CSS viewports and five required states passed in real Chromium at device scale factor 1. All 115 original PNGs, 24 connector-size captures, four connector-state captures, and four Weather-corner captures were inspected individually; six deeper fenceposts exercised keyboard navigation, focus return, direct panels, connector editors, long press, pointer capture, guides, drag, keyboard movement, layers, profiles, exact Cancel, and explicit Save. Focused RED/GREEN work corrected bottom-bar hit geometry, the 4K Full Clock cap, Weather outside-close focus timing, and insufficient larger-size fixture rows. The final focused gate passed 4 files / 92 tests, the harness contract passed 6 tests, TypeScript and diff hygiene passed, and the canonical evidence contains zero runtime errors, failed requests, or unexpected external requests. The bounded review returned Ready with no Critical or Important defect open. Minor observations are limited to truthful Arrange collision outlines, bounded fixture greeting truncation at 2560x1080, intentional overlay coverage while Weather is open, and existing Settings `act()` warning noise. The eight original owner captures are ready; PR-P7 and all Store work remain blocked pending explicit visual approval.
- **Prior Canvas acceptance (historical):** Restore Aurora's V1 composition and direct free placement while retaining Aurora 2's security, privacy, storage, migration, connector, accessibility, and recovery foundation. Every visible widget must be movable; Bookmarks return to the top; tools return to direct launchers; Focus stays centered; widget sizes must be materially distinct; rich connector content and complete calendars must be restored.
- **Written design:** The owner approved `docs/superpowers/specs/2026-08-16-aurora-v1-canvas-adaptive-safety-rails-design.md` on 2026-08-16. Internal profile keys remain compatible while the user-facing canvas uses Small, Desktop, Large, and Wide layouts with direct drag, safety rails, and non-occluding editing.
- **Executable plan:** `docs/superpowers/plans/2026-08-16-aurora-v1-canvas-adaptive-safety-rails.md` defines eight bounded packets, two owner visual gates, one review/fix cycle per packet, checkpoint pushes, and the single post-approval final gate.
- **Rejected candidate:** Calm Canvas commits through `3c5015a` remain recoverable historical evidence but are not visually accepted, release-ready, or a foundation to continue polishing. Their full gates were correctly withheld.
- **Canvas-P1 evidence:** Schema v12 and the additive V1/V2/V3 union are implemented without eager layout rewrite. V1/V2 recovery is exact, V3 Save is explicit, active block corruption falls back per block, backup validation remains strict, and legacy Settings/Arrange paths cannot destroy V3. The final focused gate passed 9 files / 609 tests, TypeScript, and diff hygiene. One review/fix/rereview cycle closed all blocking findings; repeated Settings test `act(...)` warnings remain Minor harness noise.
- **Canvas-P2 evidence:** One normalized Canvas now renders V1, V2, and V3 profiles with source-owned V1 anchors, Small/Desktop/Large/Wide selection, safe fitted geometry, optional Bottom bar ownership, preserved compact presentations, and no semantic Day/Now/Work Pulse/Signal Dock presentation regions. Custom saved blocks remain exact while newly enabled identities find a safe snapped position. `Use Desktop layout everywhere` is an in-memory fitted preview with no write. The final focused gate passed 8 files / 71 tests, TypeScript, and diff hygiene; the bounded rereview returned Ready with no findings open.
- **Canvas-P3 evidence:** Direct Arrange now owns one explicit-save draft with measured Canvas-local pointer geometry, grid and magnetic guides, 8px-bounded keyboard and size changes, collision/layer feedback, undo/reset, four real profile previews, draft visibility, a non-occluding inspector/Small sheet, exact Cancel, one authority-backed Save, real-invoker focus restoration, and durable terminal announcements. The final focused gate passed 6 files / 79 tests, TypeScript, and diff hygiene. The one review/fix/rereview cycle closed five Important findings; the rereview's remaining restored-size branch was reproduced and fixed under the exhausted cycle. No Minor issue was reported.
- **Canvas-P4 evidence:** The V1 hierarchy now keeps Bookmarks at the top, Clock and one stable Focus footprint centered, movable Timer/Tasks/Notes launchers opening direct contained panels, and Briefing absent unless explicitly enabled with real signals. The optional Utility Tray exposes only working Home Assistant and Refresh commands. The exact focused gate passed 7 files / 332 tests, TypeScript, and diff hygiene. The focused real-Chromium probe exercised direct panels, pointer drag, guides, keyboard movement, selection, inspector, exact Cancel, Small sheet containment, overflow, focus restoration, and Timer continuation with no runtime errors or failed requests. One review/fix/rereview cycle closed the blank Utility Tray regression. Minor observations remain for the exercised Clock overlap warning and existing Settings test `act(...)` hydration warnings.
- **Canvas-P5 evidence:** The owner approved the Canvas-P4 direction and refined compact Weather, bookmark marks, Small Search clearance, and connector-size expectations. Each registry identity now declares only materially distinct sizes with explicit content contracts. Canvas size reaches GitHub, GitLab, Jira, Vercel, Status, RSS, Crypto, and Home Assistant without changing connector settings; Compact preserves truthful primary state, Standard preserves selected rows or visuals, and Full preserves complete selected compositions. Inspector conflicts are derived from actual selected sections, including conditional Jira and RSS requirements. Compact Weather retains icon, temperature, condition/location, and chevron; named folders gain one- or two-character marks; Small Search reserves utility-control clearance. The final focused gate passed 15 files / 317 tests, TypeScript, and diff hygiene. One review/fix/rereview cycle closed three Important issues; its two residual Important branches were closed with focused evidence under the exhausted cycle and no second rereview. Minor: a Compact Canvas folder may paint both glyph and monogram when viewport and Canvas size disagree. Exact Small clearance remains a Packet 7 real-browser witness.
- **Canvas-P6 evidence:** Month now offers only Compact and Standard. Its default and Today Compact state is one complete Sunday-to-Saturday week, navigation remains coherent with complete seven-day rows, and Standard renders the natural complete four-, five-, or six-row month without the rejected CSS fragment. ICS feeds have optional identity-owned colors, exact legacy Auto ordering, one closed palette, matching Settings and event dots, and accessible named controls. Color-only edits preserve URLs, permissions, cached events, omitted optional fields, and in-flight refresh ownership. The final packet gate passed 7 files / 445 tests, the focused snapshot gate passed 3 files / 76 tests, TypeScript and diff hygiene passed, and the single review/fix/rereview cycle plus exact residual legacy-default closure left no Critical or Important issue open. Existing Settings test `act(...)` warnings remain Minor harness noise; visual proof is assigned to Packet 7.
- **Canvas-P7 evidence:** The focused real-Chromium gate produced three separate DSF-1 owner captures at 1600x900, 2560x1440, and 3440x1440. Desktop shows top Bookmarks plus rich GitHub/Jira content; independent Large and Wide profiles show all nine connector families, visibly named multi-calendar sources, and a complete Month. Direct evidence covers all visible Arrange identities, long press, active pointer capture, guides, keyboard movement, overlap/layers, Undo/Cancel/Save, profile previews, nested local scrolling, cursor semantics, Small containment/clearance, Focus lifecycle, and cache-neutral persisted ICS color changes. The Calendar attribution and incomplete-evidence review findings were closed in the single fix/rereview cycle; rereview returned Ready. The owner then correctly rejected the submitted Desktop capture because Compact Month dates were vertically stacked. `01f5076` removed the Compact table-cell layout override and added an exact Chromium row-geometry regression. The regenerated Desktop capture now shows `16 17 18 19 20 21 22` left to right, Large/Wide remain complete, and all three corrected captures have zero intersections, clipped blocks, missing images, runtime errors, and failed requests. The compact folder glyph/monogram duplication remains Minor, and the documented native/live-service ceilings remain manual.
- **Canvas-P8 evidence:** The owner approved the corrected Packet 7 captures. The one full unit run exposed only an obsolete source-text assertion for the retired Day presentation; the affected behavior family passed 2 files / 29 tests after its removal and the full suite was not repeated. Production and preview builds each transformed 193 modules, and the production bridge scan was clean. The canonical harness run and its one permitted rerun both stopped in the same stale Utility Tray/direct Timer harness family; the rerun passed its first six product assertions before a leftover `timerDialog` reference stopped traversal. The corrected W1-P7-only Chromium family then passed all seven reported checks, including exact restoration and a real advancing clock. No third canonical run occurred. Final original-resolution inspection reconfirmed the accepted captures and `errors: []`; one final review returned Ready with no Critical or Important blocker. The incomplete later canonical traversal is Minor procedural evidence debt under the explicit no-third-run limit.
- **W6-P3 evidence:** Current official Chrome Web Store policy is mapped to Aurora's executable data-flow inventory. Canonical privacy/listing/Data Usage source was reconciled after two Important disclosure fixes and a Ready rereview. The signed-in live dashboard was then transcribed read-only: publisher `jcooler`, Aurora item `akjalbmacojpmebkgohhcaaiacicpgkh`, live/draft version `1.2.1`, status `Published - public`, five V1 screenshots, and five live 2.0 Data Usage category changes. The privacy URL, remote-code answer, and all three current certifications match. Exact differences are assigned to W6-P4 preparation or W6-P5 action; no dashboard field, package, draft, submission, publication, or rollout changed.
- **W6-P4 evidence:** Version 2.0.0 is synchronized. `release/aurora-2.0.0.zip` was built once from clean source commit `6fae415`, is 60,443,024 bytes with 59 entries and SHA-256 `31930FEC4A288992EC74FD3173F08FE444D132A00FA7EF51C0900B629F356116`, and passed production manifest/root/leakage guards. Five current 1280x800 Store screenshots passed the real-extension capture gate with zero console/page/request errors and were inspected separately. Listing, release/reviewer notes, checklist, exact field map, and dossier are staged. One Important routable Jira fixture namespace was changed to `.invalid` in `f6d0b6f`; regenerated evidence and the single rereview are Ready with no Critical/Important issue. The live Store remains unchanged.
- **Remaining gate:** PR-P6, PR-P7, and a new action-specific W6-P5 approval all remain. Stop before upload, field typing/save, submission, publication, distribution, or rollout.

## Previous packet envelope

- **Acceptance:** One selected Tray tool exposes Tasks, Notes, Timer, Home Assistant actions, or background refresh. A running timer remains represented after detail closure and the tools remain coherent in the accepted responsive shell.
- **Implemented files:** Added controlled tool navigation/host and existing-owner portals for all five written families. Notes save guards now cover close, switching, Settings, Arrange, and external disable. No registry, schema, planner, storage, migration, privacy, permission, connector identity, or Store contract changed.
- **Test scope:** Focused development passed 9 files / 133 tests plus the protected-disable regression and TypeScript. The focused built-extension replay passed representative Tasks/Notes/Timer/refresh operations, single-detail selection, timer survival, responsive modality, focus restoration, cleanup, and zero runtime errors. One review found one Important Notes guard edge; the fix/rereview returned Ready. Final unit evidence passed 137 files / 2,254 tests; production/preview builds transformed 189 modules; bridge scan was clean.
- **Visual scope:** Compact 800x600 and Standard 1600x900 working-tool captures were inspected once. The full harness and its one rerun were exhausted by stale standalone-tool selectors; the remaining implicit-region selector was source-corrected without a third run and is Minor evidence debt only.

## Last completed commits

- `b895207` - Expansion Platform review fixes for trust-root safety, independent origins, exact migration/contract checks, and painted-content usefulness.
- `ee0cdff` - guarded 76-capture scratch Chromium proof for the Expansion Platform.
- `4a9f787` - one executable Expansion Platform contract command.
- `43ff333` - truthful logical Arrange artboards, physical viewport modality, scale-correct editing, legacy-safe Small coordinate persistence, and reference-proven obsolete presentation deletion.
- `7e16c12` - converged Connector workspace, truthful all-nine state model, one active editor, exact configured visibility, complete privacy disclosure, and five-display Chromium proof.
- `4b84fb7` - bounded Settings workspace, compact navigation, grouped Widgets, session-only editor disclosures, and concise profile guidance.
- `077ab0d` - information-first role typography, legibility, meaningful Weather sizes, Bookmark identity, Search safe zone, and truthful setting labels.
- `8081f9d` - withdrawn release candidate and approved information-first design checkpoint.
- Plan checkpoint subject: `docs: approve information-first implementation plan`.
- `2e194bc` - synchronized Aurora 2.0.0 version metadata.
- `6fae415` - current five-image Store capture source and real-interaction harness.
- `c564687` - audited 2.0 release dossier, notes, and checklist.
- `f6d0b6f` - review fix moving the Jira capture fixture to the non-routable `.invalid` namespace.
- `19771f0` - current-policy/source reconciliation and bounded disclosure review fixes; W6-P3 checkpoint subject is `docs: checkpoint W6-P3`.
- `01f5076` - owner-reported Compact Month row-geometry correction and real Chromium regression witness.
- `cb58bd2db35557e884008e683d64687548e1d146` and `73b279431fb868f5d31ec628a5d8c2b6579052b2` - integrated Canvas Chromium evidence, visible calendar-source attribution, and the bounded review fix/rereview closure.
- `c774b57` - Packet 8 stale semantic/source assertions and direct-panel canonical harness alignment.
- Final ledger checkpoint subject: `docs: checkpoint V1 Canvas recovery`.
- `45bcf45a4a30f8d05f78abf02ac4cb913d54c64a`, `8ce4fb824e78e6abac29b8422321b0fc3d32b926`, and `f6c35af` - complete Month sizes, identity-owned ICS colors, cache-neutral refresh ownership, and backward-compatible color saves.
- `10ff5334567664965eae7518c9c34f88b2c9ce35`, `484cd633958f3a739affd8c7617ef7cf3b152239`, and `121da8d8658c55d6898f70173ad705b97f8cefa7` - truthful Canvas size contracts, review fixes, and configuration-aware residual closure.
- `6403b26198dd3f94742316850ae1a1564c1492ee` and `045ed09` - restored V1 interactions and the bounded Utility Tray rereview fix.
- `5aa01e532b3e286195407d2a8cbd6dfb0b7f80b8`, `96482b8495c5da50206cd39a4492d68a5eb09558`, and `8996b15a3f2084d21b41f4dab9db10c7b00e8f63` - direct Canvas editing, bounded review fixes, and the restored-size safe-margin regression.
- `eb1354b6a5b041fb6d494655c3dae1862572bc51` — audited V1.14.0 base (`test(ha): the drawer sits for its portrait`).
- `0ae676905fd54d3acf3aaf2cdcb79d13e4b47f7e` — reviewed master specification and executable W1-P1 plan (`docs: establish Aurora 2 observatory program`).
- `87a8aa93dfd5544322daf855d10e780f64d1e882` — isolated Wave 0 ledger checkpoint (`docs: checkpoint A2-W0`).
- `b2256f2a13f00e0e987593fdd9b83b839f401394` — scoped connector snapshot identity (`feat(connectors): scope snapshot identity`).
- `0e93944116281cf95fe6b444398f7aa30730353d` — generation-safe lifecycle, nine call sites, and reconnect epochs (`fix(connectors): refresh scoped snapshots safely`).
- `cd511f06c3758ce9b237f6a493087376e34886f8` — bounded-review fix for queued stale-owner writes (`fix(connectors): guard queued snapshot writes`).
- `aa7648817aa3c076e3e960bcd0ed6bff8d6f14b0` — independently reviewed executable W1-P2 plan (`docs: plan W1-P2 cross-context storage authority`).
- `8efbb0a3b9be4787dff1924da128321901c28217` — Web Lock mutation authority, atomic restore, and two-page harness proof (`fix(storage): serialize cross-context mutations`).
- `fac883ba4cccf6f567a8460188a84902d219730c` — bounded-review fixes for explicit harness fixtures and timing-free concurrency tests (`fix(storage): address W1-P2 review`).
- `2a7abdf2b6db6f70e0a383453a79bbf852bdb977` - independently reviewed executable W1-P3 plan (`docs: plan W1-P3 optional permission transactions`).
- `64af77ecf4caf3606e889f632d48951eb5e0c854` and `49819c477254cfa0d259b4990bde9ba6a28a62e4` - permission mirror, lifecycle transactions, owner registry, and Task 1 review fixes.
- `e9dce0c3847e981c793c591e95eb179b219077c6` and `03f56b3af31d09560ddc1ce5cdf5839f279c6ea8` - token connector transactions, durable cleanup UI, and restored form regressions.
- `9a6f0c7200444034cba4d724a9fef2162e62fec2`, `8e8867023a5b45411d02df96accbd0ec78678215`, and `ccf478126126fe87b22ca1a8db9e71e400602aa8` - RSS/Crypto/ICS/Status ownership, review fixes, and fresh retry ownership proof.
- `c054a66f1e04d1742f8ca9b9ff37ae341e25295d` and `a576921f7d6e08462d052bade8d3ec760020ed5c` - APOD two-origin transactions and authoritative stale-render exit fix.
- `79f6d37f334e20440e4357f6e269c1810450c774` and `75efc1160fcd3e58bc5f17656960bbbb8536b4e1` - preview-only extension transaction matrix and strengthened gesture/pre-existing proof.
- `a8d62367db97ba42b2b70047b7d0414df6019a90` - bounded whole-packet review fixes (`fix(permissions): address W1-P3 review`); verified W1-P3 implementation head.
- `206e1b8bbc18d306ec3089496eb32456109d4e56` - independently reviewed executable W1-P4 plan (`docs: plan W1-P4 atomic backup restore`).
- `110b483b78d08a148036661b55b9729973d91722` and `3234796608bdece5196650679ecdc8a3a6d9fdf4` - descriptor-driven secret/capability redaction, prepared import, and legacy Calendar review fix.
- `6c561a8b32ea532242d43c9b6a8b25da3011627e` and `1bf2f2e0165b8a92408e1064609f882a33e74f10` - authority-held snapshots, verified rollback-capable all-key replace, and hardened contention proof.
- `0dcdbdca32eb2f5bbb670411905da7a8bf2e246c` and `5d57dcab9da45292e22312b3f933c79d3da293ec` - gesture-safe restore coordinator, accessible Data recovery, and failed-restore cleanup coverage.
- `5daa5ac4b3f7c9cc038715df1ca8fd5a31f0dd24`, `768f1c47d1037ca2fc67d9e0adf5ac1b9b72bce1`, and `6f30b915719586cb519aa0f8f2b4418b0724debe` - real-extension restore proof, exact predicates/failure-isolated teardown, and post-reload locked teardown quiescence.
- `d28d6f53d6ffa87444646caa39816310d8cd6b00` - bounded whole-packet review fix that moves irreversible permission cleanup after atomic storage commit; verified W1-P4 implementation head is `6f30b915719586cb519aa0f8f2b4418b0724debe` including its reviewed harness proof.
- `4b5a9b5e55172a5e393f47f972716eebeedd5590` - independently reviewed executable W1-P5 plan (`docs: plan W1-P5 Home Assistant safety`).
- `ea95db06d301e11d77fd945b6bab665a87e3948e` and `e74cf4b7450dba2797c14c165e4f8034969bbf53` - selected-entity polling, narrow action-only health, and task-review mutation guards.
- `cd90c3d4fbec391fd46e5743833360c03cd164e0` - generation-safe per-action exclusion and persistent accessible feedback.
- `3d4ef14a759c331b494120d0f73429a9a6e96c21` and `6f17fefd4396dae8117604a26e2abf7e5844cc42` - real-extension action/AX proof and reviewed screenshot/manual-ceiling strengthening.
- `2ab1139730d4677f4b015002f3a1f9adcd2e0d0d` - bounded whole-packet review fix that versions Home Assistant opaque snapshot identity and rejects fresh legacy action-only snapshots.
- `74d4e27a6fa1262416ac8aa0149020a0ef02918e` - preview fixture scope alignment; verified W1-P5 implementation head.
- `6a7e949adba78a5ef7266fea88d0e6577bea74cb` - independently reviewed executable W1-P6 plan (`docs: plan W1-P6 Weather races`).
- `e4b32030e95432adcebac0545c62f527dd035437` - normalized Weather request identity, provider signal forwarding, cache schema/backup validation, and multi-key storage updater.
- `7459124a6ff125900dab25a0a5c25822a6895723` - generation-safe Weather hook plus atomic location/cache mutation and retry handling.
- `ce74954777d961ecb31db9eafd385280de06a74a` - six-line built-extension Weather identity/race/visibility harness proof.
- `e29aa53cb346773753028a716a85423a33fe5129` - bounded review fixes for truthful modeled visibility evidence, updater-time ownership, fresh mismatch, rejection, and complete teardown.
- `f4ed9335dcbb74aa35b685eb0149667daaefd31e` - red/green remount hydration-race fix; verified W1-P6 implementation head.
- `ba12f028ba920c912439fdd27338e850e576f252` - independently reviewed executable W1-P7 plan (`docs: plan W1-P7 local-day rollover`).
- `578c8e483ba0ce1aee74f60127d3b9131b9d45ed` - IANA-zone day boundaries and generation-safe local-day/restoration clocks.
- `a537fa9ef598f6aa36a0a5d34c278d711c258f2c` - open-tab rollover across the named date-driven surfaces and APOD ownership.
- `f66c9345e1aef89f1eb0914fbc7d6a3a0019c74a` - explicit all-day Calendar/ICS semantics and timezone-scoped snapshots.
- `32ffb8bbc703ea498ad1de6ab13f1d31ebfd2def` - deterministic built-extension rollover, Timer wake, and Calendar semantics proof.
- `2fcb4438b0857500db1e5fb0785e1da45e9eacd8` - bounded-review fix for skipped-midnight/skipped-day inverse conversion and expired-boundary rescheduling; verified W1-P7 implementation head.
- `5f2b32f863f1ec4c479e4216de673c18a3b9eb0b` - independently reviewed executable W1-P8 plan.
- `4915f2f5320cdd1e235ea471d7140a795b7b7304` and `8f101c22d352af04cf8dbba6e8c26a37223a082b` - revision-owned recoverable Notes persistence plus awaited close, disable, arrange, and navigation boundaries.
- `58a3e7992c1422e879b58e9d66df5fe476128fca` - preview-only authority-preserving failure controller, built-extension navigation/AX proof, and error screenshot.
- `5c570f7087d8d6e41b73db9e271339c75c93f62c` - bounded-review fixes for a 36px Retry target and close-before-restore teardown quiescence; verified W1-P8 implementation head.
- `3864ba7ee5f9f349f393ac555ecbf6d5791970df` - independently reviewed executable W1-P9 plan.
- `db94a73f0c56139f4d4ebbed4de1ad00ac3ae1f7` - typed privacy inventory, shared Quick Link trust boundary, manifest copy, and local plaintext disclosure.
- `9e1d43937c5ec4f1a9523a709cea46928dd628e0` - behavior-aligned source privacy/listing copy and exact dev-only `nanoid` override.
- `1cefce7d8b498737c1e1c28264963210a1bd9c1a` - bounded-review fixes for malformed HTTP(S), exhaustive transmitted/received flow fields, APOD truth, and interim listing contradictions; final focused rereview Ready.
- `34f89600f24c7be203854bdbaf348932ab1831f0` - development harness alignment to an intercepted safe HTTPS Notes navigation target; verified W1-P9 implementation head.
- `a13f5b1b6a619f99d48e095d564677cd1c4acdef` - independently reviewed executable W2-P1 plan (`docs: plan W2-P1 shared async state`).
- `7dc510ac55f0f0175d517027b762510b889bdf1b`, `0d71e1f12d34a69d2d7ad31b6f71aa323114844f`, and `3505b13a825bf3684f1e566675f256cc79daf092` - pure operation/freshness algebra, shared render-only feedback, and hardened browser truth inputs.
- `76dddaeae3f0947d694bfc611ff115989d0b7467` and `6d930685ba9fb86629d0b4f5363afcd868a46779` - connector/Weather semantic resource state and Notes/Home Assistant shared operation feedback.
- `0b2db71f26c1686b2c0cd5528303ebe19756945f` - six exact built-extension state captures and Chromium AX evidence without adding another counted W2-P1 result.
- `f2c1b788d6f341eb0137a05f270e7a531a4db9af` - bounded whole-packet review fixes for Notes retry/edit ownership, 36px Weather recovery targets, causal Chromium Enter retries, and exact Unicode primitive tests; verified W2-P1 implementation head.
- `e59e929de0884126c7a7c493607dfcb027c29b1b` - independently reviewed executable W2-P2 plan (`docs: plan W2-P2 recovery semantics`).
- `dd958894a7ebaafa49c0402d0f041b53e2a3e42b` - stable Focus and Quick Link editor focus, cancel, commit, error, and target semantics.
- `914ac4b17dcaf2870524f3b7cf1ffea490fb32c3` - Calendar source relationships and Home Assistant picker naming/group semantics.
- `5449fc7cde652cca6a8ffeaa3880321e9ebfd791` - DrawerBoundary reopen recovery and Data export/import/restore feedback.
- `5db8dc44121fd9bece4cf670d8d04d9d337de3c3` - bounded whole-packet review fixes for async picker focus restoration and causal/stable browser evidence.
- `1fdaf0c9c8192b10b53d732b552aa656f1ca23a3` - packet-local Focus prompt geometry regression fix; verified W2-P2 implementation head.
- `b51674f9b356363790c4ec83f72f67bba9c62610` - independently reviewed executable W2-P3 plan (`docs: plan W2-P3 narrow reflow`).
- `461319d1c52b96bdc8271b61f86f3d5f27210557` - narrow Settings reflow, complete packet browser contract, exact target inventory, and reviewed lifecycle evidence.
- `135d1305f4042878c81e84d33ed6da803efdf560` - 8px viewport-fit math, current rendered-panel resize ownership, and bounded Notes/Tasks/Timer surfaces.
- `ce285f0de67ab2feeb6ad60fd29f7573e8af6aac` - bounded picker, Palette, Location, Folder, Reset, and reviewed body-portalled Tasks overflow behavior.
- `ced5e1cfc068426a8856b40bca8c22bd544b7e17` - whole-review fixes for the General endpoint, complete 320x180 evidence matrix, and non-occluded Location/Weather composite.
- `64675d245df08d126ab5610b807931ac47320b8e` - final resize/active-descendant ownership race fix; verified W2-P3 implementation head.
- `b9ec83296c9854de9449b6b1d2657ffed962ddad` - independently reviewed executable W3-P1 plan (`docs: plan W3-P1 layout migration`).
- `4d8aae1e5a5c0c423b326bbf53c8f6ddabaf281f` - pure versioned Layout V2 schema, deterministic mapper/normalizer, compatibility helpers, and immutable browser aggregate.
- `0f4ce3915078c785d7303a8dc3780070d8d16b99` - schema v10 migration and verified rollback-safe startup transaction.
- `a760fff8714bc0ac044448c39f38988d3635689b` - App, Arrange, reset, and old/current backup compatibility seams.
- `255a05f442900aac3cd10c1adc6408dc1bbab38c` and `be6ae5dc26da713c1bb7dd4c1dde906f12c76c0e` - separate full-suite timing fixes found only by controller-owned closeout attempts.
- `925438f890de62d5540221b86b53330f2dd0a5a1` - reviewed v10/V2 browser-fixture alignment; verified W3-P1 implementation head.
- `d77717c2f3662aabda0811d2e10be6d22f03660d` - reviewed executable W3-P2 Adaptive Stage registry plan.
- `ef26d73`, `cdaf9f6`, `170d05a`, and `648b7b2` - pure profile engine, schema-v11 density persistence, source registry/BoardItem, and semantic Adaptive Stage rendering.
- `b29ec6c`, `9952f7f`, and `ad79a523c81e51b593b7b57b9ba5cd45492873aa` - bounded review fixes and final accessibility/geometry/calibration gate fixes; verified W3-P2 implementation head.
- `c6a22cb`, `dc022b2`, and `088cf71` - executable W3-P3 plan, immutable semantic draft model, and production Arrange/profile editor with live Adaptive Stage preview.
- `eab1bfe` - bounded review fixes preserving disabled-widget placements during profile copy and preventing Escape from cancelling an in-flight Save.
- `8bc420d`, `672e136`, and `314bcbd` - focused semantic browser coverage, stale Arrange predecessor alignment, deterministic Weather recovery convergence, and the standalone W3-P3 replay; verified W3-P3 implementation head.
- `108ac59` - executable W3-P4 legacy-retirement plan preserving the frozen rollback-provenance boundary.
- `1951eb2` and `f5132fc` - retired live percentage placement/editing machinery, dead rail CSS, and whole-widget height-hide paths while retaining exact migration/backup provenance.
- `e4cebe4` - causal W3-P3 focus-restoration harness wait plus the focused W3-P4 semantic parity, representation, rollback, runtime, and capture proof; verified W3-P4 implementation head.
- `9f206da` - executable W4-P1 Day/Now plan with frozen schema, registry, capacity, storage, privacy, permission, and Store boundaries.
- `6d87137`, `9561074`, and `05669b8` - responsive sparse Day context, calm photo-forward Now presentation, expanded date detail, and the bounded-review accessibility fix.
- `089886e`, `5c33afb`, and `7acb851` - focused W4-P1 browser proof, frozen Compact-capacity evidence alignment, and causal full-harness Clock selector correction; verified W4-P1 implementation head.
- `8277845` - executable W4-P2 Aurora Briefing plan with local-only synthesis and frozen schema/registry/allocation/network boundaries.
- `5ec2f4a` and `4a66a50` - pure deterministic Calendar/Tasks/Rain synthesis and render-only Greeting integration.
- `c6c95c2`, `5258e2a`, and `e656263` - responsive text fitting, focused Chromium evidence, and bounded Compact Greeting containment corrections; verified W4-P2 implementation head.
- `793af31` - executable W4-P3 Work Pulse variants plan with frozen schema, registry, allocation, storage, privacy, network, permission, and Store boundaries.
- `ce23c04`, `fc258c0`, and `13a0b4a` - shared render-only attention summary, connector prioritization, and responsive Work Pulse hierarchy.
- `3248890` and `1ab0fa6` - reviewed Status detail progression plus causal global geometry and Vercel Compact containment corrections; verified W4-P3 implementation head.
- `5227110` - executable W4-P4 Signal Dock and connector-survival plan with frozen schema, registry, allocation, storage, privacy, permission, network, and Store boundaries.
- `4481358`, `c90fd94`, and `f441149` - one shared Dock-only connector disclosure, condensed connector geometry/presentation, and exact nine-connector built-extension survival/operation proof.
- `049be58` - bounded fix/rereview correction that yields the legacy Compact Vercel detail line and aligns stale fencepost reporting with Signal Dock survival; verified W4-P4 implementation head.
- `e1167ca`, `58d3d1a`, and `a1ee6d7` - adjacent launcher shelf composition plus Calendar/RSS/Home Assistant/Weather content variants.
- `4b8c714` and `250c1c3` - content-sized launcher surface and the bounded-review Weather variant-transition fix.
- `6656182` - full-harness alignment for render-only launcher ancestry and exact content-variant fixtures; verified W4-P5 implementation head.
- `e762699` and `8b36bac` - responsive Utility Tray shell plus the exact backdrop-dismissal focus-restoration fix and focused browser proof.
- `e7b3c5f` - final harness alignment for the W5-P1 persistent control and accepted Compact Calendar content budget; verified W5-P1 implementation head.
- `44e05c0` - existing-owner Tasks, Notes, Timer, Home Assistant action, and background-refresh integration with guarded Notes lifecycle and focused browser proof.

## Latest verification

- W6-P2 covered every named viewport/state plus 100/125/150/200/400% CSS-space reflow once and inspected the required captures in three bounded batches. The packaged replay passed overflow, 36/44px targets, keyboard focus/restoration, reduced motion, Chromium AX naming, touch taps, cleanup, and zero runtime errors.
- One Important compact Weather setup collision at 200/400% was fixed in `7c51e78`; focused evidence passed 3 files / 78 tests and the final source contract passed 21/21. One implementation review/rereview returned Ready with 0 Critical / 0 Important open.
- Final evidence passed 137 files / 2,264 tests, clean 189-module production/preview builds, and a clean production bridge scan. The full pass and its one permitted rerun printed 455 PASS / 2 FAIL / 3 SKIP because the legacy W2-P3 generic point-scanner could not discover the intentional Compact modal backdrop and aborted before cleanup, cascading into W3-P2. Direct focused backdrop evidence and component tests remain green; `cf3d212` source-corrected the selector without a prohibited third run. This is Minor evidence debt, not a product failure. Native zoom, mixed-DPI, real screen readers, physical hardware, native permission prompts, and live services remain explicit manual ceilings in `W6-P2-QA-MATRIX.md`.

- W6-P1 completed four first-run focused matrices: migration/stored-data 7 files / 317 tests; permission/secret recovery 7 / 219; offline/stale/error/persistence 13 / 190; civil-time/open-tab recovery 6 / 94. This is 820 executed assertions and 690 unique assertions because backup validation intentionally belongs to two families.
- The unchanged W5-P4 full gate was reused instead of repeated. The bounded W6-P1 review returned Ready with no Critical, Important, or Minor product finding; no fix/rereview and no product/test code change was required. Exact results and retained manual ceilings are in `W6-P1-QA-MATRIX.md`.

- W5-P4 focused development passed 6 files / 280 tests. The packaged Chromium replay proved an opaque local Focus prompt surface at 18.04:1 across bright, dark, and detailed packaged photos, 44px spacious persistent controls, 36px Settings controls, 14px ordinary and 12px metadata type floors, opaque Settings/Tray work surfaces, reduced-motion convergence, exact cleanup, and zero runtime errors. All three required captures were inspected once.
- One implementation review returned Ready with 0 Critical / 0 Important findings. The final unit gate passed 137 files / 2,262 tests; production and preview builds transformed 189 modules; TypeScript and the production bridge scan were clean.
- The first full browser run exposed two actual families: the Utility Tray invoker stayed at 36px under the spacious 44px Stage token, and a W2-P3 320x180 panel-level outside-point probe demanded empty space that the accepted fitted Tasks surface legitimately did not have. `a83f6ad` added the persistent target and retained 320x180 menu/geometry coverage while moving only the panel-level outside-pointer check to 320x568. The single permitted full rerun completed at 457 PASS / 0 FAIL / 3 SKIP. No third run or documentation-triggered gate was performed.

- W5-P1 focused development passed 2 files / 30 tests plus TypeScript. `scripts/preview-w5-p1.mjs` passed Standard 1600x900 modeless and Compact 800x600 modal behavior with free/trapped focus as appropriate, outside/Escape/backdrop dismissal, exact restoration, containment, dashboard isolation only in Compact, clean teardown, and zero runtime errors. Both captures were inspected once.
- The focused replay's first run found one genuine backdrop focus-restoration failure. `8b36bac` moved dismissal to the completed click and added its failing regression; the single focused rerun passed. One implementation review then returned Ready with 0 Critical / 0 Important findings, so no additional review was launched.
- The final `npm test` run passed 137 files / 2,250 tests. TypeScript, production, and preview builds exited 0 with 189 modules transformed; the production bridge scan found no forbidden preview symbols.
- The full harness ran once and reran once after adding the W5-P1 invoker to its persistent-target family. The rerun cleared W3-P2 and exposed one unrelated W2-P2 Calendar assertion that still required hidden agenda rows from a Compact variant. `e7b3c5f` made that assertion variant-aware; no third full run was performed. This is Minor legacy evidence debt, not a W5-P1 acceptance, security/privacy, stored-data, migration, core-functionality, or accessibility failure.

- W4-P5 focused development and rereview gates passed: the stabilized component/App set passed 5 files / 140 tests plus TypeScript; the focused built-extension replay passed Compact 800x600, Standard 1600x900, and Display 2560x1440 with one content-sized shelf where eligible, exact launcher identities, exact Calendar/RSS/Home Assistant budgets, Expanded Weather trend, safe links, keyboard focus restoration, no page clip, exact cleanup, zero external requests, and zero runtime errors.
- The one final `npm test` run passed 136 files / 2,244 tests. Final TypeScript, production build, and preview build exited 0; both builds transformed 188 modules. The production preview-bridge scan returned the expected no-match result.
- The full browser harness was run once, then rerun once after its actual stale content-variant/direct-parent family was corrected. The permitted rerun confirmed the corrected rollover, RSS, launcher-aware semantic ownership, Home Assistant expanded action fixture, and downstream interaction flows. Later failures were additional stale detailed-Calendar fixtures, hidden Status pixel coordinates, and the same direct-parent assumption in the frozen W3-P2 aggregate; `6656182` corrected those harness assertions without a third full run. They are Minor evidence debt pending the next scheduled harness and do not demonstrate security/privacy risk, stored-data loss, broken core functionality, or failure of a W4-P5 acceptance criterion.
- Required captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w4-p5\` were inspected once. The initial oversized shelf was the only focused visual acceptance failure; `4b8c714` made Standard and Display shelves content-sized, and the focused replay passed after the correction.
- One implementation review found one Important issue: Weather remained expanded after a Display-to-Standard/Compact variant transition. `250c1c3` added the failing transition regression and fixed it; the single focused rereview returned Ready with 0 Critical / 0 Important open. No schema, registry, profile threshold, capacity, migration, stored-data, permission, privacy, production-network, protected-checkout, or Store contract changed.

- W4-P4 focused development gates passed: the stabilized component/App/sizing/presentation set passed 5 files / 47 tests plus TypeScript; the final Vercel cascade regression test passed 1 file / 3 tests. `node --check scripts/preview.mjs` exited 0.
- The one final `npm test` run passed 135 files / 2,235 tests. Final `npx tsc --noEmit`, production build, and preview build exited 0; both builds transformed 187 modules. The production bridge scan returned the expected clean no-match result.
- Focused `node scripts/preview-w4-p4.mjs` exited 0 with `PASS: W4-P4 Signal Dock connector survival`. All nine frozen connector IDs were present exactly once in Dock across Compact 800x600, Standard 1600x900, and Display 2560x1440; identity/current value, inert closed state, 36px disclosure, containment, safe links, open detail, Escape/focus restoration, keyboard reveal, exact teardown, zero external requests, and zero runtime errors were green. All three captures were inspected once.
- The first full browser run found one genuine Compact Vercel paint escape and stopped later on a stale legacy fencepost log that dereferenced a Dock-condensed connector. The causal fix removed the high-specificity supporting detail, and the exact compact-density/six-deployment focused diagnosis reported no escaping box or text. The one permitted full rerun exited 0 and the Vercel family passed.
- The full rerun retained two historical Status pixel-sampling FAIL logs and one stale W3-P2 aggregate FAIL label. These are Minor evidence/report debt: Status meaning remains explicit in text and the W3-P2 observations reported empty runtime/captured-error, paint-escape, duplicate, and collision arrays with exact ownership/cleanup. They do not demonstrate security/privacy risk, data loss, broken core functionality, or failure of W4-P4 acceptance, so the packet was not reopened.
- One bounded implementation review plus one fix/rereview cycle ended Ready with no Critical or Important issue open. No schema, registry, profile threshold, capacity, allocation, migration, stored-data, permission, privacy, production-network, Aurora V1, or Store contract changed.

- W4-P3 focused development gates passed: the broad stabilized set was 9 files / 184 tests; the final causal Vercel set was 2 files / 26 tests. TypeScript and both harness syntax checks exited 0.
- The one final `npm test` run completed at 132 files with 2,225 / 2,226 tests passing. Its only failure was an unrelated untouched Settings color-write timing case; the exact one-test rerun passed 1 / 1. The full unit suite was not repeated.
- Final production and preview builds exited 0 with 186 modules transformed; the production bridge scan returned the expected clean no-match result.
- Focused `node scripts/preview-w4-p3.mjs` exited 0 with `PASS: W4-P3 Work Pulse variants`. Compact/Standard/Display expose exact progressive anatomy; healthy and attention Status states are explicit; Vercel Compact is contained; links are safe; teardown restores storage; and external requests/runtime errors are empty.
- The full browser harness was run once, then rerun once after its actual W4-P3 assertion/geometry family was corrected. The permitted rerun cleared the global geometry and obsolete variant-expectation families, leaving one genuine Vercel Compact paint-containment failure and two obsolete Status pixel-sampling assertions. The Vercel family passed its bounded focused fix; the two cosmetic pixel probes are Minor ledger items and did not reopen the packet. No third full harness run was performed.
- Required captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w4-p3\` were inspected once at Compact 800x600, Standard 1600x900, and Display 2560x1440.
- One bounded implementation review plus one fix/rereview cycle ended Ready with no Critical or Important issue open. No schema, registry, profile threshold, capacity, allocation, migration, stored-data, permission, privacy, production-network, Aurora V1, or Store contract changed.

- W4-P2 focused pure/component/source development gates passed: final combined set 3 files / 15 tests plus the final source guard 1 file / 4 tests; TypeScript exited 0.
- Final `npm test` after implementation stabilized - exit 0; 131 files / 2,218 tests passed. Final `npx tsc --noEmit` also exited 0.
- Final `npm run build` and `npm run build:preview` - exit 0; both transformed 185 modules. The production bridge scan returned the expected no-match result with no forbidden preview symbol output.
- Focused `node scripts/preview-w4-p2.mjs` - exit 0 with `PASS: W4-P2 Aurora Briefing semantics`; Compact/Standard/Display expose exact 1/2/3-signal text, one visible sentence, current local cache use, existing Greeting ownership, no clipping, no Briefing network request, restored teardown, and `runtimeErrors: []`. The final actual-failure replay also proves Greeting/Briefing containment at 600x800 and 320x800.
- The full browser harness first run reached 455 PASS / 2 FAIL / 3 SKIP. Both failures belonged to one Compact Greeting paint-containment family introduced by the new wrapper. After the causal selector fix, the single permitted rerun reached 456 PASS / 1 FAIL / 3 SKIP and exposed the same family at the adjacent 600px edge. The final bounded typography correction passed the focused 600x800 and 320x800 replay; the full harness was not run a third time, per pragmatic-delivery policy.
- Required captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w4-p2\` were inspected. The corrected 600x800 and 320x800 Compact captures were also inspected after the causal fix.
- One bounded implementation review plus its related fix/rereview cycle found and corrected the Display summary's second CSS truncation and Compact Greeting selector regression. No Critical or Important issue remains open. Minor/cosmetic observations did not reopen the packet.
- No schema, registry, profile threshold, capacity, allocation, migration, stored-data, permission, privacy, production-network, Aurora V1, or Store contract changed.

- W4-P1 focused development gates passed after RED: final focused set 6 files / 46 tests, the accessibility fix set 4 files / 37 tests, and TypeScript exited 0.
- Final `npm test` after implementation stabilized - exit 0; 129 files / 2,206 tests passed. `npx tsc --noEmit` also exited 0.
- Final `npm run build` and `npm run build:preview` - exit 0; both transformed 183 modules. The production bridge scan returned the expected no-match result with no forbidden preview symbol output.
- Focused `node scripts/preview-w4-p1.mjs` - exit 0 with `PASS: W4-P1 Day and Now zone semantics`; Compact/Standard/Display preserve the exact Clock/Greeting/Search/Focus order, every BoardItem is singly represented without percentage positioning, sparse/populated Day ownership is correct, expanded date detail is visible only where intended, targets are operable, page clipping is absent, teardown restored state, and `runtimeErrors: []`.
- The final full harness first run reached 453 PASS / 4 FAIL / 3 SKIP. All four failures were one selector family reading the new secondary date through the Clock block's aggregate `textContent`; measured geometry was already non-overlapping. Three probes were corrected to target the established Clock `<time>`, and the single permitted rerun exited 0 at exactly 457 PASS / 0 FAIL / 3 SKIP.
- Captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w4-p1\`: `w4-p1-compact-sparse-800x600.png`, `w4-p1-standard-populated-1600x900.png`, and `w4-p1-display-populated-2560x1440.png`. All three passed original-resolution inspection.
- One implementation review found one Important issue: the visible expanded date was hidden from assistive technology. `05669b8` fixed it; the focused rereview returned Ready with 0 Critical / 0 Important open. Minor ledger only: the first focused browser assertion incorrectly assumed Compact could hold all four items inside Now despite frozen capacity; evidence now checks the exact hierarchy across Now then Dock without changing product contracts.
- No schema, registry, profile threshold, capacity, migration, stored-data, permission, privacy, Aurora V1, or Store contract changed.

- W3-P4 focused source/layout/App/Arrange/backup/migration/widget development gates passed: 10 files / 346 tests, with the final narrowed source/widget gate at 5 files / 113 tests. TypeScript exited 0.
- Final `npm test` after implementation stabilized - exit 0; 127 files / 2,197 tests passed.
- Final `npm run build` and `npm run build:preview` - exit 0; both transformed 182 modules. The production bridge scan returned expected exit 1 with no forbidden preview symbol output.
- The final full harness first run reached 456 PASS / 1 FAIL / 3 SKIP. Its sole failure sampled Cancel focus one React effect before the already-tested Settings restoration completed; no product failure was present. The focused W3-P3 family passed after the causal wait, and the single permitted full rerun exited 0 at exactly 457 PASS / 0 FAIL / 3 SKIP, including the named W3-P2 and W3-P3 aggregates.
- Focused `node scripts/preview-w3-p4.mjs` - exit 0 with `PASS: W3-P4 legacy retirement and rollback semantics`; GitHub, GitLab, and Vercel are each represented once in Compact/Standard/Display, all omit inline percentage positioning, allocation is identical with and without extreme legacy provenance, legacy data is exact, teardown restored the preimage, and `runtimeErrors: []`.
- Captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w3-p4\`: `w3-p4-compact-800x600.png`, `w3-p4-standard-1600x900.png`, and `w3-p4-display-2560x1440.png`. All three passed original-resolution inspection.
- One implementation review against only the explicit packet criteria found 0 Critical / 0 Important issues. Minor follow-up only: historical percentage-era evidence names/comments and internal content-detail tiers owned by W4; neither reopens W3-P4.
- No schema, registry, migration, stored-data, permission, privacy, Aurora V1, or Store contract changed. The optional frozen `layout.legacy` member remains exact rollback provenance but has no live rendering/editing authority.

- W3-P3 focused red/green coverage exercises reorder, move, variant, priority, spans, lock, preview, Undo, Cancel, reset-one, copy-profile, Save-only persistence, sibling-profile/legacy preservation, and pending-Save lifecycle.
- Final `npm test` after implementation stabilized - exit 0; 130 files / 2,232 tests passed. `npx tsc --noEmit` also exited 0.
- Final `npm run build` and `npm run build:preview` - exit 0; both transformed 182 modules. The production bridge scan returned expected exit 1 with no forbidden preview symbol output.
- The first full browser attempt stopped after 66 PASS / 0 FAIL on stale legacy Arrange selectors. After that actual predecessor family was updated, the single allowed rerun reached 261 PASS / 0 FAIL / 3 SKIP and passed the legacy and semantic Arrange families before stopping on an unrelated W2-P1 Weather fixture that swallowed its offline-convergence timeout. The fixture now waits causally; no third full run was made.
- Focused `node scripts/preview-w3-p3.mjs` - exit 0 with `PASS: W3-P3 semantic Arrange/profile editor semantics`, three captures, exact draft-only/Cancel/Save evidence, preserved Compact/Display/legacy data, bounded Compact/Display geometry, and `runtimeErrors: []`.
- Captures under `C:\Users\SickT\Documents\Codex\2026-08-16\change-aurora-execution-to-pragmatic-delivery\outputs\w3-p3\`: `w3-p3-standard-editor-1600x900.png`, `w3-p3-compact-editor-800x600.png`, and `w3-p3-display-editor-2560x1440.png`. All three passed visual inspection.
- One implementation review found two Important issues; the single fix/rereview cycle addressed both and returned Ready with 0 Critical / 0 Important open. Minor follow-up only: historical percentage-era browser result names/comments and cosmetic evidence naming can be rationalized with their owning packet and do not reopen W3-P3.
- No schema, registry, migration, permission, privacy, Aurora V1, or Store action occurred. Percentage rendering/provenance remains only for the W3-P4 retirement gate.

- W3-P2 focused red/green tests covered the final Timer Dock boundary, Month compact labelling/rows, Quote clamping, Sun compact content, Vercel finite geometry, and exact full-composition regressions.
- Final `npm test` after implementation stabilized - exit 0; 129 files / 2,254 tests passed.
- Final `npm run build` and `npm run build:preview` - exit 0; both transformed 186 modules. The production bridge scan returned the expected exact `rg` exit 1 with no forbidden preview symbol output.
- The 234-profile Dock calibration completed successfully. One canonical final browser run identified three genuine geometry/timing families; their bounded fixes were followed by the single permitted full rerun.
- Final `node scripts/preview.mjs` rerun - harness process exit 0; exactly 456 `PASS:` / 0 `FAIL:` / 3 `SKIP:`, including `PASS: W3-P2 profile engine, registry, BoardItem, and semantic grid semantics`. Cleanup restored page, viewport, storage, and locks; captured page/console errors are empty.
- Fresh durable captures are under `C:\Users\SickT\Documents\Codex\2026-08-14\continue-aurora-2-continuously-through-all\outputs\w3-p2\`: `w3-p2-compact-800x600.png`, `w3-p2-compact-320x800-keyboard.png`, `w3-p2-standard-1600x900.png`, `w3-p2-display-2560x1440.png`, `w3-p2-ultrawide-3440x1440.png`, and `w3-p2-compact-dense-dock.png`. All six passed original-resolution inspection.
- One bounded implementation review and one fix/rereview cycle ended Ready with 0 Critical and 0 Important findings open. Minor follow-up only: rationalize stale pre-Adaptive-Stage evidence naming/explanatory comments and cosmetic edge cases when their owning packet is scheduled; they do not fail a W3-P2 acceptance criterion.
- The temporary multi-entry legacy Arrange bridge remains deliberately present for W3-P3 and is not a W3-P2 defect. The three SKIPs remain the existing live Home Assistant/user-instance and native NASA permission ceilings. Native zoom, mixed-DPI, and real screen-reader evidence remain W6-P2/manual. No Store or Aurora V1 action occurred.

- W3-P1 literal targeted gate at `925438f` - exit 0; 11 files / 590 tests passed.
- `npx tsc --noEmit` at `925438f` - exit 0.
- `npm test` at `925438f` - exit 0; 120 files / 2,085 tests passed.
- `npm run build` and `npm run build:preview` at `925438f` - exit 0; both transformed 180 modules.
- Production scan for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview bridge matched in `dist`.
- `npm audit --omit=dev` and `npm audit --include=dev` - exit 0; zero vulnerabilities.
- Fresh controller-owned foreground `node scripts/preview.mjs` at exact `925438f` - process exit 0; exactly 455 PASS / 0 FAIL / 3 SKIP, exactly one `PASS: W3-P1 layout v2 migration and compatibility semantics`, and zero named FAIL.
- W3 evidence reports exact deterministic migration into all four profiles, unknown-ID removal, unchanged representative percentage-render centers, current-V2 backup prepared and committed with exact layout/version, complete preimage restoration, disposable page closure, shared-lock crossing, and `errors: []`.
- The bounded whole-packet review returned Ready. Three later independent gate-fix reviews addressed full-suite timing, old v9/current v10 harness fixture separation, V2 Arrange fixtures, and bounded backup preparation timing without weakening product assertions; the final rereview found 0 new Critical / 0 new Important issues.
- W3-P1 adds no intended visual change or packet-specific screenshot. `newtab.png` and `rails-1280.png` were inspected at original resolution after the final run; board/rail geometry remained intact. The generated tracked Notes screenshot was restored to HEAD.
- The three harness SKIPs remain the existing live Home Assistant/user-instance and native NASA permission ceilings. Native zoom, mixed-DPI, and real screen-reader evidence remain W6-P2/manual. No Store, release, manifest-version, W3-P2, or V1 action occurred.

- W2-P3 literal plan-focused gate at `64675d2` - exit 0; 21 files / 475 tests passed. The reviewed plan command contains 21 literal paths.
- `npx tsc --noEmit` at `64675d2` - exit 0.
- `npm test` at `64675d2` - exit 0; 119 files / 1,998 tests passed.
- `npm run build` and `npm run build:preview` at `64675d2` - exit 0; both transformed 179 modules.
- Production scan for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview bridge matched in `dist`.
- `npm audit --omit=dev` and `npm audit --include=dev` - exit 0; zero vulnerabilities.
- Fresh clean-isolated foreground `node scripts/preview.mjs` at exact `64675d2` - process exit 0; exactly 454 PASS / 0 FAIL / 3 SKIP, exactly one `PASS: W2-P3 settings and tool reflow semantics`, zero named FAIL, exact 15 captures, and evidence errors `[]`.
- All fifteen W2-P3 captures were inspected at original resolution. Narrow Settings, Notes recovery, Tasks/Todo, Timer, picker, Palette, Location, and Folder states are contained and legible; corrected Location active option is visible/topmost inside its single scrollport; ordinary Settings is exactly 896,0-1280,720 and large Tasks exactly 2160,894-2544,1378.
- Initial whole review found 0 Critical / 3 Important gaps: the General Remove endpoint was focused but offscreen, the Location composite was occluded, and the 320x180 evidence matrix was incomplete. Separate RED-first fixes plus a final causal Location resize/active-descendant race fix ended with the same reviewer `Ready`, I1-I3 Addressed, and 0 new Critical / 0 new Important.
- The user's exploratory global `--canvas-fg-muted` white override was discarded with approval because it affected unrelated muted UI and broke a Focus appearance contract. W5-P4 now owns a targeted Focus-prompt contrast treatment over arbitrary photo backgrounds with representative contrast/screenshot evidence.
- Native Chrome 400% zoom, Windows scaling/mixed-DPI moves, and real screen-reader behavior remain explicit W6-P2/manual ceilings. No Store, release, manifest-version, or V1 action occurred.

- W2-P2 exact targeted suite at `1fdaf0c` - exit 0; 16 files / 661 tests passed.
- `npx tsc --noEmit` at `1fdaf0c` - exit 0.
- `npm test` at `1fdaf0c` - exit 0; 117 files / 1,962 tests passed.
- `npm run build` and `npm run build:preview` at `1fdaf0c` - exit 0; both transformed 178 modules.
- Production scan for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview bridge matched in `dist`.
- `npm audit --omit=dev` and `npm audit --include=dev` - exit 0; zero vulnerabilities.
- Fresh foreground `node scripts/preview.mjs` at `1fdaf0c` - process exit 0; exactly 453 PASS / 0 FAIL / 3 SKIP, exactly one `PASS: W2-P2 focus, naming, boundary, and recovery semantics`, and zero named FAIL. Focus completion/Edit and prompt input remain at least 36 CSS px with no interactive collisions; bottom-member clearances are 8.7, 8.5, 13.5, 24.5, and 84.5 CSS px at the five exact geometry viewports.
- The six exact W2-P2 captures (`w2-p2-focus-link-800x600.png`, both Calendar source captures, both Home Assistant picker captures, and `w2-p2-data-feedback-2560x1440.png`) were inspected at original resolution: the Quick Link alert and pending restore status are visible and non-color-only; Calendar rows are stable and legible; picker content, focus, and controls are unclipped; and the pending restore controls visibly remain disabled.
- Chromium `Accessibility.getFullAXTree` exposes the named/associated Focus, Quick Link, Calendar, Home Assistant picker, and Data status/alert/button/disabled/busy relationships. This is supporting Chromium AX evidence, not a real screen-reader run.
- The bounded whole-packet review found 0 Critical and 3 Important findings. Real async picker focus loss plus non-causal/unstable browser evidence were fixed with red/green coverage in `5db8dc4`; the same reviewer rereviewed every finding and returned Ready. Controller verification then exposed six packet-local bottom-band failures caused by the new 36px Focus prompt input increasing centered flow height; an independent causal audit, literal unit/browser RED, and exact margin-box compensation fixed them in `1fdaf0c`. Final rereview returned Ready with no Critical/Important issue open.
- The three harness SKIPs remain the existing live Home Assistant/user-instance and native NASA permission ceilings. Real screen-reader behavior remains manual; no Store action occurred.

- W2-P1 exact targeted suite at `f2c1b78` - exit 0; 10 files / 187 tests passed.
- `npx tsc --noEmit` at `f2c1b78` - exit 0.
- `npm test` at `f2c1b78` - exit 0; 116 files / 1,929 tests passed.
- `npm run build` and `npm run build:preview` at `f2c1b78` - exit 0; both transformed 178 modules.
- Production scan for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview bridge matched in `dist`.
- `npm audit --omit=dev` and `npm audit --include=dev` - exit 0; zero vulnerabilities.
- Fresh foreground `node scripts/preview.mjs` at `f2c1b78` - process exit 0; exactly 452 PASS / 0 FAIL / 3 SKIP, exactly one `PASS: W2-P1 shared async and freshness semantics`, and zero named FAIL. One prior unchanged run was rejected at 451 / 1 / 3 for the unrelated legacy Status removal timing flake; the accepted rerun changed no source.
- The six exact W2-P1 compact/standard/large Notes and Weather captures plus `w2-p1-weather-cached-recovery-1600x900.png` and `w2-p1-weather-no-data-recovery-800x600.png` were inspected at original resolution: feedback is readable and non-color-only, focus is visible, controls/panels are unclipped, both recovery targets are at least 36 CSS px, and geometry is unchanged.
- Chromium `Accessibility.getFullAXTree` exposes the named Notes/Home Assistant/Weather status, alert, button, description, disabled, and busy semantics, including both Weather recovery paths. This is Chromium AX evidence, not a real screen-reader run.
- The bounded whole-packet review found 0 Critical, 3 Important, and 1 packet-local Minor finding. All were fixed with red/green coverage in `f2c1b78`; focused rereview found no new Critical/Important breakage. The three harness SKIPs remain the existing live Home Assistant/user-instance and native NASA permission ceilings; W2-P1 added none.

- W1-P9 exact targeted suite - exit 0; 6 files / 362 tests passed.
- `npx tsc --noEmit` - exit 0.
- `npm test` - exit 0; photo manifest 23 entries / 46 tier files; 114 files / 1,890 tests passed.
- `npm run build` and `npm run build:preview` - exit 0; both transformed 176 modules. Production and preview descriptions are exactly `A calm, local-first new-tab dashboard. No Aurora account, no tracking, no backend.`; permission arrays preserve their exact existing split and `https://*/*` optional-host boundary.
- Production search for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `npm audit --omit=dev` and `npm audit --include=dev` - zero vulnerabilities. `npm ls nanoid --all` resolves only `vite@6.4.3 -> postcss@8.5.23 -> nanoid@3.3.18 overridden`; the lock diff changes only nanoid 3.3.16 to 3.3.18 metadata.
- `node scripts/preview.mjs` final W1-P9 run - process exit 0; exactly 451 PASS / 0 FAIL / 3 SKIP. The W1-P8 Notes navigation probe now uses a routed HTTPS Quick Link compatible with W1-P9 policy and still proves warning, retained dismissed draft, Retry, navigation, and exact teardown.
- Independent plan review fixed two Important Quick Link/cache-contract gaps plus inventory/copy/dependency/preview-check precision. Independent implementation review found three Important malformed-HTTP(S), inventory-depth/APOD, and listing-truth gaps; red/green fixes plus two focused rereviews ended `ADDRESSED — Ready` with no Critical issue open.
- The three SKIPs remain unchanged manual ceilings: live Home Assistant picker/successful action against the user's instance, native NASA Block, and native NASA Allow. No W1-P9 SKIP was added.

- W1-P8 exact targeted suite at `5c570f7` - exit 0; 13 files / 173 tests passed.
- `npx tsc --noEmit` at `5c570f7` - exit 0.
- `npm test` at `5c570f7` - exit 0; photo manifest 23 entries / 46 tier files; 112 files / 1,848 tests passed.
- `npm run build` and `npm run build:preview` at `5c570f7` - exit 0; both transformed 174 modules.
- Production search for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `node scripts/preview.mjs` final reviewed W1-P8 run at `5c570f7` - harness process exit 0; exactly 451 PASS / 0 FAIL / 3 SKIP. Its four W1-P8 lines prove deferred Saving-to-Saved truth, rejected-write draft/alert/Retry/AX behavior, awaited Escape and arrange entry, current-tab Quick Link beforeunload retention/unblock, and exact close-before-restore teardown.
- `screenshots/w1-p8-notes-error.png` was inspected at original 1600×900; the fixed 320×256 panel is unclipped, the alert is visible and not color-only, and Retry owns a 36px target. Built checks also passed at 800×600 and 2560×1440. Chromium's accessibility snapshot contains the alert and named Retry button; this is not a real screen-reader run.
- The independent implementation review found two Important gaps: the recovery target was approximately 16px high and assertion-failure teardown could restore before dirty-editor unmount cleanup. Red target coverage plus close/reset/lock-barrier teardown fixed both in `5c570f7`; focused rereview found no remaining Critical, Important, or packet-local Minor issue.
- The first and third W1-P8-capable full harness attempts each hit only the known unrelated Status row/dot UI timing race after the W1-P8 lines passed; a fresh post-review run produced the required 451 / 0 / 3 total without changing Status behavior. The three SKIPs remain the unchanged Home Assistant/native NASA permission ceilings; W1-P8 added none.
- W1-P7 exact targeted suite at `2fcb443` - exit 0; 23 files / 563 tests passed.
- `npx tsc --noEmit` at `2fcb443` - exit 0.
- `npm test` at `2fcb443` - exit 0; photo manifest 23 entries / 46 tier files; 110 files / 1,831 tests passed.
- `npm run build` and `npm run build:preview` at `2fcb443` - exit 0; both transformed 173 modules.
- Production search for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `node scripts/preview.mjs` final W1-P7 run at `2fcb443` - harness process exit 0; exactly 447 PASS / 0 FAIL / 3 SKIP. All four W1-P7 lines passed: Focus/Countdown/Quote/Background crossed midnight in an already-open extension page; Timer completed exactly once after wake; explicit all-day, timed-midnight, and Join semantics remained distinct; request dedupe, exact storage, and an advancing restored main-page clock proved deterministic teardown.
- Unit/component evidence covers New York and Berlin 23-hour/25-hour days; Havana, Santiago, and Azores skipped-midnight first-valid boundaries; Apia's skipped civil day; earlier overlap and least-after gap resolution; calendar distances; no expired-boundary one-millisecond loop; stale generations; Strict Mode/unmount; restoration signals; every named surface; APOD stale resolve/reject/finally ownership; ICS v2 timezone scope and exact payload validation; Calendar tokens/windows; and Timer exactly-once wake completion.
- The independent implementation review found one Critical midnight-transition inverse defect that could stamp the previous civil day and drive a one-millisecond resample loop. Red tests reproduced all cited zones before `2fcb443`; the scoped rereview found no remaining Critical, Important, or packet-local correctness finding.
- Final harness SKIPs remain the unchanged manual ceilings: real Home Assistant picker/action behavior against the user's instance, native NASA Block, and native NASA Allow. W1-P7 added no SKIP. Genuine operating-system timezone changes and real sleep/wake remain additional manual environment gates rather than skipped automated assertions.
- W1-P6 exact targeted suite at `f4ed933` - exit 0; 17 files / 532 tests passed.
- `npx tsc --noEmit` at `f4ed933` - exit 0.
- `npm test` at `f4ed933` - exit 0; photo manifest 23 entries / 46 tier files; 106 files / 1,751 tests passed.
- `npm run build` and `npm run build:preview` at `f4ed933` - exit 0; both transformed 172 modules.
- Production search for `__auroraStorageHarness`, `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `node scripts/preview.mjs` final controller-owned run at `f4ed933` - harness process exit 0; exactly 443 PASS / 0 FAIL / 3 SKIP. All six W1-P6 assertions passed: fresh same-label/different-coordinate mismatch suppression; complete normalized B request while A is held; B render/persist identity; late A fulfillment rejection; fresh-cache no-fetch plus exact-boundary modeled visibility refresh; repeated modeled visibility dedupe and quiescent exact-state teardown.
- The first fresh full harness run after review was an intentional red record at 441 PASS / 2 FAIL / 3 SKIP: independent storage hydration let `location` resolve before `weatherCache` on reload and started a needless request. A delayed-read regression reproduced it before `f4ed933`; the final full rerun above is green.
- Independent W1-P6 plan review fixed seven Important and two Minor gaps. The bounded implementation review fixed two Important and two Minor evidence/test gaps in `e29aa53`; controller verification then exposed the hydration race fixed in `f4ed933`. Final scoped rereviews found no Critical, Important, or packet-local Minor issue open.
- Headless visibility evidence is deliberately limited: production-listener convergence is exercised with a modeled `visibilityState` signal and standard event, while fake-time unit tests prove the exact `MAX_AGE_MS - 1` to boundary transition and listener semantics. No native headless tab-visibility transition is claimed.
- Final harness SKIPs remain the unchanged manual ceilings: real Home Assistant picker/action behavior against the user's instance, native NASA Block, and native NASA Allow. W1-P6 added no SKIP.
- W1-P5 expanded exact targeted suite at `74d4e27` - exit 0; 10 files / 378 tests passed.
- `npx tsc --noEmit` at `74d4e27` - exit 0.
- `npm test` at `74d4e27` - exit 0; photo manifest 23 entries / 46 tier files; 104 files / 1,709 tests passed.
- `npm run build` and `npm run build:preview` at `74d4e27` - exit 0; both transformed 171 modules.
- Production search for `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `node scripts/preview.mjs` final controller-owned run at `74d4e27` - process exit 0 in 276.5 seconds; 437 PASS / 0 FAIL / 3 SKIP. Home Assistant evidence covers selected fixture scope v2, real Enter/Space activation, synchronous disabled/busy pending, same-action no-op input, sibling isolation, persistent natural failure, genuine pending/focused/error screenshot state, retry quiescence, `entities:null` hiding the whole card, exact Settings disclosure, and `Accessibility.getFullAXTree` status/alert semantics. The preserved W1-P3/W1-P4 permission, all-key restore, locked teardown, and native-boundary evidence also passed.
- Official endpoint boundary: regular polling uses authenticated single-entity `GET /api/states/{entity_id}`; picker-only discovery uses bulk `GET /api/states`; action-only health uses authenticated `GET /api/`; actions retain service POSTs without new permissions. Tokens remain in headers and opaque SHA-256 scopes only.
- `screenshots/ha-action-error.png` was inspected at original 1600x1100: six chips and three actions are unclipped; Movie night is genuinely pending; Porch plug is focused with complete persistent error text; meaning is independent of red.
- Chromium AX evidence exposes `Run Porch plug` as disabled/busy and described by `Running Porch plug…`, then enabled and described by the complete retry alert. This is screen-reader-oriented accessibility-tree evidence, not a real assistive-technology run.
- Final harness SKIPs remain honest ceilings: real Home Assistant picker contents plus a successful real service action against the user's instance, native NASA Block, and native NASA Allow. A real screen reader remains a separate manual ceiling.
- Independent W1-P5 plan review fixed all confirmed Critical/Important and packet-local Minor gaps; final verdict Ready. Task reviews fixed missing service mutation guards and harness evidence gaps. The bounded whole-packet review found one Important legacy action-only snapshot bypass, fixed in `2ab1139`; controller verification then exposed the preview v1/v2 fixture mismatch, fixed in `74d4e27`. Scoped rereviews found no Critical, Important, or packet-local correctness issue open.
- W1-P4 exact targeted suite at `6f30b91` - exit 0; 15 files / 600 tests passed.
- `npx tsc --noEmit` at `6f30b91` - exit 0.
- `npm test` at `6f30b91` - exit 0; photo manifest 23 entries / 46 tier files; 104 files / 1,694 tests passed.
- `npm run build` and `npm run build:preview` at `6f30b91` - exit 0; both transformed 171 modules.
- Production search for `__auroraPermissionsHarnessApi`, `__auroraBackupHarness`, and `__auroraRestoreHarness` - expected exit 1; no forbidden preview symbol matched in `dist`.
- `node scripts/preview.mjs` final controller-owned run - process exit 0 in 274.9 seconds; 432 PASS / 0 FAIL / 3 SKIP. All eight W1-P4 lines passed: secret-safe real export, confirmation-turn adapter request, exact cleaned atomic commit/cache reset, exact restored-owned set and old-only revoke, committed revoke failure with durable tab-round-trip Retry, exact trusted re-entry copy, exact old-only retry removal, and post-reload locked all-key/native-boundary teardown. The permission-matrix outer fallback also passed its all-key locked restore proof.
- Final harness SKIPs remain honest ceilings: real Home Assistant entity-picker behavior against a live user instance, native NASA Block, and native NASA Allow. Headless automation cannot settle the browser permission prompt.
- Independent W1-P4 plan review - four Important contract gaps and one Minor timestamp ambiguity were fixed; final verdict Ready.
- Independent W1-P4 implementation review - task reviews fixed all findings; bounded whole-packet review found one Important rollback/revoked-old-grant defect, fixed in `d28d6f5`; fresh rereview marked it addressed. Controller verification then exposed an in-flight connector-snapshot teardown race, fixed in `6f30b91`; fresh scoped review found no Critical, Important, or packet-local correctness issue open.
- W1-P3 exact targeted suite after final review fix - exit 0; 18 files / 631 tests passed.
- `npx tsc --noEmit` at `a8d6236` - exit 0.
- `npm test` at `a8d6236` - exit 0; photo manifest 23 entries / 46 tier files; 103 files / 1,631 tests passed.
- `npm run build` and `npm run build:preview` at `a8d6236` - exit 0; both transformed 170 modules.
- `rg -n "__auroraPermissionsHarnessApi" dist` after the production build - expected exit 1; no adapter-name match in production output.
- `node scripts/preview.mjs` final reviewed W1-P3 run - process exit 0 in 277.1 seconds; 424 PASS / 0 FAIL / 3 SKIP. PASS evidence covers trusted event-turn request ordering, Home Assistant newly acquired rollback, post-validation pre-existing preservation, RSS + Status shared/final ownership, APOD + RSS partial/final ownership, one-shot revoke failure with durable Retry, and exact storage/UI/native-boundary restoration.
- Final harness SKIPs remain honest ceilings: real Home Assistant entity-picker behavior against a live user instance, native NASA Block, and native NASA Allow. Headless automation cannot settle the browser permission prompt.
- `remove revokes live` disposition: the renamed live Status row/dot assertion passes but never held a native grant; adapter-held production transaction final-owner removal passes separately; revoking an actually held native Chrome host grant remains unautomated headlessly and is not claimed fixed.
- Independent W1-P3 plan review - initial acquisition-classification, cross-context, owner-readiness, retry-lifetime, and harness-truthfulness findings fixed; final verdict Ready.
- Independent W1-P3 implementation review - per-task reviews fixed all findings; bounded whole-packet review found three Important issues, fixed in `a8d6236`; scoped rereview found no Critical, Important, or packet-local correctness issue open.
- W1-P2 exact targeted suite — exit 0; 7 files / 312 tests passed.
- `npx tsc --noEmit` after the review fix — exit 0.
- `npm test` after the review fix — exit 0; photo manifest clean; 98 files / 1,544 tests passed.
- `npm run build` after the review fix — exit 0; production build passed, 165 modules transformed; `__auroraStorageHarness` absent from `dist`.
- `npm run build:preview` after the review fix — exit 0; preview build passed, 165 modules transformed.
- `node scripts/preview.mjs` final W1-P2 run — harness process exit 0; 412 PASS / 0 FAIL / 3 SKIP. Both exact MV3 extension pages exposed Web Locks and the preview-only authority probe; all 50 concurrent mutations were retained.
- Independent W1-P2 plan review — initial one Critical and six Important findings fixed; a follow-up NotesPanel fixture finding fixed; final verdict Ready with no Critical or Important findings.
- Independent W1-P2 implementation review — one Important global harness monkey-patch and two Minor determinism/path findings confirmed and fixed in `fac883b`; bounded rereview found no Critical, Important, or packet-local Minor findings.
- W1-P1 exact targeted suite — exit 0; 12 files / 409 tests passed.
- `npx tsc --noEmit` after the review fix — exit 0.
- `npx vitest run` after the review fix — exit 0; 97 files / 1,531 tests passed.
- `npm run build` after the review fix — exit 0; TypeScript and Vite production build passed, 164 modules transformed.
- Independent W1-P1 review — one Important queued-update stale-write race confirmed; red/green regression added and fixed in `cd511f0`; no other actionable findings and none left open.
- `npm ci` — exit 0; 199 packages installed; 200 audited.
- `npm test` — exit 0; photo manifest 23 entries / 46 tier files; Vitest 96 files / 1,515 tests passed.
- `npm run build` — exit 0; TypeScript and Vite production build passed, 163 modules transformed.
- `npm run build:preview` — exit 0; preview build passed, 163 modules transformed.
- `node scripts/preview.mjs` full run 1 — harness process exit 0; 408 PASS / 1 FAIL / 3 SKIP. Sole failure: `remove revokes live`.
- `node scripts/preview.mjs` full run 2 — harness process exit 0; 408 PASS / 1 FAIL / 3 SKIP. Identical sole failure: `remove revokes live`.
- Harness skips: real Home Assistant picker against user instance; NASA optional-permission Block path; NASA optional-permission Allow path. Headless prompt does not settle.
- `npm run package` — exit 0; guards passed; 59 files; 60,400,065 bytes; no source maps; 46 photos; 3 icons.
- `npm audit --omit=dev --json` — exit 0; zero production advisories.
- `npm audit --json` — exit 1; one indirect high advisory: `nanoid <3.3.18` custom generator loop with size zero; development tree only.
- Independent Wave 0 review — initial With fixes; one Critical and three Important plan findings fixed; scoped re-review Ready with no unresolved Critical/Important or new issues.

## Release and Store inventory

- Original V1 checkout remains clean on `main` at `eb1354b`.
- Preserved original `release/aurora-1.14.0.zip`: 60,400,065 bytes; SHA-256 `4da05f9763dfddd529695dcc5c41f7e8d73b53090740bb5d330166b1aec2f1fa`.
- Isolated baseline rebuild: same byte size; SHA-256 `0c46ecbaab9329e9933cc96d3edbb2eb4eb988e6e797a5dbc251702ba4fff9b7`; baseline-only and not a replacement artifact.
- Tracked release files now include `release/RELEASE-DOSSIER-2.0.0.md`, `release/RELEASE-NOTES-2.0.0.md`, `release/LAUNCH-CHECKLIST.md`, the historical `release/RESUBMISSION-NOTES.md`, and `release/store-listing.md`.
- Rejected local 2.0 candidate: ignored `release/aurora-2.0.0.zip`, 60,443,024 bytes, 59 entries, SHA-256 `31930FEC4A288992EC74FD3173F08FE444D132A00FA7EF51C0900B629F356116`. Retain as historical evidence only; do not upload.
- Current ignored Store evidence: five exact 1280x800 PNGs in `release/store-shots/`, generated from non-personal fixtures and inspected separately at original resolution.
- Historical local release inventory still includes ZIPs 1.2.0, 1.2.1, and 1.3.0 through 1.14.0 plus the replaced v1.2-era screenshot set in history.
- Live Chrome Web Store item: Aurora `1.2.1`, item ID `akjalbmacojpmebkgohhcaaiacicpgkh`, `Published - public`; signed-in dashboard values were transcribed read-only on 2026-08-17.
- No Store dashboard field, listing, package, draft, submission, publication, or rollout state has been changed.

## Visual artifacts inspected

- `D:\DEV\Chrome plugin\output\product-concepts\aurora-arrange-mode-future.png`
- `D:\DEV\Chrome plugin\output\product-concepts\aurora-layout-quiet-column.png`
- `D:\DEV\Chrome plugin\output\product-concepts\aurora-connected-command-center.png`
- Recorded cues: protected centered Now anchor, coherent Day rail, shared Work Pulse surface, launcher shelf, direct preview/reset arrange affordances.
- Explicit non-proof: static concepts do not validate responsive, accessibility, collision, focus, or real-data behavior.

## Known blockers and residual risks

- Native Chrome optional-permission Block/Allow interaction remains unavailable in headless automation; W1-P4 does not claim a synthetic click, a grant restored from a backup file, or native-grant revocation.
- Real Home Assistant entity-picker contents, successful service execution, and real screen-reader behavior still require the user's live instance/manual session.
- Chromium AX trees support W2-P1/W2-P2 semantic inspection but do not establish real assistive-technology speech, timing, or interaction behavior.
- Store-side upload and dashboard changes require contemporaneous W6-P5 owner approval; the current signed-in session does not itself grant that authority.
- The current local 2.0 ZIP, Store screenshots, and listing dossier are historical only. Final release restaging occurs after the owner-selected feature programs stabilize; it does not authorize a Store action.
- Mixed-DPI monitor moves and real Home Assistant hardware/service behavior require later environment/user evidence.
- The indirect development-only nanoid advisory is resolved by the exact `3.3.18` override; production and full audits are clean and no production dependency changed.
- A native hidden-to-visible browser transition is unavailable in the current headless harness; W1-P6 records modeled built-extension convergence plus exact fake-time listener coverage without overstating that boundary.
- Genuine operating-system timezone changes and machine sleep/wake cannot be induced by the deterministic extension harness; W1-P7 proves their production event paths and clock discontinuities with exact unit/component coverage and retains the real-environment checks as manual gates.
- Browser beforeunload is intentionally best-effort: W1-P8 starts the authority flush and warns only while dirty, but cannot promise an async write finishes after a user explicitly confirms navigation.

## Files intentionally dirty

- Pre-existing untracked `artifacts/`.
- Pre-existing untracked `docs/superpowers/TAKEOVER-2026-08-24-WIDGET-UI-RECOVERY.md`.

## Continuous remaining-work protocol

- **Packet:** PM-P3 hosted sandbox billing return and Checkout recovery verified to the signed-in manual ceiling
- **Plans:** `docs/superpowers/plans/2026-09-01-tab-two-stripe-billing.md`, `docs/superpowers/plans/2026-09-01-tab-two-branded-billing-return.md`, and `docs/superpowers/plans/2026-09-01-tab-two-resumable-checkout.md`
- **State:** PM-P2 hosted account authority and PM-P3 hosted test billing authority are active on Supabase Free. The Stripe sandbox catalog/webhook, branded Cloudflare return surface, exact production extension bridge, same-plan Checkout recovery, completed sandbox subscription, and paid-status convergence repair are verified. The user should reload the exact corrected account-enabled build and confirm `Active subscription` after refreshing billing. Live Stripe, Supabase Pro, PM-P4, merge, release, and Store state remain separately gated.
- After each packet's dedicated checkpoint, push, local/upstream equality proof, and clean target/protected-original proof, proceed directly to the next Not started `ROADMAP.md` packet without a new chat handoff or continuation prompt.
- Before each just-in-time plan, re-read the master specification, `STATUS.md`, `ROADMAP.md`, and `DECISIONS.md`, and revalidate repository provenance and cleanliness.
- Keep one written packet envelope, one review/fix round, one focused verification set, and one checkpoint per packet. Do not combine packets or skip their gates.
- Pause only for a genuine blocker, a material decision requiring new authority, either written owner visual gate, an explicit handoff request, or the W6-P5 Store approval boundary. W6-P5 must stop before any upload, submission, publication, rollout, or live-listing change unless contemporaneous explicit approval is received.
- Historical packet plans remain evidence; their previous handoff wording does not override the newer continuous-run protocol.

## Single continuous-run seed

```text
Worktree: D:\DEV\Chrome plugin-aurora-2
Branch: feat/aurora-2-observatory
Next gate: manually witness corrected `Active subscription` display from the exact latest account-enabled build
Verified PM-P2 local runtime source checkpoint: b3555b0f47245a31ea8878c562b708ed8935430c
Production Google/session diagnostic source: 35a0853c093d78dec5cb1a387380f5e602c5fb83
Hosted lease hydration repair source: a6be8691717a657ac6486711282940671673e0af
Expected next action: owner reloads the corrected unpacked build, opens Account & Sync, refreshes billing, and confirms `Active subscription`; no new Checkout should be started.
Execution: PM-P3 hosted sandbox return, recovery, completed test subscription, and status-convergence repair are verified to the corrected installed-build display ceiling. Stop before live Stripe, Supabase Pro, PM-P4, merge, release, or Store mutation until the applicable explicit approval is recorded.
Hard stop: no Chrome Web Store upload, edit/save, submission, publication, distribution, or rollout without contemporaneous explicit approval.
```
