# Aurora v1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v1.1 — bookmark folders bar, notes scratchpad, world clocks, countdown, weather details, multi-photo backgrounds, export/import backup, and the reviewed polish batch.

**Architecture:** unchanged. New widgets follow the established pill/panel and chip/popover patterns; all pure logic TDD'd; storage bumps to schema v2 with the first real migration. Where a step says "follow the X pattern," the named file is the exemplar and its conventions (surface classes, focus-visible, Escape handling, gate/inner hook split) are requirements, not suggestions.

**Spec:** `docs/superpowers/specs/2026-07-26-aurora-v1.1-design.md`

## Global Constraints

- Everything from the v1 plans still binds: local-first (network = Open-Meteo + the one reverse-geocode; NOTHING new), `chrome.*` only in the storage driver, `faviconUrl`, the preview harness, and (new, Task 26 only) `src/services/bookmarks.ts`; no new runtime deps; a11y non-negotiable (labels, focus-visible, reduced-motion, Escape via the NEW dialog stack after Task 22).
- Panel surfaces: floating panels/popovers use `bg-[#17171c]/95 border border-panel-border backdrop-blur-[var(--panel-blur)] rounded-panel`; pills follow the Tasks pill classes.
- chrome.storage deep-equal writes emit no events (memoryDriver mirrors); nonce pattern where a re-read must trigger.
- Verification per task: `npm test` + `npm run build` + `node scripts/preview.mjs` (extend when the task says so) with NO console errors; controller reviews new screenshots.
- Commits end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` (`<footer>`); after each task's commit, `git push` (remote `origin` exists).
- Work on `main`, continuous, no pauses.

---

### Task 21: Schema v2 + first real migration

**Files:**
- Modify: `src/lib/storage/schema.ts`, `src/lib/storage/migrations.ts`
- Test: `src/lib/storage/migrations.test.ts` (extend)

**Interfaces:**
- Produces: `CURRENT_VERSION = 2`. New types `Notes { text: string; updatedAt: number }`, `WorldClock { zone: string; label: string }`, `Countdown { id: string; name: string; date: string /* YYYY-MM-DD */ }`. `AuroraData` gains `notes: Notes`, `worldClocks: WorldClock[]`, `countdowns: Countdown[]`. `WidgetToggles` gains `bookmarks: boolean; notes: boolean; clocks: boolean; countdown: boolean` (defaults false/true/false/false). `WeatherSnapshot` gains `sunriseISO?: string; sunsetISO?: string`. `migrations[1]` upgrades v1→v2.

- [ ] **Step 1: Failing migration tests** — append to `migrations.test.ts`:

```ts
describe('v1 -> v2', () => {
  it('backfills the new nested widget toggles while preserving user choices', () => {
    const v1Settings = {
      ...defaults().settings,
      name: 'Jon',
      widgets: { search: false, weather: true, links: true, todo: true, timer: true, quote: false },
    }
    const out = migrate({ settings: v1Settings }, 1)
    expect(out.settings.name).toBe('Jon')
    expect(out.settings.widgets.search).toBe(false) // user choice preserved
    expect(out.settings.widgets.quote).toBe(false)
    expect(out.settings.widgets.bookmarks).toBe(false) // new keys backfilled
    expect(out.settings.widgets.notes).toBe(true)
    expect(out.settings.widgets.clocks).toBe(false)
    expect(out.settings.widgets.countdown).toBe(false)
  })

  it('tolerates a v1 snapshot with no settings at all', () => {
    const out = migrate({}, 1)
    expect(out.settings.widgets.notes).toBe(true)
    expect(out.notes).toEqual({ text: '', updatedAt: 0 })
  })

  it('new top-level keys default for v1 snapshots', () => {
    const out = migrate({ links: [] }, 1)
    expect(out.worldClocks).toEqual([])
    expect(out.countdowns).toEqual([])
  })
})
```

(Existing v1-era tests calling `migrate(snapshot, 1)` now go through `migrations[1]` and must still pass unchanged. ONE existing test requires an update: the custom-registry ordering test (`migrate({}, 0, registry)`) only defines `registry[0]`, which now throws at v1 — extend its registry with `1: (data) => { calls.push(1); return data }` and change the expectation to `expect(calls).toEqual([0, 1])`. The `No migration` throw test (`migrate({}, -1, {})`) stays untouched.)

- [ ] **Step 2: Run to fail** (missing keys/types).

- [ ] **Step 3: Implement** — schema: add types, extend `WidgetToggles` + `defaults()` (`notes: { text: '', updatedAt: 0 }`, `worldClocks: []`, `countdowns: []`, widget defaults per Interfaces), bump `CURRENT_VERSION` to 2, add optional `sunriseISO`/`sunsetISO` to `WeatherSnapshot`. migrations:

```ts
export const migrations: Record<number, Migration> = {
  // v1 -> v2: widget toggles gained nested keys (bookmarks/notes/clocks/countdown).
  // Nested keys are exactly what the final default-merge does NOT backfill.
  1: (data) => {
    const d = defaults()
    const settings = (data.settings ?? {}) as Record<string, unknown>
    const widgets = (settings.widgets ?? {}) as Record<string, unknown>
    return {
      ...data,
      settings: {
        ...d.settings,
        ...settings,
        widgets: { ...d.settings.widgets, ...widgets },
      },
    }
  },
}
```

- [ ] **Step 4: All tests green; build green.** Existing wrapper tests seed `'aurora:version': CURRENT_VERSION` symbolically, so they track the bump automatically — verify none hard-code `1`.

- [ ] **Step 5: Commit + push** — `feat: schema v2 — notes, world clocks, countdowns, new widget toggles (first real migration)` + footer.

---

### Task 22: Polish batch — dialog stack, stale-weather revisibility, radiogroup aliases, tab-order note

**Files:**
- Create: `src/lib/dialogStack.ts` + `src/lib/dialogStack.test.ts`
- Modify: `src/settings/Drawer.tsx`, `src/newtab/widgets/todo/TodoPanel.tsx`, `src/newtab/widgets/timer/TimerWidget.tsx`, `src/newtab/widgets/palette/Palette.tsx`, `src/newtab/widgets/weather/useWeather.ts`, `src/settings/SettingsPanel.tsx`, `src/settings/SettingsPanel.test.tsx`

**Interfaces:**
- Produces (dialogStack.ts): `useDialogEscape(onClose: () => void, active?: boolean): void` — registers on mount/active, unregisters on cleanup; ONE document-level listener (module singleton) closes only the most-recently-registered active dialog per Escape press.

- [ ] **Step 1: Failing dialogStack tests** (jsdom pragma): register A then B → dispatch Escape on document → only B's onClose called; second Escape → A's; unregister-on-cleanup (unmount B before pressing → A closes); an Escape already `defaultPrevented` (e.g. consumed by the palette input's own handler) is ignored. Implement as a module-level array of entries; the singleton listener attaches when the first entry registers and detaches at zero entries; `e.preventDefault()` when it closes one.

- [ ] **Step 2: Implement + migrate all four dialogs** — Drawer/TodoPanel/TimerWidget replace their own document listeners with `useDialogEscape(onClose, open?)`; Palette keeps its element-level input handling for editing keys but replaces its dialog-level Escape with the hook (its focus trap means the document listener still fires — the stack must run in the CAPTURE phase? No: keep bubble phase; the palette input's key handler must NOT stopPropagation for Escape anymore — remove any such call so the stack sees it; the stack's newest-first rule closes the palette since it registered last). TodoPanel's new-list draft input keeps its local Escape-to-cancel via `e.stopPropagation()` — that's a deliberate inner consumer and documents itself.

- [ ] **Step 3: Stale weather on revisibility** — in `useWeather`, add an effect: `document.addEventListener('visibilitychange', handler)` where handler refetches iff `document.visibilityState === 'visible'` AND snapshot is older than MAX_AGE_MS (guarded by the existing `inFlight` ref); cleanup removes it.

- [ ] **Step 4: Radiogroup ArrowUp/ArrowDown** — alias to ArrowLeft/ArrowRight in `SettingsPanel`'s `onThemeKeyDown`; extend the existing keyboard test with an ArrowDown case.

- [ ] **Step 5: Tab order** — verify with a Playwright tab-walk probe; if moving the weather/timer mounts earlier in `App.tsx`'s DOM (before the gear button) preserves layout (they're `fixed`-positioned — it does), do it so the walk becomes: search → focus → links → refresh → weather controls → timer pill → Tasks → gear. Report the final measured order.

- [ ] **Step 6: Verify + commit + push** — full suite, build, preview (open Tasks AND Timer panels, press Escape once, screenshot proving only the newest closed — extend preview or use a throwaway probe and report output). `fix: newest-first dialog stack, weather revisibility refresh, radiogroup arrows, tab order` + footer.

---

### Task 23: Export / import backup

**Files:**
- Create: `src/lib/backup.ts` + `src/lib/backup.test.ts`
- Modify: `src/settings/SettingsPanel.tsx` (+ its test), `README.md` (Data section)

**Interfaces:**
- Produces: `serializeBackup(data: AuroraData): string` (pretty JSON envelope `{ app: 'aurora', version: CURRENT_VERSION, exportedAt: string, data }`); `parseBackup(raw: string): { ok: true; data: Record<string, unknown>; version: number } | { ok: false; reason: string }` (rejects non-JSON, wrong `app`, missing/absurd version, non-object data). Settings → new "Data" section: Export button (builds a Blob URL, `<a download>` click, revokes) and Import (file input → parseBackup → `migrate(data, version)` → CONFIRM step showing counts («Replace your current data with this backup from 2026-07-26? Lists: 2, links: 5…») → on confirm, write every `DataKey` via storage and stamp the version key through `driver`-level writes — simplest: `storage.set` per key; version needs no write since migrate ran to CURRENT).

- [ ] TDD `backup.ts` (round-trip: serialize → parse → deep-equal data; each rejection reason). UI follows the drawer's section pattern; confirm state is inline (two-step button, no window.confirm). Extend SettingsPanel.test: export produces a parseable envelope (spy on URL.createObjectURL), import happy-path writes storage (memoryDriver assert), malformed file shows the error copy and writes nothing.
- [ ] README Data section: what's included, photos excluded, how to restore.
- [ ] Verify (suite/build/preview no-errors) + commit + push — `feat: JSON export/import backup` + footer.

---

### Task 24: Weather details row

**Files:**
- Modify: `src/services/weather/openMeteo.ts` (+test), `src/services/weather/units.ts` (+test), `src/newtab/widgets/weather/WeatherWidget.tsx`, `scripts/preview.mjs`

**Interfaces:**
- Produces: PARAMS gains `&daily=sunrise,sunset&forecast_days=1`; mapper sets `sunriseISO: data.daily?.sunrise?.[0]`, `sunsetISO: data.daily?.sunset?.[0]` (absent-safe). `units.ts` gains `displayWind(kmh: number, units): string` (`"14 km/h"` / `"9 mph"`, rounded) and `clockTime(iso: string, use24: boolean): string` (reuses formatClock via a Date — or slice-based like compactHour but with minutes: `"5:42 AM"` / `"05:42"`).

- [ ] TDD the two unit helpers + extend the provider test (payload gains `daily`, expectations gain the ISO fields + absent-daily case keeps them undefined).
- [ ] Expanded panel: below the hourly grid, one quiet `text-xs text-fg-muted` row: `Feels 31° · Wind 9 mph · Humidity 62% · ↑5:42 AM ↓8:31 PM` (sun times only when present; ↑/↓ get `aria-label`s "Sunrise"/"Sunset" via wrapping spans).
- [ ] Verify incl. fresh `weather-expanded.png` (controller reviews) + commit + push — `feat: weather details row — feels-like, wind, humidity, sun times` + footer.

---

### Task 25: Multi-photo background gallery

**Files:**
- Modify: `src/lib/idb.ts`, `src/services/photos/index.ts`, `src/newtab/components/Background.tsx`, `src/settings/SettingsPanel.tsx` (+test), `src/newtab/components/Background.test.tsx`
- Test: `src/services/photos/rotation.test.ts` (unchanged — rotation logic is already count-generic)

**Interfaces:**
- Produces (idb.ts v2): DB version 2 upgrade keeps store; API becomes `addUploads(files: File[]): Promise<void>` (keys `photo:<uuid>`), `listUploads(): Promise<{ key: string; blob: Blob }[]>` (migrates a legacy `'user-photo'` entry to a `photo:` key on first list — lazy migration), `removeUpload(key: string): Promise<void>`; `getUpload()` deleted (update all callers).
- Background upload mode: rotation over the gallery — reuse `resolvePhoto/nextPhoto` with `count = uploads.length`; object URLs created per current photo and revoked on change; refresh button now also shows in upload mode when count > 1. Empty gallery in upload mode → bundled fallback → gradient (cascade fixed while we're here: upload-empty falls to bundled, closing the old parked ruling).
- SettingsPanel Background section: multi-file input (`multiple`), thumbnail grid (object-URL `<img>`s, ~56px, revoked on unmount) each with a focusable ✕ delete; the existing `uploadedAt` nonce stamps on every add/remove so other tabs re-read.

- [ ] idb tests are still impractical in jsdom (no IndexedDB) — keep idb.ts thin/untested; the LOGIC additions (which photo shows for a given prefs+count, cascade order) extend `Background.test.tsx` with a mocked uploads module (`vi.mock('../../lib/idb', ...)`): upload mode with 2 mocked uploads shows an img and refresh button; upload mode with 0 falls back to bundled; still no `onPrefsChange` in gradient mode.
- [ ] Verify: suite/build/preview + a manual-probe screenshot after seeding the gallery via the settings file input is NOT automatable headlessly (file chooser) — instead extend the probe with `page.setInputFiles` on the settings file input using two repo photos as fixtures, screenshot the thumbnail grid (`settings-gallery.png`) and the resulting background. Controller reviews both. Commit + push — `feat: multi-photo background gallery with per-photo management` + footer.

---

### Task 26: Bookmark folders bar

**Files:**
- Create: `src/services/bookmarks.ts` + `src/services/bookmarks.test.ts`, `src/newtab/widgets/bookmarks/BookmarksBar.tsx`, `src/newtab/widgets/bookmarks/FolderPopover.tsx`
- Modify: `src/manifest.ts` (`'bookmarks'` permission), `src/newtab/App.tsx`, `src/settings/SettingsPanel.tsx` (WIDGET_LABELS gains `bookmarks: 'Bookmarks bar'` — plus labels for notes/clocks/countdown added HERE once for Tasks 27–28), `README.md` (privacy + feature), `scripts/preview.mjs`

**Interfaces:**
- Produces (services/bookmarks.ts): `interface BookmarkItem { id: string; title: string; url: string }`, `interface BookmarkFolder { id: string; title: string; items: BookmarkItem[]; folders: BookmarkFolder[] }`, `interface BarModel { folders: BookmarkFolder[]; loose: BookmarkItem[] }`; PURE `toBarModel(tree: ChromeBookmarkNode[]): BarModel` (finds the bookmarks-bar node — the root child whose `folderType === 'bookmarks-bar'` when present, else id `'1'`, else first non-url child — and maps it, untitled folders → 'Folder', dropping nodes with neither url nor children); `loadBarModel(): Promise<BarModel>` (thin `chrome.bookmarks.getTree()` wrapper — the only chrome.* touch, never imported by tests transitively at call time).
- Bar UI: top-center `fixed left-1/2 top-4 -translate-x-1/2 max-w-[52vw]` row; folder chips (folder SVG glyph + name, `text-xs text-fg-muted hover:text-fg` on pill borders like list tabs); loose bookmarks as favicon chips; beyond ~8 chips → `»` chip whose popover lists the rest. One popover open at a time (component state); popover = palette surface, items with `faviconUrl()` icons, subfolders listed with the folder glyph → clicking drills in (breadcrumb "‹ Back" row at top); Escape via `useDialogEscape`; click-outside closes (backdrop pattern from Palette, no aria-hidden nesting).
- Gate on `settings.widgets.bookmarks` (default OFF — a bar of chrome only bookmark users want). Empty bookmarks bar → render nothing (no empty-state chrome).

- [ ] TDD `toBarModel` with fixture trees (folders+loose split, nesting, untitled folder, empty bar, missing bookmarks-bar node). UI per pattern; mount `<WidgetBoundary name="bookmarks">` inside `<main>`. Preview: seed `settings.widgets.bookmarks = true` in the settings merge-seed, BUT headless Chromium has an empty bookmarks tree — the bar renders nothing; verify no-crash + capture `newtab.png` unchanged, and add a `toBarModel` fixture-driven RTL test of `BookmarksBar` rendering (mock the loader) asserting chips + popover open/drill-in/back. Controller reviews code + test evidence in lieu of a populated screenshot (noted honestly in the report).
- [ ] Verify + commit + push — `feat: bookmark folders bar with drill-in popovers` + footer.

---

### Task 27: Notes scratchpad

**Files:**
- Create: `src/newtab/widgets/notes/NotesWidget.tsx`, `src/newtab/widgets/notes/NotesPanel.tsx`
- Modify: `src/newtab/App.tsx`, `scripts/preview.mjs`

**Interfaces:**
- Consumes: `notes` key (Task 21), `useDialogEscape` (Task 22).
- Produces: `NotesWidget` = pill `fixed bottom-4 left-16` labeled "Notes" (gate/inner split per TimerWidget precedent), lazy `NotesPanel` `fixed bottom-16 left-4 z-30 w-80 h-64` on the panel surface: one `<textarea>` (label sr-only "Scratchpad"; `bg-transparent`, no border, `resize-none`, full size, placeholder "Jot something down…"), autosaves debounced 500ms via `storage.set('notes', { text, updatedAt: Date.now() })`, flushes pending save on unmount/close; external updates (other tab) apply when local textarea is not focused (focused local edits win — last-writer-wins is acceptable, note it).

- [ ] Implement per pattern (no new pure logic ⇒ no new unit tests; debounce via `setTimeout` ref). Preview: click Notes pill, type "Remember the milk", wait ~700ms, screenshot `notes-panel.png`, reload page, reopen panel, assert text persisted via `page.locator('textarea')` inputValue (probe assertion printed in output).
- [ ] Verify + commit + push — `feat: notes scratchpad` + footer.

---

### Task 28: World clocks + countdown

**Files:**
- Create: `src/lib/worldTime.ts` + test, `src/newtab/widgets/clocks/WorldClocks.tsx`, `src/newtab/widgets/countdown/CountdownLine.tsx`
- Modify: `src/newtab/App.tsx`, `src/settings/SettingsPanel.tsx` (+test), `scripts/preview.mjs`

**Interfaces:**
- Produces (worldTime.ts): `zoneTime(zone: string, use24: boolean, now: Date): string` (Intl.DateTimeFormat with timeZone, hour/minute, hour12 = !use24 — invalid zone returns '—' via try/catch); `daysUntil(dateISO: string, todayKey: string): number` (UTC-noon parse both, round; negative = past); `countdownPhrase(name: string, days: number): string | null` (`null` if days < 0; `"${name} is today."` at 0; `"1 day to ${name}."`; `"${days} days to ${name}."`).
- WorldClocks: gated on `widgets.clocks` && `worldClocks.length > 0`; centered row directly under `<Greeting />` (inside the clock boundary group, after Greeting): `Tokyo 9:41 PM · London 1:41 PM` in `text-sm text-fg-muted`, ticking via `useNow(30_000)`, gate/inner split.
- CountdownLine: gated on `widgets.countdown`; renders the NEAREST upcoming (min non-negative daysUntil) countdown as one `text-sm text-accent` line under WorldClocks; none upcoming → null.
- Settings: two new sections. "World clocks": list of zone rows (label input + remove) + add row with a `<datalist>` of `Intl.supportedValuesOf('timeZone')` (input + datalist beats building a search dropdown) and a label field (defaults to city segment of the zone); max 4 (add row hides at cap). "Countdowns": rows (name, `<input type=date>`, remove) + add row; store via `storage.update`.

- [ ] TDD the three pure functions (zone formatting with a FIXED now via `new Date('2026-07-26T15:00:00Z')` + known zones UTC/Tokyo; daysUntil across today/tomorrow/past/leap-safe; phrase cases incl. null).
- [ ] Preview: seed `worldClocks: [{zone:'Asia/Tokyo',label:'Tokyo'},{zone:'Europe/London',label:'London'}]`, `countdowns: [{id:'c1',name:'Launch',date:'<today+14 computed in evaluate>'}]`, widgets.clocks/countdown true in the merge-seed; screenshot shows both lines (`newtab.png`); controller reviews.
- [ ] Verify + commit + push — `feat: world clocks row and countdown line` + footer.

---

### Task 29: v1.1 wrap — README, version, final visual pass

**Files:**
- Modify: `README.md`, `src/manifest.ts` + `package.json` (→ 1.1.0)

- [ ] README: features list gains bookmarks bar/notes/clocks/countdown/gallery/backup; permissions section explains `bookmarks` (read locally, rendered locally, never transmitted); Data section verified present (Task 23).
- [ ] Version 1.1.0 both files.
- [ ] Full verify: suite, build, `node scripts/preview.mjs` end-to-end (all screenshots re-captured), `dist/manifest.json` sanity (permissions list, icons, version). Controller does the full visual pass.
- [ ] Commit + push — `feat: v1.1.0 — bookmarks bar, notes, clocks, countdown, gallery, backup` + footer.

---

## Out of scope

Top sites, mini calendar, habit streaks, pomodoro count, quote favorites (offered, not picked — spec backlog), ICS calendar, everything in the rejected list.
