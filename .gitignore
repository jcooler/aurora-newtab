# Archived Tide extension — kept local only, never committed
legacy/

node_modules/
dist/
coverage/

# Claude Code's local working dir — per-machine settings (settings.local.json)
# and the board-worktree scratch (worktrees/). Never committed; ignoring the
# whole dir keeps a stray `git add -A` from sweeping it in.
.claude/
tmpclaude-*
*.log
.DS_Store
Thumbs.db

.playwright-profile/
.playwright-profile-store-shots/
.playwright-mcp/
screenshots/

# Packaging output: the zip and screenshots are LOCAL, binary deliverables
# for Jon (Chrome Web Store launch) — never committed. The .md docs in
# release/ (store-listing.md, LAUNCH-CHECKLIST.md) ARE tracked: they're the
# only record of the launch decisions (e.g. the Data-Usage disclosure
# reasoning) and belong in git history like any other doc.
release/*.zip
release/store-shots/

# Phase A/B photo curation scratch: downloaded candidates + contact sheets.
# Working files for controller review only — never shipped, never committed.
.photo-work/

# Product-concept renders Jon drops in for vision discussions — reference
# imagery for design sessions, not source and not shipped.
output/
.playwright-profile-forensic/
.playwright-profile-forensic-weather/
.preview-forensic-dist/
.forensic-short-height-out/

# NL-P2 anchored-canvas witness (scripts/preview-nl-p2.mjs) scratch output.
.playwright-profile-nl-p2/
.playwright-profile-nl-p2-debug/
.preview-nl-p2-dist/
.preview-nl-p2-out/
