# Archived Tide extension — kept local only, never committed
legacy/

node_modules/
dist/
coverage/

.claude/settings.local.json
tmpclaude-*
*.log
.DS_Store
Thumbs.db

.playwright-profile/
.playwright-profile-store-shots/
.playwright-mcp/
screenshots/

# Packaging output: the zip, screenshots, and listing copy are LOCAL
# deliverables for Jon (Chrome Web Store launch) — never committed.
release/

# Phase A/B photo curation scratch: downloaded candidates + contact sheets.
# Working files for controller review only — never shipped, never committed.
.photo-work/
